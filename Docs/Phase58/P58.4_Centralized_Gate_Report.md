# P58.D 严格 Gate 验收报告

## 状态

P58.D 已完成严格 Gate 并通过 attestation。全部正式证据绑定到同一干净
Git 候选：

- commit：`be2b0cdd4988327572145f94b3bc871d58c54a90`；
- tree：`4f8632b1a26527223f305446636b167aae77e393`；
- Gate Run ID：`P58-be2b0cd-20260830-001`；
- 原始 Gate 状态：`gate_attested`；见证提交的路径白名单恢复见下文。

## Attestation 恢复记录

首次见证提交 `110850e` 同时更新了本人读报告，关闭器以 `ASPW3032` 正确拒绝：
见证提交只允许 Phase state、closeout 和标准 Gate Summary JSON。恢复过程不改写或强制推送
Git 历史，而是重新打开 P58，在候选冻结前完成本报告，并为新候选生成产品路径同一性证据。
新候选仅允许流程规则、lesson、P58 文档与状态变化；`Source`、产品 `Build`、`Tests`与
`be2b0cd` 必须完全一致，才能复用已完成的 UBT 与 384/384 Automation 证据。

## 集中修复

- 含泛型值类型的 binding descriptor 统一提升到 schema 19，避免递归容器与旧 schema 混用；
- 保留 `TArray<UObject*>` 的 ObjectHandle 支持，只对真正的复合容器强 UObject 叶子
  fail-closed；
- 复合强引用对象的拒绝用例改为 `TMap<FName, TObjectPtr<UObject>>`，与平面数组能力分离；
- 同步修正 schema、delegate 参数方向、生成 binding 数量及授权包总数的测试基线。
- Semantic 合同升级到 schema 17 / `1.19`，复合 wrapper、token field 与内部构造函数
  必须来自可执行 reference source；主源码伪造同形 `FAvid*` 类型会 fail-closed；
- C# Build Invoker 和递归 Semantic/GuestIR/WASM 子步骤统一使用 PowerShell 7，修复
  内容寻址 generated package/source 在长路径下被 Windows PowerShell 误判缺失的问题；
- 新增真实 generated facade 编译闭环：合法 `avid_on_begin_play` 从 `UE.Self` 转成生成
  对象 wrapper，读取 Array/Set/Map `UPROPERTY` facade 并编译容器 read/write/find/upsert/remove。

## 能力结果

- FText、soft/weak object、递归 `TArray`、`TSet`、`TMap` 进入统一复合值 capability；
- delegate ref/out 从 descriptor、C# Guest、VM import 到 Runtime transaction 形成闭环；
- 当前 gameplay profile 生成 371 个函数和 2 个属性，共 373 个 reflected bindings；
- 授权 import 包共 390 项，其中 373 项来自反射生成，17 项为共享基础 import；
- 平面 UObject 数组继续通过 ObjectHandle 交互；复合容器内强 UObject 引用在 GC anchoring
  完成前保持拒绝，避免悬空引用。

## 严格 Gate 证据

| 验证项 | 结果 | 证据 |
| --- | --- | --- |
| 干净架构检查 | 通过 | 证据 tree 为 `4f8632b1...`，输入 clean |
| Agent Harness | audit 通过，合同测试 7/7 | 外置日志哈希已写入 Gate 报告 |
| .NET 工具链 | 253/253 passed | Semantic 85、C# Guest 111、Frontend 7、Guest IR 35、WASM Backend 15 |
| PowerShell 合同与集成 | 119/119 passed | Cache/Publication/Build/Workflow 七组 runner |
| C# 生命周期产物 | `direct_abi_built` | manifest 的 `Saved/...wasm` 路径、文件和 SHA-256 一致 |
| UE5.8 Editor 无清理构建 | 通过，10 actions，7.74 秒 | Wasmtime C API 实际编译并复制 DLL |
| 隔离环境定点回归 | 2/2 Success | 队列清空，RequestExit 0，进程退出码 0 |
| 全量 Automation | 384/384 Success，0 Fail | 队列清空，RequestExit 0，进程退出码 0 |

全量运行中出现本地代理/EOS 断连和一次 Zen DDC 恢复超时告警，但没有导致 AvidScript
用例失败；它们不作为产品缺陷计入。

## 性能采样

| 场景 | 平均耗时 | p50 | p95 | 峰值内存 |
| --- | ---: | ---: | ---: | ---: |
| FText | 0.151 us | 0.145 us | 0.212 us | 88 B |
| String Array | 1.663 us | 1.667 us | 2.153 us | 1104 B |
| Struct Array | 1.690 us | 1.677 us | 1.743 us | 1104 B |
| String Set | 3.205 us | 3.173 us | 3.450 us | 1104 B |
| Name/String Map | 3.561 us | 3.565 us | 3.603 us | 1808 B |

这些数据证明当前性能 Gate 用例通过，但不单独作为相对 Puerts、UnLua 或 AngelScript
全面领先的结论；跨框架领导力仍需在固定硬件、固定 workload 和同等语义下持续验证。

## Gate 运行预算

本轮如实记录 UBT 3 次、Automation 启动 4 次、完整 Gate 3 轮。超出原预算的原因是
隔离候选初次未映射忽略的 Wasmtime SDK，以及生命周期清单曾从物理候选路径生成。
修正 junction 根路径后先完成 2/2 定点回归，再执行唯一有效的最终 384/384 全量 Gate。
这些失败是外置 Gate 装配问题，未修改产品代码。

## 关闭边界

Gate attestation 已完成。接下来只允许提交 Phase 58 状态与本阶段中文收尾文档，
且该提交必须是验证提交 `be2b0cd` 的直接子提交。3 个 protected dirty 用户文件不得进入暂存区。
