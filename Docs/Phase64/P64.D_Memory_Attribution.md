# P64.D World 内存归因

状态：诊断接口与 100 次切图归因已交付，D07 仍在修复中；不缩小既定长稳目标。

## 问题与口径

既有一小时样例完成 877 次切图；UObject、Session、订阅与对象句柄有界，但相对第三轮的
进程 physical/virtual 内存增加约 306/73 MiB。代码包含释放路径不能证明没有 native 泄漏，
探针保留 JSON 也不能解释全部增长。原始证据见 [World 连续运行](P64.D_World_Soak.md)。

Win64 的 `FWindowsPlatformMemory::GetStats` 将 `WorkingSetSize` 写入 UsedPhysical，
将 `PagefileUsage`（commit charge）写入 UsedVirtual；后者不是 WASM 预留地址空间。
工作集、提交量、UE 容器容量和 JSON 估算是不同口径，不能相加或直接相减。

本批先执行相同 `VerifyWorld` 的 100 次切图，保留三轮预热、真实 GC、存档哈希链和原有动作验收。
不清缓存、不换轻量地图、不关闭安全机制。短运行只定位增长来源，不替代一小时或真实游玩验收。

## 冻结接口

- VM 新增 `AvidScriptVmDiagnostics.h`，导出值类型 `FAvidScriptVmMemorySnapshot` 和
  `CaptureAvidScriptVmMemorySnapshot()`。无 Runtime/Editor 依赖，无后端指针，无 Guest API/ABI 改动。
- 实例字段为 `uint64 BackendCreatedCount / BackendDestroyedCount / BackendLiveCount`；在
  `IAvidScriptVmBackend` 构造/析构处计数，拒绝隐式复制。并发读取必须得到一致的生命周期三元组。
- 缓存字段为 `int32 ArtifactCacheEntries / ArtifactCacheCapacity / AttestationEntries / AttestationCapacity`，
  `uint64 ArtifactCacheAllocatedBytes / AttestationAllocatedBytes`。在既有 compiler lock 内读取，
  累加容器容量及其自有数组/字符串容量，不复制制品、不触碰 LRU、不清缓存或登记新授权。
- 字节数仅覆盖 UE 自有容器的 `GetAllocatedSize`，不等于 Wasmtime 内部堆、代码页、线性内存、
  OS 虚拟地址保留或总进程内存。BackendLiveCount 仅是 C++ backend 对象，不声称证明所有 C/Rust 资源释放。
- 创建/销毁计数只发生于实例生命周期冷路径；快路径 Host crossing、Tick、执行指令不增加探针代码。

## World 报告

每轮 GC 后 `gc.attribution` 增加版本 1 的值快照：

- `backend_created / backend_destroyed / backend_live`。
- `artifact_cache_entries / artifact_cache_capacity / artifact_cache_allocated_bytes`。
- `attestation_entries / attestation_capacity / attestation_allocated_bytes`。
- `observer_retained_cycles / observer_retained_actions / observer_json_estimated_bytes`。

JSON 估算复用 UE 的 `FJsonValue::GetMemoryFootprint`，每个已完成周期只计算一次，再累计外层数组容量。
标明 `observer_estimate_kind=ue_json_memory_footprint`；该估算不含 shared pointer 控制块、分配器保留页
和最终报告序列化临时空间，不与进程内存作严格减法推导“剩余泄漏”。既有 physical/virtual、实际
backend mode、资源计数保持原义；旧报告缺少 attribution 时不伪装成零，新 CLI 默认要求完整字段。

## 并行与验收

1. VM owner 实现诊断及聚焦生命周期/缓存只读回归；不修改 Editor/Build/文档。
2. 主线实现 World 采样、PowerShell 严格读取及必要拒绝用例；统一完成一次 no-clean UBT、受影响
   Automation 和 100 次真实切图。存在持续增长时继续定位，不靠提高内存阈值关闭 D07。
3. 同步结果、限制、README 与 Phase 债务，按本小节白名单提交推送。人工读档反馈独立保留。

## 结果

### 集中验证

- UE5.8 canonical Editor no-clean UBT：22 个动作，39.42 秒，通过；引擎、工程、插件三份
  `UnrealEditor.modules` BuildId 一致。未清理 target、缓存或任何用户进程。
- `AvidScript.VM.Diagnostics`：6/6 通过，包含新增的 BackendMemoryLifecycle、
  BackendMemoryConcurrentSnapshot、ArtifactMemoryReadOnlySnapshot；报告 failed/notRun/inProcess 均为 0，
  Queue Empty、TestExit、进程 exit 0 完整。测试本体累计约 0.051 秒，不包含 Editor 启动时间。
- World runner 合同：53/53 通过，覆盖旧报告缺字段、错误数值类型、不一致计数、容量越界和历史计数倒退。
  后端存活数增长会如实保留为观测值，不被转换成“无泄漏通过”。
- Harness audit 通过。架构检查只报告工作树输入与 HEAD 不同，包含既有 protected dirty 文件，
  不作为干净候选 Gate。未重跑完整 Automation、.NET 或 benchmark，不扩充历史全量基线。

构建证据：`%TEMP%/AvidScriptP64MemoryBuild/f67d7755eb894f56b12c65f1922ddf4b/ubt.log`。
Automation：`%TEMP%/AvidScriptP64MemoryAutomation/470093050bf345a690bd738528adf0e5/`，
包含 `automation.log` 和 `Report/index.json`。World 合同 fixture 位于
`%TEMP%/AvidScriptUiSaveWorldContracts/a48b29c69c674db9924d5070e6ed3f9c/`。

### 同口径切图

实际运行 **411.001 秒、100 次 travel / 101 个 World / 501 个动作**，全部通过；最后新实例读回
**100/Loaded**。每轮旧 World、Host、Component、Widget、SaveGame 均回收，活跃 Session 为 1，
UObject 数恒为 52523。实际后端是 **Wasmtime 45 JIT / wasm_bytecode**，不是包内 AOT；没有变更
此 World 场景的默认加载方式。三轮预热基线不变。

| 完成轮次 | 工作集 MiB | 提交量 MiB | JSON 估算 MiB | Backend 创建/销毁/存活 |
| --- | ---: | ---: | ---: | --- |
| 3 | 1768.8359 | 1623.3984 | 0.0621 | 10 / 8 / 2 |
| 10 | 1770.7852 | 1620.1992 | 0.2126 | 24 / 22 / 2 |
| 25 | 2002.2578 | 1629.5664 | 0.5351 | 54 / 52 / 2 |
| 50 | 2025.0938 | 1633.7461 | 1.0726 | 104 / 102 / 2 |
| 75 | 2028.1836 | 1637.4531 | 1.6095 | 154 / 152 / 2 |
| 100 | 2029.6055 | 1638.9336 | 2.1472 | 204 / 202 / 2 |

相对第三轮，工作集增加 **260.770 MiB**、提交量增加 **15.535 MiB**。
第 50～100 轮仍增加 **4.512 / 5.188 MiB**，同期 JSON 估算增加 **1.075 MiB**。
两段数据均保留，不能只挑后半段来改写原始基线。

全部 100 个快照的 backend_live 恒为 2；这是进程级 C++ 后端对象计数，不等于 Session 数量。
制品缓存和 attestation 的 entry/allocated_bytes 全程均为 0，容量策略均为 32；因此本轮增长
没有表现为后端对象数量累积或这两张表的分配增长。JIT 引擎内部缓存不在这些字段的覆盖范围。
第 100 轮估算包含 100 个周期、499 个历史动作，随后两次最终恢复动作不在该 GC 时点内。

run：`f821ba41a7d64329ade3c9259acfb62f`，项目
`Saved/AvidScript/UiSaveDemo/<run_id>/world.json` SHA-256：
`a6abfeeafb39b939a2cacd6ea5c44a3e0ab096934064a9187df3f0c896f2764b`；
`verify-world.json` SHA-256：`6f9aec1057fbc14645eeac1134e63048398cab9745e7b3c527797532501feef4`。
最后存档 2050 字节，SHA-256：`478e98d3e9b57499df336986241e144dd0cce99d9312b25fef0176eeec9dc202`。
原生正常退出、日志关闭、外层独立校验与 exit 0 完整；入口、采样源码、VM/Editor DLL、catalog
共七项运行前后哈希一致。`long_run_verified=false`，不替代既有一小时证据。

### 结论与下一步

本批交付的是可重复使用的冷路径诊断与一轮真实归因，不是内存优化结果，也不声明性能领先或无泄漏。
当前还不能用 JSON 估算解释全部增长，D07 保持 Fixing。

下一批直接测量 UE Trace 与分配器/原生资源，而不是继续只增加切图时长：

- 当前日志确认 UE allocator 为 Mimalloc；记录其已分配/保留页口径，不通过强制 trim 改变测量对象。
- UE5.8 `UE::Trace::GetStatistics` 已提供 MemoryUsed、FixedBufferAllocated、CacheAllocated 等读数；
  后续将与同一 GC 时点关联，不关闭 Trace 或清缓存来制造较小结果。
- 若 UE 自有存活分配不足以解释提交量，再获取原生堆与 JIT 映射的分配栈；C++ backend 计数稳定
  不能替代 C/Rust 内部资源的释放证据。确认增长来源、完成必要优化后再跑一小时复验。
