# P64.D 包内 World 长稳与有界观测合同

状态：架构 v15 已冻结，实施与自动验收完成；D07 已验证，人工视觉、网络与移动端 Gate 仍保持打开。

## 目标

修复后的 Editor 一小时复验仍增长 Physical 322.965 MiB、Virtual 82.109 MiB。已知 Binding 字符串
参数帧和 backend 历史保留已消除，但 Editor Blueprint 引脚队列、工具线程、分配器保留与探针完整
JSON 历史尚未分开。下一批在正式 Win64 AOT 包中执行同一 World 生命周期，并让观测器自身有界，
取得更接近玩家进程的趋势。它是诊断与长稳证据，不是新的玩法实现。

## 模块边界

- 主插件 Runtime/Core/VM 不依赖验证模块，不新增 Demo 专用 Host API、生命周期 ABI 或 C# 旁路。
- `Tests/Unreal/AvidScriptValidation` 继续作为默认禁用、仅显式打包启用的 Runtime 验证插件。
- 公共 `IUiSavePackagedProbe` 只负责 `Start` 与析构；现有 write/read action probe 和新 world probe
  分别实现，报告与状态机拆文件，避免继续扩张一个探针类。
- 复用 `FUiSaveObservation`、`FUiSavePaths`、`CaptureRuntimeDiagnostics`、VM 只读内存快照和正常
  UMG/SaveGame/OpenLevel API。验证插件不访问 Editor-only Session getter，不持有旧 UObject 强引用。
- Editor 的现有完整历史报告保持兼容；包内 probe 采用有界证据 schema，不复制其线性历史设计。

## 入口合同

`Build/InvokeAvidScriptUiSavePackaged.ps1` 增加 `-Mode VerifyWorld`，默认 `Verify` 的双进程 write/read
行为、参数和结果保持不变。World 参数与 Editor 口径一致：

- `WorldCycles`：2..1000，默认 10；`SoakSeconds`：0..21600，默认 0。
- 原生参数：`AvidScriptUiSavePackagedProbe=world`、`AvidScriptUiSaveWorldCycles`、
  `AvidScriptUiSaveSoakSeconds`；同样要求 scenario、expected PackageId、RunId、隔离 UserDir。
- World 使用新的空 UserRoot，已有样例存档即拒绝；启动配置、receipt、exe 和 package 身份在运行前后
  都重新校验。外层超时至少为原生预算加 30 秒，不静默缩短时长。
- 自动化仍使用 `-nullrhi -unattended -nowrite`，只证明 headless 包内流程；可见窗口和物理点击由
  独立人工验收记录。

## 生命周期合同

每个新 World 必须从 `0/Ready` 和新 Session 开始。第一轮 Collect/Save，之后先 Load 上轮分数，
再 Collect/Save；每轮保存后至少停留 3 秒并确认状态、事件数和存档哈希不变，再通过 OpenLevel
重建同一真实地图。最后一次 travel 后还要在新 World 恢复最终分数，才可成功退出。

`OnWorldCleanup` 只读取停止后的诊断：Component/Guest EndPlay、Runtime 不再 live、owner 已释放、
停止态诊断拒绝且输出清空、四个按钮解绑、Widget 移出 viewport、宿主 SaveGame 引用清空且无迟到
事件。此处不把诊断拒绝冒充为私有 Session 指针可见；新 World 就绪后强制 GC，旧 World、Host、
Component、Widget、SaveGame 弱引用必须全部失效，新对象 identity 必须不同。每轮活跃脚本实例为 1，
订阅/按钮为 4，owned/pending/prepared 为 0，borrowed 为 7..8；实际 backend 必须是 Wasmtime AOT
serialized，live backend 与缓存 entry/bytes 不随轮次增长。

## 有界证据

原生报告保留根 schema_version=1，并以 `mode=world` 增加 `world_lifecycle`：

- 请求/完成轮数、World/travel/cleanup 计数、真实时长、最终分数、存档 SHA-256、long_run_verified。
- `checks` 记录累计 action、cleanup、GC、resource 通过数；任一失败立即终止，不把累计数替代检查。
- `evidence_chain_sha256` 从 RunId、PackageId 和每轮规范化字段滚动计算，外层验证格式、最终计数与
  报告/文件身份；不允许根据日志补造缺失结果。
- `samples` 只保留固定里程碑 1、2、3、5、10、25、50、100、200、500、1000、2000、5000、10000，
  以及不重复的最终轮；最大 15 项。每项含 Physical/Virtual、UObject、VM live/cache、资源与哈希。
- 当前动作和当前轮对象在完成后释放；失败时仅附带 `failure_cycle`。报告内存估算、sample count/capacity
  必须写入，成功时 observer retained cycle/action 均不随总轮数增长。
- 第三轮继续作为预热基线；报告 baseline/final/peak 与 50、100、最终里程碑。工作集、提交量、
  UObject 和 VM 计数分别解释，不将 WASM 4 GiB 地址预留当作实际提交量。

## 实施与验收

1. 独立实现公共 probe 接口、packaged world 状态机、报告/内存采样和严格 PowerShell validator。
2. 合同覆盖默认 Verify 无漂移、参数/模式拒绝、报告字段类型与边界、样本上限、累计计数、证据链、
   路径与身份；纯字段矩阵直接调用生产 validator，不逐项启动进程。
3. 集中执行一次 no-clean Editor UBT、受影响合同、Development BuildCookRun 与短包内 World 流程。
   先看 50/100 轮趋势；候选与包身份冻结后再执行一小时，不为通过修改基线、trim 或清缓存。
4. Shipping 至少完成短 World 兼容运行；一小时先以 Development 取得归因，二者不可互相替代。
5. 结果更新 README、D07 与收尾文档，按小节精确提交推送。人工窗口反馈只更新 D03，不写入自动字段。

## 成功边界

包内趋势显著低于 Editor 且 observer 计数/footprint 有界，可把 Editor 专属与探针开销从产品增长中
剥离；仍有持续增长则按实际栈/模块继续修复。只有包内一小时流程、资源边界、趋势归因和必要修复
均有证据时，才评估 D07 是否 Verified。短测、exit 0、单一端点或用户视觉反馈均不能单独关闭它。

## 当前实施结果（2026-09-05）

第一批实现已经落地公共 probe 接口、独立 World 状态机、内存/报告拆分、`VerifyWorld` runner 和严格
validator。PowerShell 合同最终 **80/80** 通过，包含 Development/Shipping 报告、参数边界、固定
里程碑、资源时刻和内存字段拒绝矩阵；启用验证插件的 no-clean Editor UBT 成功。

人工可视包复启时暴露了一项真实产品缺陷：永久 `UAvidScriptDelegateBridge` 类的函数表引用了运行期
复制、但未进入 root set 的 `AvidDelegate_*`。GC verifier 因此 Fatal。修复后动态函数在加入类表前
取得 root 生命周期，既有函数也会修复；按稳定签名复用并限制最多 4096 个唯一函数，不关闭 verifier。
新增 Automation 在真实多播绑定后执行 `CollectGarbage`，再验证函数仍可查找和广播，专项 **1/1**
Success，Queue Empty 与退出状态 0，日志 SHA-256 为
`295c398a1bc3f5828759298a4708da161e8b629e80cc95bd2e6c8dc734cf774c`。

新的 Development BuildCookRun 成功，fresh receipt **28/28**，package id 为
`832efd6dabd73139c5c162db675550c7b674d5dc32039afc35113a7d6489d00b`；`build.json` SHA-256 为
`4ac636c57f9a430595a859c66f4ecc129124cf0b98fe45149eabfb26c0282013`。同一冻结包依次完成：

| 包内运行 | 结果 | Physical：cycle 3 → final | Virtual：cycle 3 → final | UObject / backend / VM cache |
| --- | --- | ---: | ---: | --- |
| 3 cycles | 3/3，16 actions | `0 MiB` | `0 MiB` | `35580 / 2 / 0+0` |
| 50 cycles | 50/50，251 actions | `+72.984 MiB` | `+1.016 MiB` | 全部里程碑不变 |
| 100 cycles | 100/100，501 actions | `+2.172 MiB` | `+1.758 MiB` | 全部里程碑不变 |
| Development 3600 秒 | 1173/1173，5866 actions | `+76.16 MiB` | `+4.25 MiB` | `35580 / 2 / 0+0` 全部里程碑不变 |
| Shipping 3 cycles | 3/3，16 actions | `0 MiB` | `0 MiB` | `34282 / 2 / 0+0` 全部里程碑不变 |

100 轮耗时 307.312 秒，8 个固定样本，observer 约 42,380 bytes；50 轮独立进程的 Physical 工作集
出现较大波动，但 Virtual、UObject、backend live、artifact/attestation cache 均未同步增长，且 100 轮
未复现线性 Physical 趋势。三份 runner 证据 SHA-256 依次为
`6d3e868d50ee0dd3818b2639bb552fad76ca849fd39652e5e580067b8d5574db`、
`0193a017385e1ef731d3a6569e7c93e96df29b3e8b773bc4565aaf305994a0a3`、
`87290c0ab60b2cae70d30b59b57ddb7e9ca5c5d398b451642413f7915f51d108`。

两项纠偏均保留为失败证据：首次构建复用了缺少当前 `reference_source_sha256` 的旧 binding package，
在 Release 阶段拒绝且未启动 UAT；首次 3 轮原生报告已成功，但 validator 错把 Ready 时刻
`borrowed_entries=7` 与最终 Load 后根资源 `8` 要求相等。后者现按观测时刻分别断言并有拒绝合同。

Development 一小时使用同一冻结包和同一进程运行 3602.566 秒，完成 1173 次 travel、1173 次
WorldCleanup、1173 次 GC、1174 次资源快照和最终第 1174 个 World 读回 1173 分。12 个固定样本未超过
15 个容量，observer 汇总约 60,284 bytes；Session 恒为 1、UObject 恒为 35,580、backend live 恒为 2，
artifact/attestation cache entry 与 bytes 均为 0。Physical 在第 50 轮一次性跃升约 73 MiB，之后到
1173 轮只再增加约 3.15 MiB；Virtual 从第三轮到终点增加 4.25 MiB。该形态与 Editor 中逐轮保留全部
动作/周期 JSON 并增长 322.965/82.109 MiB 的口径显著不同，不支持 AvidScript 产品资源随 World
线性泄漏的假设，但也不宣称系统分配器驻留或整个进程为零增长。runner/native 报告 SHA-256 为
`6adfe5b864fd383b1376996788fc1a2db79784d8692511bfee9d9b0b5db4ba8d` 与
`6533bff336244add184f20da3b3262294f714fd45fdfb1e643ebceb09c2a2540`。

Shipping 首次 BuildCookRun 在 UAT 后由 receipt validator 拒绝：Development Generated Type pointer
不属于所选 Shipping catalog 变体。未降低校验；通过 `BuildCSharpScriptTypes.ps1 -HeadlessRelease
-PackageConfiguration Shipping` 重新发布，验证 pointer 在 catalog 中恰有一个 Win64/x86_64/Shipping
匹配，再用全新目录重跑。最终 fresh receipt **25/25**（23 UFS、2 NonUFS），`build.json` SHA-256 为
`83a3f4cfb94f50d1bdc466e66eb94bbdd094d7836c1846b4816e05a74c5a3767`。实际 Shipping Game 完成
3 次 World 循环、16 次动作、3 次 cleanup/GC 与第 4 个 World 读回，runner/native SHA-256 为
`b2b044fd0d59a088c70f543121f3244507884a9b763c4030e51e38a210e6e886` 与
`4895e3866ed7c8e2945de42bee0189599c1de611c61305ccbb981c1d2d1f1086`。验证后恢复本轮开始前备份的
Development pointer，未改变日常 Editor 选择。

据此，D07 所要求的产品泄漏修复、Editor/探针/分配器与包内进程分层、Development 一小时趋势及
Shipping 兼容证据均已完成，`P64-D07-WorldSoakMemoryAttribution` 可标记为 Verified。人工可视复验
仍属于 P64-D03；自动报告中的 `physical_click_verified=false` 与 `visual_verified=false` 保持原值。
