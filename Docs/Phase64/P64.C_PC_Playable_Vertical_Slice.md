# P64.C PC 可玩纵向切片

## 已实现

- `PickupRushScript.cs` 以 C# 实现 BeginPlay、Tick、Overlap、Timer、Gameplay Event、计时、收集、胜负与重开。
- `AAvidScriptPrimitiveHost` 仅提供通用 StaticMesh 与碰撞，不包含样例游戏规则。
- `pickup_rush` scenario 在 TopDown/TwinStick 世界中自动生成宿主并挂载内容寻址模块。
- `InvokeAvidScriptPickupRush.ps1` 统一发布当前 Reflection 绑定、编译模块、启动 Editor/Package 与校验报告。
- `InvokeAvidScriptStartupScenarioProbe.ps1` 为任意 packaged scenario 提供可复用的机器验收入口。
- UE 同步碰撞委托在 Guest 正忙时进入 64 项有界 FIFO，回调返回后派发；溢出仍 fail-closed 并计数。

## 当前证据

`Editor` 模式已通过：

- 模块 `avidscript.pickup_rush` 完成 C# 语义编译、WASM 生成和 Wasmtime AOT；
- TopDown 世界由 scenario 自动生成 1 个宿主并进入 BeginPlay/Tick；
- 5/5 个事件完成，Runtime 保持加载，丢弃回调为 0；
- 最终位置为 `(600, 0, 300)`，缩放为 `(2.5, 2.5, 2.5)`，可见且碰撞开启；
- UE5.8 no-clean Editor UBT 通过。

报告位于 `Saved/AvidScript/PickupRush/`，不写入仓库，也不包含个人路径。

## 待完成

Development 与 Shipping BuildCookRun、package scenario report 将在本批次集中执行。键鼠/手柄体验、
碰撞手感与画面效果保留为人工验收项，不以无头探针冒充。
