# P46 UE 反射属性绑定实施计划

**目标：** 在不手写逐属性 API 的前提下，让 C# 通过 descriptor v4 与缓存 Runtime plan 读取真实 UE5.8 `UPROPERTY`，并进入生命周期游戏逻辑闭环。

## P46.0 架构

- [x] 冻结第一等 `property_get` binding 模型。
- [x] 明确 v2/v3 兼容与 v4 canonical 字段。
- [x] 明确只读首切片和 setter/replication/reload transaction 边界。

## P46.1 选择与 Descriptor v4

- [x] 增加 reflected property selection/profile/result 数据合同。
- [x] 建立 property policy，并复用共享 reflected type policy。
- [x] Resolver 输出确定性 property compatibility report。
- [x] Descriptor v4 写入 `binding_kind`、`ue_member` 与 `cached_property_get`。
- [x] Parser 兼容 v2/v3，并严格验证 v4 identity、ordinal、ABI 与 import 唯一性。

P46.1A Result: class include/exclude、批量 discovery 与 strict explicit property 已落地；`AActor.CustomTimeDilation`、`InitialLifeSpan` 被共享 scalar policy 接受，`Tags` 数组产生结构化非致命拒绝。UE5.8 Editor scoped build 成功，PropertySelection Automation 2/2 通过。

P46.1B Result: 函数-only 包继续确定性发布 v3；包含属性时才发布 v4。一个函数与两个真实 Actor 属性可组成同一确定性 descriptor，shared parser 保留第一等 member kind；BindingDescriptor Automation 5/5 通过。

## P46.2 Runtime 与 C# Facade

- [x] Package load 缓存 `FProperty` 读取 plan，不在热路径查找成员。
- [x] Dispatch 复用对象 handle、type marshaller 和 guest memory contract。
- [x] Renderer 生成普通 C# 只读 property，并保持 semantic reachability 裁剪。
- [x] Architecture gate 锁定 property ownership 和无逐属性 switch 规则。

P46.2 Result: schema v4 属性包现可在 load 阶段解析并缓存真实 `FProperty*`，dispatch 通过对象 registry 与既有 marshaller 将值写入 WASM guest memory；C# facade 输出普通只读属性，canonical emission 与最小 runtime slice 均按 `binding_kind` 保留属性 import。真实 `AActor.CustomTimeDilation` 读取与无效 generation fail-closed 已通过 UE Automation。

## P46.3 真实闭环与收尾

- [x] C# 生命周期读取 `AActor.CustomTimeDilation` 并驱动可观察 UE 行为。
- [x] 验证错误属性、类型漂移、伪造 kind/ABI、无效 handle 与 debug map 组合。
- [x] UE5.8 no-clean build、聚焦 Automation 和必要 host contracts 全绿。
- [x] 写中文成果文档、错误记录、安全审计、提交并普通推送。

P46.3 Result: EngineGameplay 授权 profile 现由 115 个函数与 `AActor.CustomTimeDilation` 属性组成，正式发布 schema v4；真实 C# 样例在 `BeginPlay` 读取该属性，经 Roslyn reachability、三 binding 最小 slice、WASM backend 与 WAMR Runtime 驱动 Actor scale。冷/热 semantic cache、Tick、事务 reload commit、候选 rollback 与项目工作区流程均通过。
