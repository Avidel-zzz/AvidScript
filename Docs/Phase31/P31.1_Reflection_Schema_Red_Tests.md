# P31.1 反射 Schema RED 测试

状态：Done

## RED 证据

1. 新增 Editor 自动化后，模块编译因缺少 `AvidScriptEditorBindingSchemaGenerator.h` 失败。
2. 新增 manifest 契约测试后，编译因缺少 `ValidateManifestImports` 失败。
3. 重复导入测试首先报告生成器错误地接受重复 `(module, name)`。
4. 畸形 `required_imports` 测试首先触发 `LogJson` 类型错误，证明解析前缺少 JSON 类型门禁。

## 测试夹具修正

- PowerShell/C++ 双重字符串转义曾破坏 JSON 字面量，改用 C++ raw string。
- `TArray.Add(Array[0])` 在扩容时触发容器别名断言，改为先复制元素再追加。

这些夹具问题均在生产实现前单独修正，没有把测试故障当成功能 RED。
