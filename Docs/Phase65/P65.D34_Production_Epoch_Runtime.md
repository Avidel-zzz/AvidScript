# P65.D34：Win64 epoch 优化运行库候选

状态：生产补丁、版本身份、中性重建、UE 集成回归与干净候选完整矩阵已完成；正式性能门禁为 **16/18**。
最新离线发布候选 `8c032e9f` 已验证重复发布、实际替换、同源 Shipping 包内回归与 Windows Automation **468/468**。
D33 的 0.8848x 定位结果不能替代本节 `6188ebe3` 正式矩阵。

正式性能协议的 `wasmtime_toolchain` 输入哈希已同步到 `.3` 工具链锁，修正候选冻结时的
`ASP65L2006` 身份漂移拒绝；七个受控输入的范围、工作负载、采样配置和门禁阈值不变。

## 运行库身份与兼容边界

生产 patch 合并 D33 已通过 17/17 定向合同的 Win64 `PreserveAll` 实现。
补丁涉及原来的四个 C API 文件与三个 Cranelift epoch 文件；宿主 C ABI、公开导出、
编译配置和逐回边 epoch 检查频率保持原合同，其他目标平台保持原生成调用约定。

- toolchain：`avidscript-wasmtime-v45.0.0-patchset.3-win64-multiarch`。
- 新安装根：`Source/ThirdParty/Wasmtime/installed/Win64/v45.0.0-avidscript.3`。
- canonical patch SHA-256：`1635067d97b9af7fdc4c1cf85c5b4b0b0d9141a86ad1c1401c624484d2170b1c`。
- Wasmtime commit、Rust 1.93.0、六个 Cargo features、CPU/优化配置、安全检查均未改变。

`Wasmtime.Build.cs` 选择新的安装根，并继续将实际 DLL 哈希写入编译定义。
`ResolveAvidScriptWasmtimeRuntimeIdentity` 使用观测 DLL 哈希建立 runtime build identity，
因此新 DLL 与旧预编译产物的身份不同；不靠更改 workload 或降低校验来复用旧缓存。

新发布声明使用 patchset.3。可信发布校验器明确允许已知 `.2` 和 `.3` 两种布局，
由工具链版本决定唯一路径；Schema 的 `oneOf` 同时绑定版本与路径，禁止混装。
这使新安装器能够验证既有 `.2` 离线包，再执行替换。它没有放开任意版本或任意包内路径。

## 当前合同证据

- 工具链锁与合同通过，固定源码、patch hash、安全配置及七个补丁文件范围已核对。
- 发布器/安装器原有合同 **26/26**。
- Win64 bundled runtime 合同 **21/21**，包括当前版本选旧布局拒绝、未知工具链拒绝，
  以及 D32 实际 `.2` 离线包的验证、安装、替换和读回。
- Cook receipt 合同 **24/24**，Win64 fixture 同步使用 `.3` 路径。

原始记录保存在仓库外 `as34-release-contracts.log`、`as34-bundle-contracts-v2.log`、
`as34-receipt-contracts.log`。旧包替换测试的一次复跑曾在 staging 目录改名处被 Windows 拒绝访问，
同一流程前后两次成功；失败日志保留在 `as34-bundle-contracts.log`。
尚未证明该次占用的根因，也未把复跑成功表述为修复；最终离线候选还需重复替换验证。

## 新中性运行库

从锁定 archive 加完整 patch 在中性源码/缓存根重建成功。隐私报告
`binary-privacy-241babe40e59467b864a6f1bf49a4bd9.json` 为 passed，inventory_verified 为 true，
覆盖 **176 个文件 / 37,530,938 字节**。依赖声明覆盖 94 个 Cargo package 与 Rust 声明。
可信 bundled resolver 和主工程安装后的工具链 Verify 均通过。

| 身份 | SHA-256 |
| --- | --- |
| DLL | `82616c0c06fb605b6ea6dd15933f64ffe888e57cb612b0a2596e4999c60870a3` |
| 完整安装内容 | `bf8553b4675c23a5d69837f923c89095338ed78b292527fe03611790681d5b8d` |
| managed marker | `de91fd22bfa4ce1acf191dc45231654c991f58b10edc895e04b276ed4ae9d00b` |
| notices manifest | `243dbeb06041fd49a7fdeb95b4a20135fbe3f02e02bee4e00a7272c5d87350db` |

主工程 `.3` 安装已就位，原 `.2` 目录保留。第一次 no-clean Editor 构建通过，67 actions，
实际编译了 VM 模块并更新 DLL/声明的 staging；原始记录为 `as34-editor-build.log`。
第一次 Automation 在发现测试前因测试插件旧 BuildId 不匹配而退出，0 tests，不计入通过。
受控重试使用 UBT 的 `-EnablePlugin=AvidScriptPerfHarness` 刷新测试插件的合法 target metadata，
实际 **16/16** Wasmtime 测试成功，进程退出 0、队列结束及 TestExit 标记完整。
覆盖执行预算、生命周期、reload、artifact、Host/Guest ABI 与 TypedHost；其中 Android 命名项只是
Windows 主机上的编译器合同，不是 Android 包或设备验收。

外置 runner 初版错误地期待 15 项并使用旧结束标记，已保留其 false 报告。最终通过记录
`as34-wasmtime-verified.json` 按两个源码文件声明的完整 16 项名单逐项比对原日志，并绑定原始报告哈希，
没有为了修正报告重复启动 Editor。最终报告 SHA-256 为
`a38a5e811a13256160f124c031178ef3a2a36babf913c8fea558c6f6d2f841f2`。
测试后恢复普通 Editor target 成功，engine/project/plugin 三处 BuildId 均为
`e4461c3d-2e25-42f2-b9ad-2de17ad0c702`，实际 staged DLL 与上表身份一致。

这些 UE 回归来自保留用户私有改动的开发工作树，不充当 clean candidate 的阶段最终 Gate。

## 干净候选与 Editor 准备顺序修复

候选源码为 `6188ebe3a70e6363393fd9e023437bd4e7d44850`，tree 为
`74d661ea939f6d71622e76b75a26a61f01015258`；候选 ID 为 `dabbce27d1d0c503bc1f`。
Win64 Development 打包构建完成 1,140 项任务，UBT 用时 3,295.72 秒；随后 Cook、Stage、归档成功。
归档游戏的 micro 已完成 **9,000/9,000** 正确样本，实际 DLL 与本节 `.3` 身份一致。

首次 Editor gameplay calibration 因 `/Script/AvidScriptGenerated.EncounterSubsystem` 无法解析而退出 1。
冻结器此前先编译 Editor，再调用 Generated Type 发布器生成原生类型，却没有重新编译 Editor。
因此后续打包目标包含新类型，Editor DLL 仍保留生成前的内容。失败没有被算作通过，原始日志和样本均保留。

冻结器现于 Generated Type 发布后执行 no-clean Editor 构建，并将该次构建日志作为候选的 build evidence。
两项行为合同分别要求返回的构建证据包含新类型，以及最终构建失败时拒绝冻结；前者在修复前复现失败，
修复后两项均通过。工作负载、采样配置、运行库和门禁阈值不变。

当前候选补编译 Editor 成功：43 actions，75.49 秒；在新的输出目录恢复 gameplay，
五个计时进程全部正常退出，**1,800/1,800** 样本正确，源码与 DLL 身份一致。
恢复只复用已验证的原始 packaged micro，保留失败目录，不重新解释或覆盖失败结果。
构建记录 `generated-editor-build.log` SHA-256 为
`e006b87745cdee5460800c281a94f69a81663ba9a68f210f052f7b32cd2491e7`。
纯执行与成本分层现已完成：分别为 **7,200/7,200** 与 **1,050/1,050** 正确样本，
无 fallback 或正确性失败。四组共 **19,050** 个样本；完整报告 validity 为 true，
supplemental candidate identity 匹配，性能门禁 **16/18** 通过。它仍属于上述 `6188ebe3` 候选，
不改写为后续工作流或安装器修复提交的 Gate。

| 纯执行指标 | 本候选 | 门槛 |
| --- | --- | --- |
| Wasmtime / V8 P50 几何均值 | 0.9814146167 | ≤ 0.95 |
| Wasmtime / V8 P95 几何均值 | 1.0495391053 | ≤ 0.95 |
| P50 kernel 胜率 | 83.33% | ≥ 60% |
| P95 kernel 胜率 | 8.33% | ≥ 60% |

未通过项仍为 `wasmtime_v8_geo_ratio` 与 `kernel_win_rate`；P65-D03 保持 Fixing。
原始 `performance-gates.json` SHA-256 为
`2ba961db2f41fbeb540f5ce05d622be20ad797b086f216469c1f4af95769b5c7`，
controlled aggregate 为 `3fb4b15eea382be720420c71fe53c0a440668991814db9922c6e4a16f2532036`，
physical aggregate 为 `445ae7e15d2fd7ff211b1a629c67b8fef780e3b955a86d3270d8964dbcb44058`。
矩阵结束后 canonical Editor 恢复成功，34 actions / 80.47 秒，engine/project/plugin 三处 BuildId
均为 `161e0c58-7262-49c9-a318-cc6664ea8c67`，实际 DLL 身份不变。

同一候选的两次离线发布结果一致：release ID
`c22b6f2cc1f14be6acfb2fb3cf07bb0aa931d853b6c8ae0c0f12c614682e0316`，
清单 SHA-256 为 `2b1fca28c26c6d62f68901f6ec15c7eb185ca5bcb6b806520b97cdb0c6982928`，
inventory SHA-256 为 `e7d06dc9ecf47b613e5de6e400e07d46fb7c1304ad781c9b5820a6246229e9a1`，
共 2,063 个文件、59,881,600 字节。该结果证明重复发布，不证明升级可靠性或 Shipping 运行。

## Windows 安装事务占用恢复

上述真实离线包的三轮替换验证中，前两轮各七步成功；第三轮在 staging 到插件目录的改名处
再次出现 Windows `Access denied`。失败后的旧 `.2` 包 Verify 通过，完整 inventory 匹配且无 warning，
确认该次回滚保留了旧安装。原始失败的外部占用者尚未确定，不能归因于某个扫描器。

安装器此前对备份、提交与回滚三个目录改名均仅尝试一次。现对 Windows 错误 5/32/33
增加最多三秒、每次不超过 100ms 的有界重试；源目录缺失、目标已存在及其他错误立即拒绝。
恢复成功在 warnings 中记录重试次数；持续占用保留原始失败并进入既有回滚，不修改权限或覆盖目标。

真实 Win32 句柄合同覆盖两条路径：短暂占用 staging 内的文件，复现同类 `Access denied` 后完成升级
和新包 Verify；持续占用旧插件目录，超时拒绝升级，旧包 Verify 通过且没有遗留 staging/backup。
同一短暂占用合同在已提交的旧安装器上失败，在修复后通过；发布/安装合同合计 **28/28**。
旧安装器故障记录 SHA-256 为 `b2d0925de99eda24d6d0d340f3a91a43403bba3888a9a7ba9a5770ca819992fd`，
修复后合同记录为 `aaff747dedc7c11af2bb6895a62a9a3ecc04eccd861a4a3c8f02ebfe2f61c7df`。
这些合同证明占用处理语义；真实发布验证见下节，不能据此确定此前外部占用者的身份。

## 修复离线包重复发布与替换验证

修复候选 `f8159f769fd1d355fee21fb3c7be4f7be0b594f0`、tree
`3e0bfb136b82050bf997df8a49ff2a8e246800b4` 的干净架构检查通过。
同一候选两次独立发布的七项身份字段完全一致，共 **2,063 个文件 / 59,883,300 字节**。

| 身份 | SHA-256 |
| --- | --- |
| release ID | `851c5f8e1846297bcf62c1a4fc50e1e204742d74fe35afd44af38a54084fcdd6` |
| release manifest | `e3edd8f0d5edbec60f8c998e1dbb6a9c7097af45b758e9008778bd185746644a` |
| payload inventory | `f6d56dac03dba83b0f2916d3c63b640d4bd7dbe44fbd2ef6181bf07e0348d078` |
| 包内安装事务模块 | `e8fea73bfbb4a6a877109ee496441b9d134d9af66a586dc1c318676740e9eb3a` |

使用该包内安装器，在三个独立新工程执行旧 `.2` 包 Plan/Install/Verify、新 `.3` 包
Plan/Repair/Verify/NoOp，**21/21 操作成功、0 warning**。新旧版本号同为 `0.1.0`，因此替换动作
按合同为 Repair；已验证 release ID 从旧包转换到新包且 `.uproject` 内容未变。
每个 Verify 都校验完整 inventory；收尾再次验证全部操作报告哈希与包内安装事务模块的清单身份。
原来失败的第三轮保留为独立失败记录，没有被新一轮结果覆盖。

外置 `upgrade-proof.json` SHA-256 为
`6c5a90a492876050c0fac7ebe011e1088aef1dd51e132e043247ea98bfe113e3`，
重复发布证明为 `d08c4add0dd3e0f1a74cced4cd5fabf2c53bc54f3fa01d9240aee43a624afa26`，
干净架构记录为 `3084e307eb4fd65a7bfa6e4bcce122ca0817590768f41bbc2a8938bf4bcd8cf9`。
此处覆盖实际安装与版本替换，最新 DLL 的 Shipping 运行仍单独验收。

## 生成类型发布的进程环境隔离

Shipping 候选的初次 Editor 编译成功，但生成类型发布后的 Editor 重编译以 999 退出：
UBT 的 .NET 10 依赖报 NU1100。`BuildCSharpScriptTypes.ps1` 会为 C# 工具重定向
`DOTNET_CLI_HOME`、`APPDATA`、`LOCALAPPDATA` 与 `NUGET_PACKAGES` 等环境变量；
冻结器原先在同一 PowerShell 进程调用它，导致后续 UE 构建继承了工具专用 NuGet 环境。
外层进程执行的 canonical Editor 恢复成功，38 actions / 88.53 秒；该轮未进入 Shipping 打包。

冻结器现以独立 `pwsh -NoProfile -NonInteractive -File` 进程发布类型，保留原有参数、输出及
非零退出码拒绝行为。行为合同调用实际发布函数与会改写六个环境变量的轻量生成器，分别验证成功
和退出 7：父进程环境、工作目录不变，成功返回经校验的 package identity，失败仍传递到调用方。
旧实现复现 `DOTNET_CLI_HOME` 泄漏，修复后两条合同和完整 protocol 合同通过。
修复前记录 SHA-256 为 `00512322519c57a92d02e4729308f61aeca89fe1a08312d43b68adc698a28c37`，
修复后为 `08eb54dcf9331eba6c9357c17af0c606e8329a8ef224760cf7dc025634430ad5`。
新的真实 Shipping 运行仍须继续；此处不将准备阶段或合同通过表述为 Shipping 验收通过。

实际恢复已在源码 `f8159f76`、控制器 `d05c2c62` 上完成：先恢复候选 Editor 的构建身份，再通过
隔离的发布函数生成类型，六个父进程环境变量保持不变，发布后的 Editor 重编译成功。
另由正式 Editor 入口发布 EngineGameplay 绑定包，供 GeneratedBindingLifecycle oracle 使用。
`preparation-proof.json` SHA-256 为 `60f6a409eb2582a47ec25018049feecea67219dccdffb1bc27ef11fd1317284e`。

## BuildCookRun 工程文件身份

上述工程位于带时间戳的候选目录中，真实文件为 `AvidTPSTemplate.uproject`。
BuildCookRun 原先从文件夹名推导工程名，在 validation 阶段报告 `project_file_missing`。
现与 Release 入口一致，读取根目录唯一的 `.uproject`，从该文件名取得 Game target 名并继续验证
对应 `Source/<Target>.Target.cs`。没有工程文件仍报缺失；多个工程文件报 `project_identity_invalid`，
不会猜测或优先选择同名文件。

两项行为合同分别验证目录名与工程名不同的合法工程，以及包含两个工程文件的歧义目录。
旧实现两项均失败，修复后 BuildCookRun 合同 **33/33** 通过；没有改变 UAT、receipt 或 packaged oracle 断言。
修复前记录 SHA-256 为 `ea46883db7fa2b291887e97a6bf3e5fff68d4a7e78f14b904e065d8081e635d1`，
修复后为 `6c0bc5f1cfe4e6e32545cbdde7bc0885e0c76367890e3ebd946c95e3eb32dab7`。
这轮 validation 失败未进入打包，canonical Editor 随后恢复成功；更新候选后继续真实 Shipping 验证。

## 最新同源离线发行验证

合并上述两项构建编排修复后的源码为 `8c032e9fd9f0afe23c3b18a15057aa7a90d8774c`，
tree 为 `cdc7a8a11a9649100713ca2fced97b6e2d7cdf63`；干净架构检查通过。
两次独立发布的七项身份字段完全一致，共 **2,063 个文件 / 59,883,621 字节**。

| 身份 | SHA-256 |
| --- | --- |
| release ID | `510b7960a41ccea65e446fd2030af9b43f6bc9cf4e51abbfbc20558b9540dfda` |
| release manifest | `185ff132262baa3e67d9399e8c99bfe343387c8d08d76261d8f1bc93ae60000a` |
| payload inventory | `e156465efe025f7b43994ec3a8360df64a49b6146134af0d93639004275ce0b4` |

使用最新包内安装器，在一个新工程执行 `f8159f76` 旧发布包 Plan/Install/Verify、
`8c032e9f` 新发布包 Plan/Repair/Verify/NoOp，**7/7 操作成功**。本轮两端均为 patchset.3；
`.2 → .3` 兼容性由前节三轮验证覆盖。新旧版本号均为 `0.1.0`，替换动作按合同为 Repair。
两次 staging 提交分别经历两次目录改名重试后恢复，报告保留 **2 条 warning**，并非零警告。
完整 inventory Verify 与最后的 NoOp 通过，工程 descriptor 未变；包内安装事务模块与
`f8159f76` 的字节身份相同。所有操作报告哈希均已再次核对。

重复发布证明 `as34-release8-repeat-proof.json` SHA-256 为
`9d23f246901715cce77e9420c71149143fae5a323a02574cfd83c4fd846e703d`；
本轮 `upgrade-proof.json` SHA-256 为
`b6b5f7cbddaaa2e7e5c34918ce962c98e75fb98d4d73d83a4a8c591de05b321c`。
该外置 runner 的通用 scope 文本仍写作 patchset 2 to 3；本节按实际新旧 release ID 和清单
界定为 `.3 → .3` 发布替换，保留原报告，不改写历史证据。

## 正式纯执行尾部定位

只读拆解 `6188ebe3` 的十二个 kernel aggregate，并逐项核对它们与 suite 的哈希。
每个 kernel 含五个配对计时进程；正式跨进程 P95 比率在该样本数下取五个比率中的最大值。
六十个进程对中，**42/60** 的 P95 比率小于 1，**18/60** 不大于 0.95；只有一个 kernel
在五个进程中均低于 V8。正式跨 kernel 胜率仍为 **8.33%**，没有改成进程胜率。

`hashing`、`scalar_float`、`simd128` 各有三个进程的 P95 未领先，其配对 P95 中位数分别为
**1.0290x、1.0057x、1.0202x**，优先用于后续生成代码定位。
这些中位数仅用于诊断，不替代正式 P95，不删除高值样本，也不能证明外部争用或调度是根因。
诊断报告 `as34-controlled-tail-analysis.json` SHA-256 为
`100ae5917df2dbc7b6c3ae51f6a4db1a75f63efeeac21c469e708970dcff9a2a`。

## 同源 Shipping 包内闭环

冻结 `8c032e9fd9f0afe23c3b18a15057aa7a90d8774c`，tree 为
`cdc7a8a11a9649100713ca2fced97b6e2d7cdf63`，运行库保持本节 patchset.3 身份。
沿用 `GeneratedBindingLifecycleScript.cs`、EngineGameplay binding 与 cooperative safepoints，
完成 Shipping Release、UBT、Cook、归档、回执校验及实际包内运行，owner 进程退出 0。

隔离工程的 Shipping 配置排除 `AvidScriptPerfHarness` 及仅包含其成员的
`AvidScriptGeneratedBindings` 模块；Development/Editor 仍保留两者，正式
`AvidScriptGenerated` 脚本类型模块保留。模板地图及依赖复制 482 文件、141,459,796 字节，
源与目标逐项哈希一致。未清理编译缓存，也未修改插件冻结源码或主工程资产。

| 实际检查 | 结果 |
| --- | --- |
| 回执依赖 | 78/78；17 UFS、61 NonUFS |
| 归档非 UFS 内容 | 61 文件逐项与回执 SHA-256 一致，含完整 59 文件 Wasmtime 声明 |
| 生命周期 | BeginPlay/EndPlay、549 次 Tick、Timer 1 次、事件 1 次 |
| continuation | 实际触发，最终 Y 为 65 |
| 故障隔离 | 注入整数除零，故障 Session 被拒绝，World 继续运行 |
| 主工程恢复 | no-clean Editor 构建成功，引擎/工程/插件 BuildId 一致 |

实际模块包 ID 为 `c20caaca3fc4bc92e4b9a2b53bf3a8675965f67116e6795573f4592cdd932d4c`。
独立 `shipping-closure.json` SHA-256 为
`3d1b89ebfe8bfb3ca3109953c89bbdfc24ce1af98848d32e3f36b89074132dde`；
其核对 owner 结束状态、干净源码身份、原始包内报告与归档字节，BuildCookRun 报告 SHA-256 为
`02befdac30bf0c5c0dacf141c6b68c790a4a23bcfc25aa784ba990806f9bb7d2`。

先前失败记录保留：性能测试插件误入 Shipping、隔离工程未复制模板地图，以及重跑使用非空归档目录。
地图缺失时 UAT 曾成功退出，但包内 oracle 没有报告，因此未计通过；最终运行使用独立归档目录，
Cook 不再报告缺少 TopDown 地图或 GameMode。此结论覆盖自动包内验证，不替代人工视觉与操作验收。

## 同源完整 Windows Automation

同一冻结源码 `8c032e9f` 和 patchset.3 DLL，按独立 Windows 选择清单执行 **468/468**，
零失败；逐项测试名称与选择清单完全一致，队列结束标记和 Editor 进程退出码均为 0。
两项 Android 主机编译器测试按平台范围单列未执行，不计入 468，也不代表移动端验收。

首轮 **460/468** 的八项 Generated Types 失败保留原始报告。Shipping 发布后，Editor
Development descriptor 与 Cook current 指针不一致，启动时被身份检查拒绝；继承、生命周期、
属性、网络函数、RepNotify、包加载、Blueprint 子类与 GeneratedTypeMatrix 因而缺少已安装包。
调用维护中的 `Invoke-LeadershipGeneratedTypePublication` 重新发布 Win64 Development，
核对 Editor/Cook 的模块、包和生成结构身份，确认子进程不泄漏 .NET 环境，再执行发布后
no-clean Editor 构建，随后完整重跑全部 468 项。原先失败的八项全部通过，未修改测试断言。

Development 包为 `addbd80de95823c4aa56e3648709b7b2a91800fa7b632a4c8778fe498964e89f`；
生成结构 key 仍为 `518b18c1dcf3fcb625c9af5ac27f239d4db56ae063dc333f58395ff59f0b4775`。
Shipping 归档保持原内容与闭环哈希。测试 owner 退出 0，主工程恢复成功，三处 BuildId 一致，
主工程 DLL 仍为 patchset.3。独立验证器重新解析原始日志，并核对候选干净状态和归档可执行文件。

| 证据 | SHA-256 |
| --- | --- |
| `windows-closure.json` | `a529eb409ffa280bb238d0ea34347ec7c1a12790b6ac2515f792bbb59e82509b` |
| 完整回归原始日志 | `ae88903dbccb8566e12871d6d12f8fdbb45268f084a145b643dd30d4c246a305` |
| Development 发布证明 | `5bdf6981808df0cb77736955d8e15d91d66e650633a6797a9d3c6ad04569d99d` |
| 首轮失败报告 | `ff8344f51c90b090d31af48e9d414cca7e97c0a0f6e846ff645467a45942a1c9` |

## 未完成

1. 优化正式 Windows 矩阵剩余两项性能红项。
2. 独立保留 AngelScript 同口径矩阵与人工 Release 验收；Android/Mac 按用户要求暂不验收。
