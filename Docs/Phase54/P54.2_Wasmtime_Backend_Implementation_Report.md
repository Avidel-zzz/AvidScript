# Phase 54 P54.2 Wasmtime Cranelift JIT Backend 实现报告

## 结论

Task 3A/3B 已实现 Win64 Wasmtime `v45.0.0` Cranelift JIT backend，并以共享 Runtime oracle 验证 WAMR interpreter 与 Wasmtime JIT 语义一致。实现包含：

- 明确的 `Wasmtime/Jit/WasmBytecode` factory 选择；
- config、engine、module、store、linker、instance 生命周期；
- canonical WASM layout/import contract 先验验证；
- event export 类型检查、缓存、调用与 stale/foreign handle 语义；
- guest memory read/write 与同步 borrow；
- 完整静态 host import catalog、host failure trap 和结构化 frame；
- per-instance dynamic binding-package imports 与直接 ordinal dispatch；
- 通用 numeric export ABI，支持多参数 lifecycle/timer/event 调用；
- Runtime lifecycle、反射调用、所有权、UObject、timer/event 与 reload parity；
- active call 中 reentrant unload 延迟；
- WAMR 与 Wasmtime 标准 wasm-c-api 的可靠链接边界。

生产默认仍为 `Wamr/Auto/WasmBytecode` 且允许既有 fallback。本阶段没有切换 PC 默认，没有实现 serialized artifact 或 unchecked calls，也没有运行 benchmark 或清理 Editor target。

## 关键设计

### Cranelift 与生命周期

Wasmtime C shim 创建 fresh config，固定：

- `WASMTIME_STRATEGY_CRANELIFT`
- `WASMTIME_OPT_LEVEL_SPEED`
- component model 显式关闭
- 不注册 WASI

metric 映射如下：

- `RuntimeInitMs`：一次性 DLL 可用性确认、config 与 engine；
- `ModuleLoadMs`：`wasmtime_module_new` 的 Cranelift compile；
- `ExecEnvCreateMs`：store/context；
- `ModuleInstantiateMs`：linker、静态 imports 与 instantiate。

卸载顺序为 export wrapper、instance wrapper、module、linker/host bridge、store、engine。重复 unload 与析构均幂等；active guest call 中的 unload 延迟到调用展开后执行。

### C API 编译与链接边界

WAMR `libiwasm.lib` 原产物包含 `wasm_c_api.c.obj`，导出 `wasm_config_*`、`wasm_engine_*`、`wasm_functype_*`、`wasm_trap_*`，与 Wasmtime import library 冲突。仅禁 Unity 或改变链接顺序不能保证正确实现被选中。

本任务采用两层边界：

1. WAMR CMake 增加 `WAMR_BUILD_WASM_C_API` 开关，Win64 构建固定传 `0`，从 source glob 移除 `wasm_c_api.c` 并重建 tracked `libiwasm.lib`。
2. Wasmtime 头只出现在独立 C 编译单元 `AvidScriptWasmtimeApi.c`，向 C++ backend 暴露 `avidscript_wasmtime_*` 唯一前缀 opaque API，解决 Unity 聚合时 WAMR/Wasmtime 类型名冲突。

shim 直接调用官方 import library，不使用 `GetProcAddress` 或动态 symbol table。开发运行时通过插件路径一次加载并缓存 DLL handle；打包 RuntimeDependency 发布到 `$(PluginDir)/Binaries/Win64/wasmtime.dll`。

`BuildWAMRWin64.cmd` 的 `dumpbin /linkermember:1` gate 要求：

- `wasm_runtime_init`、`wasm_runtime_load` 存在；
- `wasm_config_*`、`wasm_engine_*`、`wasm_functype_*`、`wasm_trap_*` 不存在。

### Wasmtime 依赖布局修正

Task 2 锁定 archive 身份正确，但选择了 `min/` 子布局；其 `wasmtime/conf.h` 未启用 compiler/Cranelift，不能提供 Task 3A 所需 API。

本任务保持同一 v45 archive URL、size 与 SHA-256，只把 lock layout 改为 archive 的 `include/` 和 `lib/` 完整 C API 布局。安装器新增 `WASMTIME_FEATURE_CRANELIFT` gate，仍只发布 headers、DLL、import library 与 LICENSE，不安装或链接静态 `wasmtime.lib`。

最终受管安装：

- content SHA-256：`4399f843601d637308bd85fff7a59245afdf3546fae327996d2504c7635528e5`
- file count：`115`
- `wasmtime.dll`：`21353984` bytes
- `wasmtime.dll.lib`：`203258` bytes

### ABI 与静态 imports

compact ABI parser 已从 WAMR dynamic registry 提升为 backend-independent contract：

- `i/I/f/F` 对应 `i32/i64/f32/f64`
- 最多 64 个参数
- 零或一个结果

静态名称、签名、binding id 与 `env` compatibility 只保存在一个 ASCII catalog。Wasmtime linker 和 WAMR `NativeSymbol` 注册表都从该 catalog 构造；WAMR callback 也按 dense binding id O(1) 取名称，避免第二份字符串表漂移。

Wasmtime host callback 使用 inline value storage，成功 warm call 不构造字符串。dispatcher/memory failure 保留 import name/details，并转换成 Wasmtime trap。

### Guest memory borrow

`IAvidScriptVmGuestMemory` 新增同步、受控 borrow：

- `BorrowReadOnlyBytes`
- `BorrowMutableBytes`

默认实现 fail closed，保证现有非 VM provider 兼容。WAMR 与 Wasmtime 均验证 size、power-of-two alignment、guest address alignment 和 bounds，并在每次操作重新获取 native pointer。

borrowed view 只在当前同步 host dispatch 内有效，不跨 guest call 保留。transform batch 直接将 guest spans 映射为 `InputCells`/`OutputFloats`，删除每次调用的 `TArray<uint32>`、`TArray<float>` 分配、输入复制与输出回写，也没有全局 scratch。

### Dynamic binding-package imports

Wasmtime 在 module instantiate 前遍历已验证的 `FAvidScriptVmBindingPackage`：

- load 时一次解析 compact ABI，并构造精确 Wasmtime function type；
- linker callback 绑定 per-instance metadata，保存 ordinal、stable id、module/name、原始签名与解析结果；
- metadata 由 backend 持有，晚于 linker callback 失效后才释放，不使用全局 mutable registry；
- callback 直接使用 metadata ordinal 调用 `DispatchDynamicHostCall`，并传入当前 caller 对应的同步 guest-memory view；
- `i32/i64/f32/f64` 参数转换为 canonical `uint64` cells，结果严格返回 `i32` 或 `i64`。

成功 warm call 使用固定栈上 `uint64[64]`，不做 import 名称查找、UTF 转换、descriptor walk、`FString::Printf` 或 heap allocation。dispatcher 失败时 callback 触发 trap，外层恢复为 `host_import_failed`，并保留 binding package 的 module、name 与 details；guest trap 仍保留 Wasmtime stack frames。

### 通用 export ABI

Task 3A 的 event shim 只接受 `()` 与 `(f32)`。共享 C# oracle 暴露了 timer/event 多参数 ABI 缺口，因此 C shim 在 `ResolveExport` 时缓存 numeric parameter kinds 与 32 位 ABI cell 数，warm call 使用固定栈数组转换 `i32/i64/f32/f64`。该实现按签名工作，不包含 fixture 或 API 名称分支。

### Runtime parity lane

测试专用 lane helper 枚举：

- `Wamr/Interpreter/WasmBytecode`，`bAllowFallback=false`；
- 安装并编译可用时的 `Wasmtime/Jit/WasmBytecode`，`bAllowFallback=false`。

每个共享 oracle 校验实际 backend kind、execution mode 与 artifact format。reload session 增加仅在 `WITH_DEV_AUTOMATION_TESTS` 下可见的 backend selection 注入口；production constructor 显式保持既有 WAMR 默认。

共享覆盖包括 BeginPlay/Tick/EndPlay、静态与动态 imports、host failure、反射 scalar/property、FVector/transform guest-memory、owner slot/generation 与 packed handle、UObject/component generation、timer、gameplay event、state migration、成功 reload、失败 rollback、missing/wrong/forged import 合同、unload 与 stale handle。综合 C# source-adapter fixture 使用同一 WASM artifact 和同一 host oracle 跑两条 lane。

## 测试

### 聚焦 UBT

```powershell
& "C:\UnrealEngine\Engine\Build\BatchFiles\Build.bat" AvidTPSTemplateEditor Win64 Development "-Project=C:\tmp\AS54T1\AvidTPSTemplate.uproject" -WaitMutex -NoHotReloadFromIDE -Module=AvidScriptVM -MaxParallelActions=1 -NoUBA
```

Task 3B 最终分别对 `AvidScriptVM` 与 `AvidScriptRuntime` 执行 no-clean UBT，均通过。Unity 保持开启，C shim、VM backend 与 Runtime parity tests 成功编译链接。

### 聚焦 Automation

```powershell
& "C:\UnrealEngine\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" "C:\tmp\AS54T1\AvidTPSTemplate.uproject" -Unattended -NullRHI -NoSplash -NoSound -NoP4 -NoLiveCoding -DDC-ForceMemoryCache -stdout -FullStdOutLogOutput -FORCELOGFLUSH -CrashForUAT "-ExecCmds=Automation RunTests AvidScript.VM.Wasmtime+AvidScript.Architecture.VM" "-TestExit=Automation Test Queue Empty" "-abslog=C:\tmp\AvidScript_P54_Task3A_Focused.log"
```

Task 3A 结果：`17/17` 通过。覆盖 factory/ABI/borrow contract、WAMR backend/host/batch/dynamic registry 回归，以及 Wasmtime lifecycle、memory、static imports、batch borrow、trap、structured frame、stale/foreign handle 和 reentrant unload。

Task 3B focused parity：

```powershell
& "C:\UnrealEngine\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" "C:\tmp\AS54T1\AvidTPSTemplate.uproject" -unattended -nop4 -NullRHI "-ExecCmds=Automation RunTests AvidScript.VM.Wasmtime+AvidScript.Runtime.PackagedTimingSmoke+AvidScript.Runtime.ErrorSmoke+AvidScript.Runtime.TypedOwnerImports+AvidScript.Runtime.WasmImportBridgeSmoke+AvidScript.Runtime.WasmMissingImportSmoke+AvidScript.Runtime.WasmFailingImportSmoke+AvidScript.Runtime.WasmActor.ExternalFileSetLocationSmoke+AvidScript.Runtime.Timer.CallbackRescheduleNextFrameSmoke+AvidScript.Runtime.Event.SuccessAndLifecycleSmoke+AvidScript.Reload.CompatibleSmoke+AvidScript.Reload.MissingExportRollbackSmoke+AvidScript.Guest.CSharp.SourceAdapterArtifactLifecycleSmoke;Quit" "-TestExit=Automation Test Queue Empty"
```

结果：`16/16` 通过。综合 C# oracle 单独复跑也通过，并实际覆盖 WAMR 与 Wasmtime 两条 no-fallback lane。

### 依赖与符号

- Wasmtime dependency contracts：通过；
- Wasmtime `Verify`：通过，115 个文件与 content digest 一致；
- WAMR rebuild：通过，编译清单不含 `wasm_c_api.c.obj`；
- WAMR `dumpbin`：仅匹配 `wasm_runtime_init/load`，禁用标准 C API family 无匹配；
- Wasmtime import library：确认包含标准 C API 与 `wasmtime_module_new`；
- `git diff --check`：通过。

## 原 owned paths 外扩展

以下修改是用户发现的阻塞风险或实现后暴露的不可避免依赖，并已在实施前说明：

- `Build/BuildWAMRWin64.cmd`、WAMR CMake、README 与 tracked lib：从生产者侧移除冲突符号；
- `Build/CheckAvidScriptArchitecture.ps1`：固化链接、Cranelift、shim 与 borrow gate；
- Wasmtime lock/schema/installer/contracts/README/Build.cs：从错误 minimal layout 切到同 archive 的 Cranelift full layout，并修正 DLL 部署；
- `AvidScriptVM.Build.cs`：增加 `Projects` 以通过 `IPluginManager` 解析插件 DLL 路径；没有禁用 Unity；
- WAMR backend/host bindings：实现 borrow，并让 WAMR 消费共享静态 catalog；
- `AvidScriptWasmtimeApi.h/.c`：隔离第三方同名 C 类型，保持 import-library 直接调用。
- Task 3B 继续修改 `AvidScriptWasmtimeApi.h/.c`：缓存通用 export 参数 kind/cell layout，避免为 event/timer 手写 API 特例。
- `AvidScriptRuntimeSession.h/.cpp`：增加 test-only backend selection 注入口，使 reload oracle 能选择 Wasmtime；生产默认显式保持不变。
- `Build/CheckAvidScriptArchitecture.ps1`：固化 per-instance dynamic metadata、通用 cell call、production 默认与 no-fallback parity lane。

未修改 benchmark 或 Editor target。

## 限制与关注项

- 只暴露名为 `memory` 的默认线性内存；
- 不接受 Wasmtime serialized artifact，也不启用 unchecked call；
- `StackSize`/`HeapSize` 目前执行非零 contract 校验，但没有映射为 Wasmtime store limiter；
- DLL handle 按进程缓存，不在 VM backend unload 时释放，避免 delay-import 生命周期悬空；
- Runtime production 默认仍为 WAMR；PC 默认是否切换留给 P54.3 benchmark 决策；
- dynamic callback metadata 含 `FString`，但只在 load 时构造并在 unload 时释放，warm call 不分配；
- 本批遵循阶段集中验证，没有运行全量 AvidScript Automation、formal benchmark 或 Editor clean build。
