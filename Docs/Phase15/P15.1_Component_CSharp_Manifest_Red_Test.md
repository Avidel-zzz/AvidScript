# P15.1 组件级 C# Manifest RED 测试

## 状态

- 状态: 完成
- 日期: 2026-07-06

## 新增测试

新增自动化测试：

```text
AvidScript.Component.CSharpManifestLifecycleSmoke
```

测试流程：

- 读取 `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.avidscript.json`。
- 创建 PIE test world。
- 生成测试 Actor，并把初始位置设为 `(10, 20, 30)`。
- 在组件注册前设置 C# manifest 路径。
- 组件 BeginPlay 后，期望 C# WASM 把 Actor 移动到 `(100, 200, 300)`。
- world Tick 一次后，期望 C# WASM 把 Actor 移动到约 `(102, 200, 300)`。

## RED 证据

实现组件 API 前，模块级构建按预期失败：

```text
error C2039: "SetScriptManifestPath": 不是 "UAvidScriptComponent" 的成员
```

这证明测试确实先描述了尚不存在的组件能力。
