# Phase 56 融合调用帧实施计划

> **Agent 执行要求：** 按仓库 `AGENTS.md` 的 Simplified Parallel Phase Workflow
> 执行。测试代码随功能编写，UE 构建、Automation 与正式 benchmark 只在阶段末统一运行。

**目标：** 用 callback 共享 fused frame、prepared VM export 与统一 lifecycle hot
result 入口压缩 scalar、property 和 callback 固定成本。

**技术栈：** Unreal Engine 5.8、C++20、C ABI、Wasmtime 45 Cranelift、C# Guest、
PowerShell 7 benchmark orchestration。

## 全局约束

- 普通 `UFUNCTION` 的 semantic reflection fallback 不得缩减。
- 不增加 fixture-specific 或逐 UE API 手写入口。
- Runtime 不包含 Wasmtime/WAMR 私有类型，VM 不包含 UObject。
- Journaled write、reload、ownership、Game Thread 与 reentrant unload 语义保持不变。
- PC Win64 先实现，WAMR 与移动端允许显式回退，不允许静默错误。
- 受保护用户文件不修改、不暂存、不提交。

## 文件结构

### Runtime host cell

- 修改 `Source/AvidScriptRuntime/Private/AvidScriptWasmRuntime.cpp`
- 修改 `Source/AvidScriptRuntime/Public/AvidScriptWasmRuntime.h`
- 测试 `Source/AvidScriptRuntime/Private/Tests/AvidScriptWasmRuntimeTests.cpp`

### Binding effect plan

- 修改 `Source/AvidScriptBindings/Public/AvidScriptBindingInvocation.h`
- 修改 `Source/AvidScriptBindings/Private/AvidScriptBindingInvocation.cpp`
- 测试 `Source/AvidScriptRuntime/Private/Tests/AvidScriptRuntimeSessionTests.cpp`

### VM prepared export

- 修改 `Source/AvidScriptVM/Public/AvidScriptVmBackend.h`
- 修改 `Source/AvidScriptVM/Private/AvidScriptWasmtimeBackend.cpp`
- 修改 `Source/AvidScriptVM/Private/AvidScriptWamrBackend.cpp`
- 测试 `Source/AvidScriptVM/Private/Tests/AvidScriptVmWasmtimeTypedHostTests.cpp`

### Lifecycle

- 修改 `Source/AvidScriptRuntime/Private/Session/AvidScriptRuntimeScheduler.h`
- 修改 `Source/AvidScriptRuntime/Private/Session/AvidScriptRuntimeScheduler.cpp`
- 修改 `Source/AvidScriptRuntime/Private/Session/AvidScriptRuntimeSession.cpp`
- 修改 `Source/AvidScriptRuntime/Public/AvidScriptRuntimeSession.h`
- 修改 `Source/AvidScriptRuntime/Private/AvidScriptComponent.cpp`
- 测试 `Source/AvidScriptRuntime/Private/Tests/AvidScriptRuntimeSessionTests.cpp`

### Benchmark

- 修改 `Benchmarks/PuertsComparison/AvidScriptPerfHarness/Source/AvidScriptPerfHarness`
- 修改 `Benchmarks/PuertsComparison/AvidScriptPerfHarness/Tools`
- 新增 `Benchmarks/PuertsComparison/Profiles/Phase56*.json`
- 新增 `Docs/Phase56/P56.5_Fused_Call_Frame_Benchmark_Evidence.json`
- 新增 `Docs/Phase56/P56.5_Fused_Call_Frame_Implementation_Report.md`

## P56.1 Callback-epoch fused host cell

### 交付接口

在 Bindings 增加：

```cpp
enum class EAvidScriptPreparedHostEffectMode : uint8
{
    Rejected,
    DirectRead,
    DirectWrite,
    Journaled
};

EAvidScriptPreparedHostEffectMode ResolvePreparedHostEffectMode(
    const FAvidScriptPreparedGeneratedBinding& Binding,
    const FAvidScriptBindingInvocationContext& Context) const;
```

Runtime 的 `FAvidScriptFusedCallbackFrame` 栈保存 callback epoch、reload epoch、
registry revision、ReceiverHandle 与 Receiver。prepared call site 保存冻结 target、
当前 callback/reload epoch 与 EffectMode。一个 callback 的首次 Generated S1 调用
完成 receiver 证明，后续不同 API 共享该证明；各 call site 只在首次命中时验证
class 与 effect policy。

### 实施步骤

- [x] 将静态 effect policy 从 `PrepareGeneratedHostEffect` 中抽出为无副作用推导。
- [x] 将 callback epoch 栈升级为共享 receiver frame，禁止跨 callback fast hit。
- [x] 统一 scalar/getter/setter 三个 dispatch 使用 receiver/effect helper。
- [x] Journaled mode 每次调用现有 prepare，Direct mode 不重复 class/policy 推导。
- [x] 增加 fast-hit、revalidate、call-site-prepare、journal-slow-path instrumentation。
- [x] 添加共享复用、nested frame、revoke 与 effect mode 聚焦测试代码。
- [ ] 运行 `git diff --check` 和架构静态检查，不运行 UE 全量构建。
- [ ] 提交 `perf(runtime): fuse generated host call frames`。

## P56.2 Prepared VM export

### 交付接口

`AvidScriptVmBackend.h` 增加 `FAvidScriptVmPreparedExportCall` 与：

```cpp
virtual bool PrepareExportCall(
    const FAvidScriptVmExportHandle& Handle,
    FAvidScriptVmPreparedExportCall& OutCall,
    FAvidScriptVmError& OutError);
```

prepared call 自身提供 `IsValid()` 和 `Invoke(...)`，Runtime 不读取 backend 私有数据。

### 实施步骤

- [x] 增加 backend-neutral prepared export POD/function-pointer 合同。
- [x] Wasmtime export entry 改为稳定地址所有权。
- [x] 抽取 Wasmtime 已验证 entry 的公共调用核心。
- [x] normal `Call` 保留全部 handle/frame 验证后进入公共核心。
- [x] prepared invoke 跳过重复身份和 slot 查找，保留 trap/host failure/unload 语义。
- [x] WAMR 明确返回 unsupported，Runtime 回退 normal `Call`。
- [x] Runtime 在 export resolve 后准备 lifecycle call，Unload 前清空。
- [x] 增加 shape/result/stale/unload/reentrant trap 与 fallback 测试代码。
- [ ] 运行静态 ABI/架构检查，不运行 UE 全量构建。
- [ ] 提交 `perf(vm): prepare verified lifecycle exports`。

## P56.3 Lifecycle hot result

### 交付接口

`FAvidScriptRuntimeSession` 增加：

```cpp
bool TickHot(
    float DeltaSeconds,
    FAvidScriptWasmSmokeResult& OutFailure);

bool DispatchEventHot(
    int32 EventId,
    float Value,
    FAvidScriptWasmSmokeResult& OutFailure);

bool DispatchGameplayEventHot(
    const FAvidScriptGameplayEvent& Event,
    FAvidScriptWasmSmokeResult& OutFailure);
```

成功时 `OutFailure` 不承诺完整快照；失败时与对应 Live 入口一样提供完整诊断。

### 实施步骤

- [x] 在 Runtime 结果详情中增加仅失败物化模式。
- [x] 成功路径跳过 `CopyObservableStateToResult`、字符串和诊断数组复制。
- [x] Scheduler、EventRouter 与 Session 透传 hot 入口，保留 operation guard。
- [x] Component 切换到 `TickHot`，失败日志保持 module/category/message。
- [x] benchmark callback lane 切换到生产入口。
- [x] 添加三类 callback 成功无快照、失败完整诊断、component tick 和 reload 测试代码。
- [x] 运行静态路径检查，不运行 UE 全量构建。
- [x] 提交 `perf(runtime): add hot lifecycle result path`。

## P56.4 分段 profiling 与消融

### 交付指标

- generic export 与 prepared export；
- typed empty import；
- fused cell 首次 prove 与 fast hit；
- DirectRead、DirectWrite 与 Journaled；
- Runtime callback 成功物化与 Tick/Event/GameplayEvent hot result；
- Wasmtime/V8 12-kernel P50/P95。

### 实施步骤

- [ ] 扩展 physical ladder schema 和 runner，所有 rung 使用同一候选身份。
- [ ] 将 fused instrumentation 写入 process result。
- [ ] 增加 Phase56 diagnostic/formal profile，复用现有六 lane 和 Puerts lock。
- [ ] evaluator 增加 Phase56 绝对、ratio、防回退和路径真实性 Gate。
- [ ] 删除错误的 typed-empty/Puerts 混合解释率，改用同进程配对分段和误差 Gate。
- [ ] 合同测试覆盖缺失计数、候选不匹配、fallback 和未测 Gate。
- [ ] 只运行 PowerShell/.NET 轻量合同，不启动正式 UE benchmark。
- [ ] 提交 `perf(benchmarks): measure fused crossing costs`。

## P56.5 集中 Gate 与收口

- [ ] 对完整 diff 执行一次代码与架构集中审查。
- [ ] 一次性修复全部非阻塞 findings。
- [ ] 在项目可见插件 commit 上运行 UE5.8 no-clean Editor build。
- [ ] 运行完整 AvidScript Automation，核对 found/completed/succeeded/failed。
- [ ] 准备 semantic、generated S1、data-oriented 三个 C# profile。
- [ ] 先跑 diagnostic profile，再跑 5-process 正式 micro/gameplay。
- [ ] 运行 controlled-runtime 与 physical ladder。
- [ ] 生成集中 evaluator、compact evidence 与中文实现报告。
- [ ] 未达 Gate 按严重度登记并处置 debt，不虚报领先。
- [ ] batch-complete、freeze、Gate attest、close。
- [ ] 精确暂存 Phase 56 文件，确认受保护文件未进入提交。
- [ ] 推送 GitHub `origin/main`。

## 计划自检

- 架构文档中的每个模块和 Gate 均有对应批次。
- 没有未定义的跨批次类型：effect mode 属于 Bindings，prepared export 属于 VM，
  epoch cell 与 lifecycle 编排属于 Runtime。
- 没有要求 Task 级 UE 构建或重复正式 benchmark。
- WAMR/mobile 回退、journal、reload、nested callback 和失败诊断均有明确合同。
