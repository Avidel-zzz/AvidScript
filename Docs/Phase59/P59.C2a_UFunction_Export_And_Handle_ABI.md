# P59.C2a UFunction Export 与 ObjectHandle ABI

## 结果

脚本定义 UE 方法现在拥有跨 Semantic、Guest、WASM 和 UHT manifest 一致的执行身份：

```text
method_symbol_id
  -> SHA-256 lower hex 前 128 bit
  -> avid_ue_<32 hex>
```

唯一实现位于 `SemanticUeTypeRuntimeContract`。Guest lowerer 与 UHT generator 都调用该公共合同，Runtime
后续只读取 manifest 中的 `export_name`，不在 C++ 重新实现 hash 或按函数名猜测。

## 编译合同

- 非 `BlueprintImplementableEvent` 的 UFunction 和生命周期方法成为 Semantic reachability root；
- Guest 为每个 root 生成 canonical WASM export；
- 脚本定义 UE 类的实例参数降低为既有 `handle / i64 / 8 bytes`，与 packed
  `FAvidScriptObjectHandle` 一致；
- 不创建 CLR object，也不新增第二种对象句柄；
- 本节 generator manifest 升级为 schema 2 / generator 1.1，每个函数记录 `export_name`；
- 后续 P59.C2b 已将当前 manifest 推进为 schema 3 / generator 1.2，并保留本节函数合同。

## 真实产物

完整 `ScriptDefinedTypes.cs` 重新生成成功：

- UE types：4；
- functions：5；
- generation key：`749d40b1b0f42b3471e647c9df661224b0fa3386d7f3bccaced4c7ccd7c8695c`；
- 5 个函数均具有唯一 `avid_ue_*` export identity；
- header/source 字节未变，只有 manifest 因执行合同升级而更新。

聚焦 Guest fixture 中的实例方法 `GetActiveEncounterCount()` 已完成：

```text
C# instance UFunction -> Semantic root -> Guest handle parameter -> WASM (i64) -> i32
```

## 验证

- `AvidScript.CSharpSemantic.Tests`：`93/93`；
- `AvidScript.CSharpGuest.Tests`：`113/113`；
- `AvidScript.UeTypeGenerator.Tests`：`4/4`；
- 本节验证时真实 `BuildCSharpScriptTypes.ps1`：4 types，schema 2 / generator 1.1，成功。
- 隔离 worktree 架构门禁：通过；evidence commit `7c1f2b9cac552c3dc073ebb0bbbfd2c339d5bfa8`，
  evidence tree `ead5e509ff629a0d1f3be58fb337ec55b012a662`。

## 当前边界

正式 Guest compiler 对完整样例继续 fail-closed，首个阻断点是
`Projectile.Activate()` 对 native `UPROPERTY Damage` 的读写尚无 Guest getter/setter route。由于该函数
未降低，compiler 同时拒绝发布它的 canonical export。这是正确的失败行为，不通过删掉样例逻辑绕过。

P59.C2b 已建立按 type/member ordinal 编译的 native property import 合同；后续把 manifest member plan
接入 P59.C1 的 Session-owned router 与 prepared `FProperty` codec。
