# P25.1 C# EndPlay RED 测试

状态: Done

## 目标

先用自动化测试描述 C# `EndPlay()` 的完整期望，再实现 runtime、reload session、component 和 source adapter。

## RED 期望

- 默认 C# 样例导出 `avid_on_end_play`。
- source adapter manifest 升级为 `actor_lifecycle_v4`，并要求 EndPlay 导出。
- `FAvidScriptWasmReloadSession::EndPlayLive` 执行后，Actor 位置归零。
- `UAvidScriptComponent` 收到 UE EndPlay 后，在卸载 WASM 前执行 guest EndPlay。

## RED 证据

首次仅编译 `AvidScriptRuntime` 时按预期失败：

```text
AvidScriptCSharpGuestToolchainTests.cpp(511): error C2039: "EndPlayLive" 不是 FAvidScriptWasmReloadSession 的成员
AvidScriptCSharpGuestToolchainTests.cpp(519): error C2039: "bEndPlayCalled" 不是 FAvidScriptWasmSmokeResult 的成员
Result: Failed (OtherCompilationError)
```

失败原因是待实现 API 不存在，不是测试拼写、工程配置或依赖错误，因此 RED 有效。
