# P45.3 C# Guest 状态迁移实施计划

## Task 1：状态 schema 工具链

- [x] 在 `AvidScript.CSharpGuest` 增加结构化状态 schema 模型、类型安全判定与递归类型指纹。
- [x] 从导出生命周期函数定位脚本主类型，只投影属于该类型的静态 Guest global。
- [x] CLI 支持原子输出确定性状态 schema；失败时删除陈旧产物。
- [x] 增加标量、枚举、纯值结构、危险引用类型排除和确定性回归。
- [x] `InvokeCSharpGuestCompiler.ps1` 编排 schema 产物。
- [x] `BuildCSharpActorLifecycle.ps1` 将 schema 原样嵌入最终 manifest，并在 build report 中记录摘要。

## Task 2：Runtime manifest 契约

- [x] 增加 `FAvidScriptWasmStateMigrationManifest` 与槽位值对象。
- [x] Loader 支持缺省 `none` 和可选 `host_snapshot`。
- [x] 校验策略、owner、稳定 ID、类型指纹、范围、上限、排序、重复和重叠。
- [x] 增加有效、旧 manifest、重复 ID、非法指纹、越界与重叠回归。

## Task 3：VM 与 Runtime 内存边界

- [x] `IAvidScriptVmBackend` 暴露可选 Guest memory capability，默认 backend 保持兼容。
- [x] WAMR backend 复用现有地址校验读写实现。
- [x] `FAvidScriptWasmRuntimeInstance` 增加窄化的状态字节读写方法与结构化错误。
- [x] 增加真实 WAMR 读写成功、越界和 unloaded 状态回归。

## Task 4：事务状态迁移服务

- [x] 新建独立 `Private/StateMigration` 服务，禁止 RuntimeSession 持有迁移算法。
- [x] 同 module/owner 双边 opt-in 后按稳定 ID 求交集。
- [x] 指纹或尺寸不兼容时 fail-closed。
- [x] 读取完整旧快照成功后再写候选，避免半读半写。
- [x] 记录迁移槽位数、字节数与跳过数，不记录字段值。
- [x] 增加新增/删除字段、偏移变化、不兼容类型、读写失败和 no-op 回归。

## Task 5：RuntimeSession 与真实 C# 闭环

- [x] 候选加载和导出校验后、`BeginPlay` 前执行迁移。
- [x] 迁移失败卸载候选并保留旧 runtime/scheduler/manifest。
- [x] 真实 C# 样例增加随 `Tick` 累积的静态状态。
- [x] 自动热重载测试修改源码并证明状态跨新 WASM 保留、逻辑版本生效。
- [x] 增加不兼容候选拒绝后旧实例继续 `Tick` 的回归。

## Task 6：验证、文档与发布

- [x] .NET 与 PowerShell 工具链测试通过。
- [x] 普通增量 UE5.8 Editor 编译通过；禁止清理 Editor target。
- [x] Architecture gate、聚焦 UE automation 与完整 AvidScript automation 通过。
- [x] 更新中文阶段成果、作者指南和 `AGENTS.md` 规则/错误记录。
- [x] 审核插件根 `.gitignore`，扫描个人路径、密钥、缓存和构建产物。
- [x] 仅暂存阶段拥有文件，提交后推送 `git@github.com:Avidel-zzz/AvidScript.git`。
