# P30.1 SceneComponent 对象图 RED 测试

状态：Done

## 绑定层 RED

新增 `AvidScriptSceneComponentBindingTests.cpp` 后，模块按预期因缺少 `AvidScriptSceneComponentBinding.h` 失败。该失败证明测试先于生产接口存在。

## Runtime Handler RED

DirectHandler 与 MissingContext 测试先要求：

- `HandleActorGetRootComponentImport`
- `HandleSceneComponentGetWorldLocationImport`
- `HandleSceneComponentSetWorldLocationImport`

模块按预期因三个成员不存在而失败。

## WAMR ABI RED

真实 WASM fixture 导入三个新函数。实现前，加载阶段以 `missing_import: env.actor_get_root_component` 失败，证明 fixture 确实经过 WAMR import linker。

## C# v9 RED

`SampleShapeSmoke` 先要求 `USceneComponent`、GetRootComponent、世界坐标读写和链式调用。v8 样例产生 5 个明确断言失败，随后才修改样例与适配器。
