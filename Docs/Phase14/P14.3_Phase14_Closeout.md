# P14.3 Phase 14 Closeout

## Status

- Status: Done
- Date: 2026-07-06

## Completed

- Added a constrained C# source adapter to `BuildCSharpActorLifecycle.ps1`.
- Generated a real direct ABI WASM artifact from `Samples/CSharp/ActorLifecycle/ActorLifecycleScript.cs`.
- Generated a standard AvidScript manifest for the C# adapter artifact.
- Added `AvidScript.Guest.CSharp.SourceAdapterArtifactLifecycleSmoke`.
- Verified the generated C# WASM artifact can be loaded through the runtime manifest path and can drive Actor movement from BeginPlay and Tick.

## What Works Now

A developer can edit the current C# sample within the supported subset and run:

```powershell
powershell -ExecutionPolicy Bypass -File Plugins/AvidScript/Build/BuildCSharpActorLifecycle.ps1
```

The script produces a loadable WASM + manifest pair under:

```text
Saved/AvidScriptCSharpGuest/ActorLifecycle
```

The generated module exports:

- `avid_on_begin_play`
- `avid_on_tick`

The generated module imports:

- `env.actor_set_location`

## Remaining Gaps

This is not a full C# compiler yet. It is a practical PC-first bridge for validating C# gameplay syntax against the existing WASM binding.

Important remaining work:

- `UAvidScriptComponent` still needs a manifest path/runtime-load mode instead of only loading the embedded smoke module.
- self actor binding should become explicit host context or a `self` import instead of assuming slot `1`, generation `1`.
- Editor command/settings should learn the C# adapter route.
- Diagnostics should become source-line precise for unsupported C# constructs.

## Recommended Next Phase

Phase 15 should connect generated C# artifacts to the UE component lifecycle:

- expose a manifest path or script source path on `UAvidScriptComponent`
- on `BeginPlay`, load the generated manifest/WASM instead of the embedded smoke module
- on `TickComponent`, call `avid_on_tick`
- verify the component-level lifecycle with automation

That is the step that turns this from runtime proof into a usable in-level gameplay workflow.
