# P14.2 C# Source Adapter Runtime Verification

## Status

- Status: Done
- Date: 2026-07-06

## Automation Added

New test:

```text
AvidScript.Guest.CSharp.SourceAdapterArtifactLifecycleSmoke
```

The test verifies the generated C# source adapter report and artifacts end to end:

- report result is `direct_abi_built`
- manifest and WASM artifact exist
- manifest loader validates the generated manifest and SHA256
- reload session loads the generated WASM
- BeginPlay moves the actor to `(100, 200, 300)`
- Tick with `deltaSeconds = 1 / 60` moves the actor to approximately `(102, 200, 300)`

## Verification Commands

Module build:

```powershell
& "C:\UnrealEngine\Engine\Build\BatchFiles\Build.bat" AvidTPSTemplateEditor Win64 Development "-Project=C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\AvidTPSTemplate.uproject" -WaitMutex -NoHotReloadFromIDE -Module=AvidScriptRuntime
```

Result:

```text
Result: Succeeded
```

Automation:

```powershell
& "C:\UnrealEngine\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" "C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\AvidTPSTemplate.uproject" -Unattended -NullRHI -NoSplash -NoSound -NoP4 -NoLiveCoding -DDC-ForceMemoryCache -stdout -FullStdOutLogOutput -FORCELOGFLUSH -CrashForUAT "-ExecCmds=Automation RunTests AvidScript.Guest.CSharp.SourceAdapterArtifactLifecycleSmoke" "-TestExit=Automation Test Queue Empty" "-abslog=C:\tmp\AvidScript_P14_GREEN_CSharpAdapter.log"
```

Result:

```text
Test Completed. Result={Success} Name={SourceAdapterArtifactLifecycleSmoke}
Automation Test Queue Empty 1 tests performed.
```

## Meaning

This proves the current C# sample can now become a real AvidScript direct ABI WASM module and interact with UE Actor state through BeginPlay and Tick.
