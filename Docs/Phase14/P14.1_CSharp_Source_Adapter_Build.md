# P14.1 C# Source Adapter Build

## Status

- Status: Done
- Date: 2026-07-06

## Implemented

`BuildCSharpActorLifecycle.ps1` now tries the C# source adapter before the .NET WASI diagnostic path.

Successful output:

```powershell
powershell -ExecutionPolicy Bypass -File Plugins/AvidScript/Build/BuildCSharpActorLifecycle.ps1
```

Observed result:

```text
[AvidScript][CSharp][Build] result=direct_abi_built compiler=avidscript-csharp-source-adapter
```

Generated artifacts:

- `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.csharp_adapter.wasm`
- `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.avidscript.json`
- `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.csharp.report.json`

## Report Contract

The report now uses:

- `result = direct_abi_built`
- `direct_abi_supported = true`
- `toolchain.compiler = avidscript-csharp-source-adapter`
- `observed_exports` contains `avid_on_begin_play` and `avid_on_tick`
- `artifacts.manifest_file` points to the generated manifest
- `artifacts.wasm_file` points to the generated adapter WASM

## TDD Red Evidence

Before implementation, the new lifecycle smoke test failed as intended:

```text
Expected 'C# source adapter builds a direct ABI artifact' to be "direct_abi_built", but it was "direct_abi_unsupported".
```

Log:

```text
C:\tmp\AvidScript_P14_RED_CSharpAdapter.log
```

## Notes

The .NET CLI is still detected and reported, but the successful adapter route exits before `dotnet publish`, avoiding the previous WASI `chmod` failure and network vulnerability-index warnings during the normal C# sample build.
