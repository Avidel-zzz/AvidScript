# P33.0 Gameplay Event Bridge 范围

状态：Done

Phase 33 建立 UE/Blueprint 主动推送到 WASM 的通用事件入口：组件调用 `DispatchScriptEvent(eventId, value)`，guest 接收 `avid_on_event(eventId, value)`。

本阶段只使用 `i32 + f32` 标量 payload，避免 Runtime 绑定具体输入框架。Typed Overlap/Hit、字符串事件名、事件队列和 `eventId` 分支语法留给后续阶段。
