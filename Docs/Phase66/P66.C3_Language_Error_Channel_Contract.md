# P66.C3：语言错误通道

状态：底层结果槽和新版 IR 的结果类型布局已验证；C# `throw`、`catch`、重抛和跨方法异常传播仍被拒绝。本文固定后续跨层实现的边界，不把布局或原始 Guest IR 探针算作语言功能。

## 可执行底座

Guest IR 已有结构体返回的 SRet 路径：调用者为每次 `call` 提供独立的线性内存结果槽，WASM 函数把结构体写回该槽。[WasmOutcomeSlotTests.cs](../../Tools/AvidScript.WasmBackend.Tests/WasmOutcomeSlotTests.cs) 用两个连续的嵌套调用模拟正常值、错误传播和 `finally` 抛出新错误覆盖旧错误。探针结构体为五个 `int32` 字段：`status`、`error_type`、`source`、`value`、`cleanup`；它只是测试布局，`cleanup` 是可观察计数，不属于拟定的生产 ABI。

固定 SDK 8.0.416 下 WasmBackend 测试 **323/323** 通过；[RunOutcomeSlotWasm.cjs](../../Tools/AvidScript.WasmBackend.Tests/RunOutcomeSlotWasm.cjs) 在 Node WASM 上检查五组结果，**5/5** 通过。Win64 no-clean Editor 构建通过；[TestAvidScriptLanguageOutcomeSlot.ps1](../../Build/TestAvidScriptLanguageOutcomeSlot.ps1) 把同一 WASM 放进 Editor，在 Wasmtime JIT 和 WAMR Interpreter 上检查五组结果，Automation **1/1** 通过。该证据说明现有 SRet 能承载逐次调用结果，不证明 Roslyn 异常已经可执行。

Guest IR **15/1.14** 现在可选地声明 `language_outcome_types`。每个结果结构按规范顺序包含 `status`、`error_type`、`source`、`error_root` 和可选的 `value`；前三项为 `i32`，错误根为带 payload 的 `managed_ref`，偏移和大小由 `GuestDataLayout` 计算。`source` 是留给后续源码位置表的令牌，不直接充当源码 span。验证器拒绝字段乱序、错误根形态、重复声明，以及把新元数据降版为 14/1.13；旧产物省略该字段，原有序列化字节不变。固定 SDK 下 Guest IR 回归 **45/45** 通过。当前 C# Guest 仍生成 14/1.13；15/1.14 只固定类型布局，尚未定义可抛错函数、调用点状态检查或 UE 回调适配器。

## 待落地的版本化合同

1. **Semantic。** 新异常流不得复用 31/1.40 的身份。Roslyn 投射 `throw`、`catch`、`throw;` 和异常退出边，保留异常类型、源码 span、清理嵌套顺序与 catch 类型匹配；不支持的异常对象、过滤器、异步清理或动态分派在源码位置失败。异常边与普通 `return` 边分别验证，旧版本产物不能冒充新图。
2. **Guest IR。** 新合同不得复用 14/1.13。可抛错函数的内部返回值为每次调用独占的结果结构：状态为正常或语言错误，正常值只在正常状态可读，异常类型、源码位置及异常对象根只在错误状态可读。实际字段布局由版本化类型和 `GuestDataLayout` 计算，不能把探针中的常数 7/9、31/44 或 20 字节写成产品 ABI。所有 direct、indirect 与生成的 UE adapter 调用必须检查状态后再读取值；未具备检查路径的调用形态继续拒绝。
3. **清理。** 调用者在错误返回时沿当前作用域从内到外执行 `finally` 和枚举器 `Dispose`。清理本身抛出的语言错误替换待传播错误；普通返回值在该路径失效。异常对象及其捕获环境在传播、重抛和清理期间有明确的 managed-heap 根，调用结束不留根。
4. **UE 边界。** 现有 `avid_on_*` 与生成的 UFunction 回调签名保持稳定，由入口适配器调用内部结果函数。未捕获语言错误以类型和源码位置交给 Runtime/Session 报告并终止本次调用。Host import 拒绝、WASM trap、fuel/超时和越界仍走致命故障路径，脚本 `catch` 不得捕获它们；不能用模块全局错误变量传递语言错误。

## 下一个可提交切片

下一步给 Semantic 加版本化异常流，给 Guest IR 的可抛错函数及调用点加状态检查和清理验证，再把无过滤器的本地 `catch`、跨方法传播、嵌套 `finally`、清理替换与 `throw;` 从同一份 C# 源码贯通到 WASM。入口适配器和 Runtime 分类必须与这批源码一起接受双 VM 测试；在此之前 README 继续标注 `catch`/`throw` 未支持。之后才扩展异常对象成员、委托/接口调用和跨 `await` 清理。
