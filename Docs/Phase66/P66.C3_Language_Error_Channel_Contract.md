# P66.C3：语言错误通道

状态：底层结果槽和新版 IR 的结果类型布局已验证。Semantic 可以生成版本化的异常流诊断记录；工具层已能把同一份源码中的多个受限 `throw new System.Exception()` 方法、直接调用者、跨方法 `catch`，以及同方法的直接 `throw`/`catch` 组成 IR 17/WASM。普通方法中直调错误跨越受限的线性或前向分支同步 `finally` 时，已能按内层到外层执行清理；异常方法中的单个本地 `throw` 能经过有界的嵌套线性同步 `finally`。同一 `try` 的纯条件分支可以让多个本地抛错和正常返回共用线性或有界前向分支 `finally`；返回值在清理前求值。返回表达式中的受影响直调若产生语言错误，同一清理路径执行后再传播原错误。单层 `try/catch/finally` 的两个返回叶及 `catch` 内调用再次出错也已按此规则执行。单层清理中的有界前向分支已能执行；单层清理或嵌套线性清理的任一层若再直接抛出一个受限错误，会用新错误替换旧错误；嵌套情形仍会执行剩余外层清理。`catch` 可通过 `throw;` 把原错误传给调用者或同方法的外层 `catch`；单层 `try/catch/finally` 的线性及有界前向分支清理已能在重抛前执行；内层 `catch` 重抛进入外层 `catch` 后，外层返回也可执行同一个有界前向分支 `finally`。`catch (System.Exception e)` 可判断对象是否为空。每个新抛出点有独立源码令牌；重抛沿用原令牌和对象根。UE Runtime 已在加载 canonical WASM 时校验并保存目录；受影响的 `void` 与 `int` 导出在未捕获时报告错误；生成的 `UFunction` 已通过 Win64 UE 双 VM 的聚焦 Automation。命令行发布入口仍拒绝异常产物；异常对象成员、超出有界前向图的清理、其他嵌套处理器清理、复杂清理图中的再次抛出及跨 `await` 清理尚未执行。本文不把专用入口算作已开放的语言功能。

## 可执行底座

Guest IR 已有结构体返回的 SRet 路径：调用者为每次 `call` 提供独立的线性内存结果槽，WASM 函数把结构体写回该槽。[WasmOutcomeSlotTests.cs](../../Tools/AvidScript.WasmBackend.Tests/WasmOutcomeSlotTests.cs) 用两个连续的嵌套调用模拟正常值、错误传播和 `finally` 抛出新错误覆盖旧错误。探针结构体为五个 `int32` 字段：`status`、`error_type`、`source`、`value`、`cleanup`；它只是测试布局，`cleanup` 是可观察计数，不属于拟定的生产 ABI。

固定 SDK 8.0.416 下 WasmBackend 测试 **323/323** 通过；[RunOutcomeSlotWasm.cjs](../../Tools/AvidScript.WasmBackend.Tests/RunOutcomeSlotWasm.cjs) 在 Node WASM 上检查五组结果，**5/5** 通过。Win64 no-clean Editor 构建通过；[TestAvidScriptLanguageOutcomeSlot.ps1](../../Build/TestAvidScriptLanguageOutcomeSlot.ps1) 把同一 WASM 放进 Editor，在 Wasmtime JIT 和 WAMR Interpreter 上检查五组结果，Automation **1/1** 通过。该证据说明现有 SRet 能承载逐次调用结果，不证明 Roslyn 异常已经可执行。

Guest IR **15/1.14** 可选地声明 `language_outcome_types`，但不能用这些类型作为函数返回值。每个结果结构按规范顺序包含 `status`、`error_type`、`source`、`error_root` 和可选的 `value`；前三项为 `i32`，错误根为带 payload 的 `managed_ref`，偏移和大小由 `GuestDataLayout` 计算。`source` 是留给后续源码位置表的令牌，不直接充当源码 span。验证器拒绝字段乱序、错误根形态、重复声明和降版伪装；旧产物省略该字段，原有序列化字节不变。

Guest IR **16/1.15** 允许内部函数返回上述结果结构。直接和间接调用后必须立即读取 `status` 并按其分支；`value` 只能在成功专属路径读取，错误字段只能在错误专属路径读取。生产者在每条返回路径上必须写入常量状态 `0` 或 `1`；正常状态需要 `value`，错误状态需要 `error_type` 和 `source`。状态常量必须支配写入点，不能经可变局部或地址别名伪装。调用结果与生产者结果均不能通过普通操作数或地址别名绕过检查。Host import 不能返回语言错误结果；原始结果函数不能直接导出给 UE。

固定 SDK 下 Guest IR 回归 **67/67** 通过；[GuestLanguageOutcomeFlowTests.cs](../../Tools/AvidScript.GuestIr.Tests/GuestLanguageOutcomeFlowTests.cs) 的手工模块经 WASM backend 编译后，[Node 测试](../../Tools/AvidScript.GuestIr.Tests/RunLanguageOutcomeFlowWasm.cjs)验证单次调用以及两层函数间的正常结果和错误字段，**4/4** 相符。这组底座测试导出的标量探针只读取字段便于断言，不是 UE 错误适配器；其中的 Host heap import 仅为无对象分配的测试桩。

`CSharpLanguageOutcomeRewriter` 能把已正常 Lower 的 C# 内部函数转换为 IR **16/1.15** 结果函数：正常返回写入 `status=0` 与可选 `value`，每次调用受影响函数后立刻按状态分支，错误原样返回或在返回值类型不同时复制错误类型、源码令牌与错误根。它拒绝未转换的直接调用者、原始 Host 导出、函数引用、语义来源不匹配，以及未由清理路由规划器检查的 `try`/循环/异步方法。由真实 C# 普通方法 Lower 后改写的 WASM，在[Node 探针](../../Tools/AvidScript.CSharpGuest.Tests/RunLanguageOutcomeRewriteWasm.cjs)中分别得到正常值 `8` 和注入错误后的 `99`，**2/2** 相符。这组早期传播探针的错误生产者和导出探针由测试注入；专用同源编译入口及 UE 适配器见下文。

`CSharpThrowProducerLowerer` 单独读取真实 C# 的 Semantic 34/1.43 异常记录，只接受静态无参方法内的单个零参数内置异常构造：`System.Exception`、`System.InvalidOperationException` 或 `System.ArgumentException`。派生异常抛出时，Roslyn 插入的隐式引用转换须经合同检查。它按异常类型和源码 span 确定性分配临时令牌，分配受管错误对象根并返回 `status=1`、类型及位置。带参数或自定义构造器、`catch`、`finally` 等路径继续拒绝，避免跳过构造副作用或清理。[Node 探针](../../Tools/AvidScript.CSharpGuest.Tests/RunThrowProducerWasm.cjs)在生产者返回后强制触发一次 GC，再读回类型、位置和错误对象根，结果为 `3`；调用结束后再次收集，确认帧、根和对象均已释放，**1/1** 通过。单独生产者测试仍将该函数接到另造的模块和标量导出，不代表标准 C# 构建路径或 UE Runtime 已能处理异常。

专用编译入口现可在同一模块中从两个零参数内置异常生产者传播动态类型，并依次匹配派生类或基类 `catch`。同源 C# 的 `InvalidOperationException` 与 `ArgumentException` 分别命中对应处理器，`Exception` 可捕获派生错误；其他类型、带参数构造器及异常方法内部的派生类型本地抛出仍失败关闭。固定 SDK 的 Guest 回归 **537/537**、异常子集 **35/35**、Node WASM 类型分派 **4/4** 通过。Win64 no-clean Editor 构建与 Wasmtime JIT/WAMR Interpreter 的 `AvidScript.Runtime.LanguageErrorCatalog` **3/3** 通过，两后端均加载含两个类型令牌的同一 WASM、运行 BeginPlay 并检查调用根释放。该测试的 `11/22/33` 结果值由 Node 探针读取；UE Automation 只验证回调成功和目录/根，不把异常语法开放给普通构建入口。

`CSharpLanguageErrorCompiler` 从通过异常合同校验的 Semantic 34/1.43 产物提取普通方法 CFG、多个受限抛出方法，以及无过滤器的受限 catch 方法。同方法的 `throw new System.Exception()` 被临时投影为内部 CFG 返回块，结果通道改写后再替换为带错误对象根的抛出块；源自 Semantic 的处理器决策决定跳转到本地 catch 或继续返回错误。跨方法调用仍在返回后检查状态，匹配的处理器按目录类型令牌执行，未匹配则传播。最终模块保留原始 34/1.43 来源和 Guest IR **17/1.16** 身份。`FailA()` 与 `FailB()` 的不同源码位置在同一目录获得连续令牌；两个调用者分别捕获并返回 `11` 和 `22`。[Node 探针](../../Tools/AvidScript.CSharpGuest.Tests/RunThrowProducerWasm.cjs)验证内部传播、UE 入口未捕获报告、处理器源码顺序、多生产者捕获、本地捕获返回 `7`、两个本地抛出点分别返回 `11`/`22`、不匹配继续传播，以及调用结束后对象根释放。带参数构造器、异常对象成员和复杂清理仍拒绝。受限抛出方法目前仍须静态、无参、返回 `int32`；默认 CLI 尚未调用此专用入口。

`CSharpLanguageCleanupRoutePlanner` 对已正常 Lower 的普通方法读取原始 `try/finally` 与 CFG。受影响直调返回语言错误时，结果通道复制每个清理作用域的有界前向图，按内层到外层执行后才传播原错误；正常路径继续使用原 CFG。每个清理图最多 16 块，条件必须是纯表达式，出口必须汇合到同一后继；循环、异步清理、清理中的语言错误调用与复杂处理器清理继续拒绝。同源 C# 的 `if/else`、单臂 `if` 和嵌套作用域在 CLR/Node WASM 上分别得到 `1107/11/12`、`1/0`、`10107/101/102`；未捕获探针保留原始源码令牌 `3`。清理块中强制 GC 后，错误对象根仍有效，调用结束后帧、根与对象均释放。UE 双后端加载并执行同一 WASM 产物，但清理计数与源码令牌的数值断言来自 Node 探针。

`CSharpExceptionGraphMaterializer` 接受异常方法内的无参 `throw new System.Exception()`、受限线性同步 `finally`，以及纯条件混合出口共用的有界前向分支清理。单个本地抛出可穿过有界的嵌套清理链；同一 `try` 内由纯条件决策树选出的多个直接抛出叶节点和正常返回叶节点可共用一个清理区域。`CSharpLocalThrowLowerer` 让各抛出点写入独立错误对象根与源码令牌，再走共享清理；正常返回先保存结果，再走共享的分支清理图；线性清理仍复制操作。返回表达式里的受影响直调通过结果状态分支；调用出错先复制同一清理，调用成功则求完返回值再清理。单层清理或嵌套线性清理的任一层若再直接抛错，会用新错误替换旧错误，后者继续执行外层清理。嵌套本地抛出样例的 CLR/Node 外层 catch 均得到 `11`；三个抛出叶节点的样例得到 `11/21/31`。混合出口样例中两个正常返回与两个错误捕获在 CLR/Node 均得到 `107/120` 与 `11/21`。新增调用返回样例中，两个 `Get()` 调用成功得到 `114`；第二次调用抛错、本地抛错分别得到 `11`、`21`，Node 读回不同的原始源码令牌，清理中强制 GC 后仍无遗留帧、根或对象。带副作用或可能 trap 的决策条件、非结果通道调用、复杂清理图中的再次抛出、清理中的调用及带副作用的异常构造器仍拒绝。固定 SDK 下 Semantic **387/387**、C# Guest **526/526** 通过；30 份语言错误 Node WASM 探针通过，Win64 no-clean Editor 构建通过。[聚焦 UE Automation](../../Build/TestAvidScriptLanguageErrorCatalog.ps1) 在 WAMR Interpreter 和 Wasmtime JIT 上加载清理、重抛与 catch 变量产物，执行 BeginPlay 并检查目录与遗留根，**3/3** 通过。UE 测试不直接读取清理计数或传播后的令牌；这些执行断言来自同一产物的 Node 探针。

新增混合出口分支清理样例：两个正常返回、两个本地抛错和一个返回表达式内的调用错误共用同一 `finally` 前向图。CLR 与 Node WASM 对五条路径分别得到 `1107/21/1227/32/42`；三个未捕获探针保留各自源码令牌。清理块强制 GC 后，原错误仍可传播，调用结束后无活动帧、根或残留对象。此路径只接受纯条件、有界前向清理和受限 `System.Exception`；循环、清理中的调用与再次抛错仍拒绝。

新增嵌套清理错误替换样例：内层 `finally` 在写入 `Count += 1` 后抛出第二个 `System.Exception`，外层 `finally` 再写入 `Count += 10`。CLR 与 Node WASM 的外层 catch 均得到 `11`；源码探针读回第二个抛出点的令牌 `2`。两块清理代码各自触发 GC，两次探针调用共分配四个错误对象，返回后无活动帧、根或残留对象。当前只接受某一层单块线性同步清理中的一次直接无参抛出，其余清理块不再抛错。

三层嵌套清理现在也允许中层 `finally` 在内层清理完成后直接抛出第二个 `System.Exception`，再执行剩余外层清理。同源 CLR/Node WASM 的外层 catch 都得到 `111`；源码探针读回第二个抛出点令牌 `2`。三个清理块分别强制 GC，两次探针调用共分配四个错误对象，返回后没有活动帧、根或残留对象；Win64 双 VM 对同一产物执行 BeginPlay。该路径只接受一次受限直接抛出和最多 16 个单块线性同步清理。

最外层 `finally` 抛出第二个错误的两层嵌套样例在 CLR/Node WASM 均得到 `11`，源码探针读回第二个抛出点令牌 `2`，两块清理都执行并经过强制 GC。它与内层、中层样例一起覆盖单次替换的位置。

同一嵌套链的两层线性 `finally` 现在可连续抛出受限的无参 `System.Exception`：内层错误先替换原错误，外层错误再替换内层错误。相同 C# 源码在 CLR 和 Node WASM 中均得到清理计数 `11`；WASM 源码探针读回最后抛出点的令牌 `3`，两次调用分配的六个错误对象在清理块内强制 GC 后没有遗留根。固定 SDK Guest **536/536**、Win64 no-clean Editor 构建和 Wasmtime JIT/WAMR Interpreter 的 `AvidScript.Runtime.LanguageErrorCatalog` **3/3** 通过，日志：`Saved/Logs/AvidScript_LanguageErrorCatalog_20260924T195713043Z.log`。这仍是专用工具层入口的有界线性清理路径；普通 CLI、清理中的调用和跨 `await` 清理未开放。

单个本地 `throw` 的单层 `finally` 现在可包含最多 16 个前向块：纯条件选择清理分支，所有路径汇合后传播同一个带根语言错误。新增同源 CLR/Node WASM 用例的两条分支分别得到 `11` 和 `12`，源码探针得到原抛出点令牌 `1`；在清理块中强制 GC 后仍无遗留帧、根或对象。Win64 Editor 的 WAMR Interpreter 与 Wasmtime JIT 都加载并执行同一产物。循环、清理中的方法调用和再次抛错仍拒绝。

单层线性 `try/catch/finally` 现在可让 `try` 和 `catch` 各自的返回值在同一个外层 `finally` 前求值。`try` 中的调用出错会进入匹配的 `catch`；`catch` 中的调用再次出错只执行外层清理并向调用者传播，不会回到同一处理器。新增同源 CLR/Node WASM 样例分别得到 `105`（`try` 正常返回）、`107`（`catch` 处理后返回）和 `21`（`catch` 内再次出错后由外层捕获）；源码探针读回第二个抛出点的令牌，清理中强制 GC 后没有遗留帧、根或对象。该线性样例限定单个无变量、无过滤器的 `System.Exception` 处理器、两个返回叶和一个同步清理块；有界分支路径见下文，嵌套处理器仍拒绝。

单层 `try/catch/finally` 的外层清理现可使用有界前向 `if/else` 图。两个返回叶在清理前分别求值；`try` 中调用出错先进入 `catch`，`catch` 中再次出错则执行所选清理分支后传播。相同 C# 源码在 CLR 与 Node WASM 中对两种清理分支分别得到正常返回 `1105/1205`、捕获返回 `1107/1207`、错误逃逸 `31/32`；源码探针保留第二个抛出点令牌。WASM 清理块强制 GC 后无活动帧、根或残留对象；Win64 的 WAMR Interpreter 与 Wasmtime JIT 对同一产物执行 BeginPlay。此路径仍限定一个无变量、无过滤器的 `System.Exception` 处理器，清理条件须纯且最多 16 个前向块。

无清理的单块 `catch` 中，`CSharpLanguageOutcomeRewriter` 在匹配分支把原错误的类型、源码令牌和对象根写入处理器拥有的结果槽；本地抛出也走同一结果槽。`CSharpLocalThrowLowerer` 将 `throw;` 的合成普通返回替换为该结果槽，既不重新分配错误对象，也不生成新的源码令牌。相同源码在 CLR 与 Node WASM 中分别验证“本地抛出后重抛”和“调用其他方法后重抛”；两个外层 catch 返回 `7`、`9`，探针读取的原始令牌分别为 `2`、`1`，4 次调用只分配 4 个错误对象，调用后无残留帧、根或对象。嵌套 `catch` 中的 `throw;` 现在按 Semantic 路由进入同方法的外层处理器；若外层再次重抛或类型不匹配，结果槽转交给调用者。新增 CLR/WASM 用例返回 `3`、`5`、`6`，源码探针仍读到原令牌 `1`，4 次调用只分配 4 个错误对象，调用后无残留帧、根或对象。上述嵌套处理器路径目前只接受单一受限异常类型与无清理的单块处理器。

`SemanticSymbolProjector` 现在把 `catch (System.Exception e)` 的局部符号写入 Semantic 产物。专用编译入口只接受与处理器类型、所属方法一致的唯一符号；`CSharpExceptionGraphMaterializer` 仅移除 Roslyn 生成的确切 `CaughtException` 赋值，`CSharpLanguageErrorCompiler` 再从处理器结果槽读取已验证的对象根，绑定到 `e`。声明但未使用的变量也可编译；只有单块 `throw;` 时沿用结果槽，无需读取该局部。CLR 与 Node WASM 的四条路径返回 `7`、`9`、`11`、`13`；WASM 探针在两个实际读取变量的处理器中强制 GC 并重新访问原对象，4 次调用只分配 4 个错误对象，结束后没有遗留根。缺失符号、其他异常变量类型和 `e.Message` 等成员访问继续拒绝。这个变量目前只支持受限 `System.Exception` 引用判断，不代表完整异常对象语义。

单层 `try/catch/finally` 现在可在 `catch (System.Exception) { throw; }` 后执行外层同步清理。`try` 中调用受影响方法或直接 `throw new System.Exception()` 均可进入该处理器；清理可为单块线性代码或最多 16 块的纯条件前向图。调用型样例的正常返回、线性重抛和原源码令牌在 CLR/Node WASM 中分别为 `1007/10/3`；分支样例为 `1107/1207/11/12/3`。本地抛出型样例的线性、分支清理分别为 `10/3` 和 `11/12/3`。清理块强制 GC 后原对象根仍可传播，调用结束无活动帧、根或残留对象；Win64 WAMR Interpreter 和 Wasmtime JIT 对四份同源 WASM 执行 BeginPlay。该路径仍限定一个无变量、无过滤器的处理器，默认 CLI 继续拒绝异常产物。

嵌套 `catch` 的新增路径让内层 `throw;` 进入同方法的外层 `catch`，外层返回后再执行有界前向分支 `finally`。同源 CLR 与 Node WASM 的正常分支得到 `1107/1207`，内层重抛后的处理分支得到 `1105/1205`；目录只保留最初抛出点的一个源码令牌。六个投影出的清理写入块在 WASM 探针中强制 GC，调用后无活动帧、根或残留对象。固定 SDK 下 C# Guest 回归 **527/527**、异常子集 **33/33**，31 份 Node WASM 探针通过；Win64 no-clean Editor 构建和 WAMR Interpreter / Wasmtime JIT 的聚焦 Automation **3/3** 通过。该路径只接受两个无变量、无过滤器的 `System.Exception` 处理器、一个直接重抛点和最多 16 个纯条件前向清理块；普通脚本 CLI 仍拒绝异常产物。

受限 `try` 分支现在允许内置、非 checked 的 `int` 局部或字段自增/自减作为条件的一部分；它在抛出点前求值一次，随后仍沿原 `finally` 路由清理。`if (++Choice == 1)` 选择两个不同抛出点的同源 .NET 与 Node WASM 结果均为 `11/21`，其中十位记录条件副作用、个位记录清理次数。除法等可能在条件内抛错的运算、用户定义运算符与清理区域中的副作用条件仍拒绝。固定 SDK 异常子集 **33/33**、Win64 no-clean Editor 构建及 Wasmtime JIT/WAMR Interpreter 的 `AvidScript.Runtime.LanguageErrorCatalog` **3/3** 通过，日志：`Saved/Logs/AvidScript_LanguageErrorCatalog_20260924T190535731Z.log`。此能力仍只在专用工具层入口可用。

Guest IR **17/1.16** 的 `language_error_catalog` 将类型令牌映射到语义类型 ID，将源码令牌映射到 Source ID、源码长度及 UTF-16 span/行列。目录随 Guest IR 规范序列化；验证器要求令牌从 1 连续编号、按类型及位置排序、源码 span 在范围内，并验证错误结果生产者写入的字面令牌都存在且目录无闲置项。旧版 14–16 的 JSON 字节形态不变，不能携带该目录；17/1.16 缺目录、缺结果类型或绕过状态检查均拒绝。固定 SDK 下 Guest IR 测试 **79/79** 通过；WasmBackend **323/323** 通过。

WASM 编译器把 IR 17 目录写入唯一的 `avidscript.language_errors` 自定义段，JSON `schema_version=1`，包含 `guest_ir_schema_version`、`guest_ir_version`、`module_id`、`source_sha256`、按令牌排序的 `types` 和 `sources`。段长上限为 4 MiB；旧版 IR 不生成该段。同源 C# 测试从编译后的 WASM 读回类型 ID、Source ID 与完整 UTF-16 span/行列，并确认重复编译字节一致；C# Guest 回归 **498/498** 通过。将同源 Guest IR 文件交给 WasmBackend CLI 重新编译，Node 探针仍 **2/2** 通过。

UE Runtime 从待执行的 canonical WASM 读取目录，并与 `avidscript.provenance` 的模块 ID、源码 SHA-256 和 IR 17/1.16 身份交叉校验。类型令牌和源码令牌连续、有序且有界；格式错误、缺失目录、重复段、越界 span 或不匹配身份均在 VM 加载前拒绝。已验证的目录只在加载成功后保存，卸载、重载或失败不会遗留旧目录。专用编译入口对受影响的 `void` 与返回值导出保留原签名，在错误分支通过 `avidscript.avid_language_error_report_v1` 上报类型令牌、源码令牌与受管对象引用；Host 校验目录和本次调用帧中的对象根，报告 `language_error_uncaught` 并终止调用。无效令牌或对象根走 `language_error_invalid_report`。已有 Session 失败路径接收此类别和含源码位置的错误消息，Host 拒绝与 VM trap 仍分别处理。[TestAvidScriptLanguageErrorCatalog.ps1](../../Build/TestAvidScriptLanguageErrorCatalog.ps1) 从真实 C# 源码生成 WASM，执行 Node 探针，再让 Wasmtime JIT 和 WAMR Interpreter 加载并调用同一产物；聚焦 Automation **2/2** 通过。异常语法仍不是公开的构建能力。

Semantic **34/1.43** 仍只用于含异常语法的失败诊断产物。记录按方法保存 Roslyn 的 `try`、`catch`、`finally` 区域、分支的离开/进入/清理区域、带源码 span 的 `throw`/`throw;` 和 catch 类型，以及按序的异常基本块、块所属区域、块内运算及分支值。每个 catch 处理器指向对应的 Roslyn catch 区域。一个源码单元同时含普通方法和异常方法时，普通方法的 CFG 现在保留在产物中，异常方法仍只发布上述诊断记录；两者的方法身份不能重叠。它目前**不发布异常方法的可执行 CFG**。新版本校验序号、区域归属、类型、源码 span 和运算预算；旧版 32/1.41、33/1.42 不能冒充新版。原有成功产物仍为 31/1.40，序列化时省略 `exception_flows`。C# Guest 即使收到伪造成功标志或降版的异常记录，也会拒绝输入。固定 SDK 下 Semantic 测试 **387/387** 通过；过滤器保留在记录中并给出源码位置诊断，尚未实现。

`SemanticExceptionDispatchPlanner` 可从上述区域树为每个基本块推导语言错误路由：内层 `finally` 先于外层 `catch`，未匹配处理器后才继续外层清理；`throw;` 跳过当前 catch，`finally` 自身的新错误不会再次进入同一个 `finally`。同一规划器也给 `try` 内的调用块生成 catch 候选。`SemanticExceptionDispatchResolver` 在通过异常流和类继承合同校验后，用已注册的异常类型沿路由选择第一个匹配的处理器，并返回其前需要执行的 `finally` 区域；基类 catch、同类型内外层 catch、catch-all、未匹配传播和重抛均有源码级回归。未注册类型、非异常类型和伪造继承环拒绝解析。固定 SDK 下 Semantic 测试 **387/387** 通过。专用编译入口现消费无清理、单层外部清理及受限嵌套 catch 后外层有界前向分支清理的处理器决策；另由普通方法 CFG 规划器执行受限有界前向同步 `finally`。异常方法内的单条清理路径、直接与嵌套 catch 重抛，以及受限变量绑定已能执行；其他嵌套处理器清理、异常对象成员和跨更深处理器的带清理重抛仍只是编译期规划。含过滤器的处理器不能生成路由。

`SemanticLanguageErrorEffectPlanner` 从含异常语法的方法向直接调用者做传递闭包，列出需要结果通道签名的方法；无关方法不进入闭包。属性访问器和显式转换等潜在调用一并保守计入。尚不能完整枚举目标的虚调用、接口调用和委托调用会使规划失败，不能据此输出部分结果。专用 `CSharpLanguageErrorCompiler` 已消费该闭包改写受限同源直接调用、受限 catch、受影响的返回值 UE 导出及生成的 `UFunction`；其他异常方法与导出签名仍不能使用。

## 待落地的版本化合同

1. **Semantic。** 已有 34/1.43 的异常区域、分支、基本块运算及同源普通方法 CFG；多个受限抛出方法、跨方法 catch、同方法直接 catch、直接与嵌套 catch 重抛、`System.Exception` catch 变量引用判断、普通方法的有界前向同步 `finally`，以及本地 throw 的嵌套线性清理、纯条件分支中正常返回与多个直接抛出共用单层清理、返回表达式受影响直调、单层与嵌套线性清理中任一层的直接抛错替换、单个本地抛出经过有界前向分支清理、内层重抛进入外层 catch 后运行有界前向分支 finally 能经专用工具层入口执行。专用入口还允许受限 `try` 条件中的内置 `int` 自增/自减。下一步要处理其他副作用决策、更多多块或嵌套处理器清理及其他异常退出路径，并让不支持的异常对象、过滤器、异步清理或动态分派在源码位置失败；旧版本产物不能冒充新图。
2. **Guest IR。** 16/1.15 已约束结果生产者、direct/indirect 调用后的状态分支和字段读取路径；17/1.16 增加有界、可校验的错误令牌目录。受限同源 C# 直接调用已使用这些规则；一般异常方法仍需结果槽、清理和对象根所有权合同。正常值只在正常状态可读，异常类型、源码位置及异常对象根只在错误状态可读。实际字段布局由版本化类型和 `GuestDataLayout` 计算，不能把早期探针中的常数 7/9、31/44 或 20 字节写成产品 ABI。受限 UE 导出 adapter 已检查结果状态并恢复正常返回；普通 CLI 继续拒绝异常语法。
3. **清理。** 普通方法中的受影响直调已能在错误返回时按内外顺序执行有界前向同步 `finally`；异常方法的本地 throw 已能执行嵌套线性同步 `finally`，纯条件决策树中的多个直接抛出与正常返回可共用单层线性或有界前向分支清理，嵌套线性清理中的连续两次直接抛错会依次替换旧错误并执行余下清理。仍需覆盖更一般的嵌套处理器清理、复杂清理图中的再次抛出、枚举器 `Dispose`、循环控制流及清理中的受影响调用；普通返回值在错误路径失效。异常对象及其捕获环境在传播、重抛和清理期间必须有明确的 managed-heap 根，调用结束不留根。
4. **UE 边界。** 受限导出已有入口适配器，保留 `void`、`int` 返回与原始参数；生成的 `UFunction` 入口已在同源 .NET/Node WASM 和 Win64 UE 双 VM 的 Runtime/Dispatcher 回调中验证；Editor Play 仍待验收。未捕获语言错误以类型和源码位置交给 Runtime/Session 报告并终止本次调用。Host import 拒绝、WASM trap、fuel/超时和越界仍走致命故障路径，脚本 `catch` 不得捕获它们；不能用模块全局错误变量传递语言错误。

## 返回值与生成的 UFunction 入口

专用 `CSharpLanguageErrorCompiler` 现在为受影响的非 `void` 导出保留原参数和返回类型：内部结果槽的状态先分支，正常路径只读取 `value`，未捕获错误路径调用 `avid_language_error_report_v1` 后终止。含异常流、且无其他编译错误的 Semantic 诊断产物保留 UE 方法目录，因此生成的实例 `UFunction` 也能通过同一适配器；缺少目录的伪造产物继续拒绝。

同源 C# 的 `int` 返回值与生成的 `UFunction(int)` 在 .NET 对照中均为 `7`；两份 WASM 在 Node 中的正常返回与错误上报各通过 **4/4**。固定 SDK 8.0.416 的 Semantic **394/394**、Guest **541/541** 通过。Win64 no-clean Editor 构建通过；[聚焦 Automation](../../Build/TestAvidScriptLanguageErrorCatalog.ps1) **4/4** 通过，在 Wasmtime JIT 与 WAMR Interpreter 上验证生成的 `UFunction(int)` 正常返回 `7`、未捕获错误类别与源码位置、调用根清理，以及生产 Dispatcher 故障后的共享执行域隔离。Editor Play 尚待验收；普通 CLI 仍拒绝 `catch`/`throw`。

## 带参数的条件抛错

专用编译入口现在可处理无本地 `catch/finally` 的静态 `int` 方法：用参数决定是否执行 `throw new System.Exception()`，另一条路径正常返回。异常可跨方法进入调用者的 `catch`；未捕获时仍由 UE 导出适配器报告。Semantic 的异常 CFG 必须是最多 32 块的前向图、最多 16 个抛出点，分支条件只能使用受支持的无副作用运算。条件中的除法等可能先产生另一种语言异常的表达式继续拒绝；普通 Guest CLI 也继续拒绝异常语法。

同一份源码在 .NET 对照中得到正常值 `7`、捕获后的值 `19`。专用 Guest 测试 **38/38**、Node WASM 条件探针 **5/5** 通过，覆盖正常返回、捕获、未捕获报告和受管根释放；不安全条件是拒绝负例。Win64 no-clean Editor 构建通过，`AvidScript.Runtime.LanguageErrorCatalog` Automation **4/4** 通过；新增 BeginPlay 条件样例在 Wasmtime JIT 和 WAMR Interpreter 都成功执行并释放调用根。该路径仍不是普通脚本构建能力，也未覆盖循环、跨 `await` 清理或其他异常对象构造器。

## 下一个可提交切片

下一步扩展其他嵌套处理器和非线性清理图中的错误替换、异常对象成员、委托/接口调用与跨 `await` 的错误清理。默认 CLI 只有在这些语义及清理边界具备后才接收异常产物；README 继续标注 `catch`/`throw` 只有受限测试编译路径。
