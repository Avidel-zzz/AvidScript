# P66.C8：Task 局部变量的赋值与作用域

状态：Task 重赋值、未初始化声明后的分支赋值、别名和循环局部变量已通过 Semantic 45 / 1.54 → Guest IR 25 / 1.24 生成可运行 WASM。.NET 同源对照、Wasmtime JIT / WAMR Interpreter 执行及 Session teardown 检查已通过。新语法在 Actor/World 销毁和真实 reload/rollback 中的专项验证仍待完成，P66.C 继续实现。

## 要解决的源码

```csharp
Task<int> pending = LoadAsync(1);
Task<int> saved = pending;
pending = LoadAsync(2);
int first = await saved;
int second = await pending;

for (int i = 0; i < 3; ++i)
{
    Task<int> iteration = LoadAsync(i);
    int value = await iteration;
    if (value == 0) continue;
    if (value == 2) break;
}
```

替换 `pending` 只释放该变量原先持有的一份引用，`saved` 仍指向第一个任务。循环每次进入声明都会取得新的任务引用；正常离开、`continue`、`break`、`return`、语言异常和取消都必须释放离开的作用域所持有的引用。生产者任务、await 等待者、源码变量和别名分别拥有自己的引用，不能用“释放变量”代替“取消任务”。

## Semantic 合同

源投影与读端采用 **Semantic 45 / 1.54**。不涉及新形态的模块继续输出既有版本；模块中一旦使用新形态，所有持有 Task 局部变量的方法都必须提供完整的生命周期计划：

- `SemanticAsyncMethod.TaskLocalLifetimes` 记录 Task 局部变量与其词法 scope ID 的关系；scope 的成员 segment 和进出边继续由 Roslyn 控制流构建产生。投影器直接从声明的 Roslyn local 与语法作用域建立关系，不能在 Guest 根据名称或行号猜测。
- 新合同的 `TaskLocalSymbolIds` 总是列出完整 owner 集，不再把“是否存在别名”当成启用条件。未初始化声明需单独表示声明事实，不能伪造一次返回零任务的调用。
- Task 赋值保留普通 `assignment` operation 的目标和值；`TargetSymbolId` 继续表示声明，不能把重赋值伪装成第二次声明。
- 局部 Task await 只要求变量在当前路径已初始化且类型匹配，不再固定到初始生产方法。新合同的 `task_local` 不携带单一 `TaskCallableId`；`task_call` 仍携带实际被调用方法。每个赋值中的调用各自核对静态 async 方法、返回类型和参数，允许不同合法生产方法写入同一个变量。
- 每个局部变量的写入、别名读取、await 读取和 scope 退出都进入验证。可能持有的集合用于清理；所有路径都已初始化的集合才允许读取。赋值需要按 statement 顺序检查，不能只用 block 末尾的事实授权较早的读取。
- 不扩展 CLR、任意 `Task<T>`、Task 字段存储、跨 Session 传递、捕获或空 Task await。未实现形态继续在源码位置拒绝，并保留清晰的诊断。

模型、序列化、Semantic 读端和 CLI 使用同一版本合同。Guest 接受经过验证的 Semantic 45；携带新字段的降版输入、遗漏变量、伪造 scope、隐藏 Task 生产调用和非法读取继续拒绝。可执行合同升级为 IR 25 / 1.24，不新增 Host Task ABI。构建缓存指纹包含编译器源码及产物；普通 Task 源码允许缓存，语言错误预览仍沿用禁用缓存和 prepared 输入的既有约束。

## 引用取得与释放顺序

1. **初始化和替换。** 先求值右侧；别名来源先 retain，再释放目标的旧引用，最后存入新 token。`pending = pending` 与两个变量指向同一 Task 时仍遵循此顺序。右侧调用失败时旧变量保留到相应的清理路径，不能提前清空。
2. **作用域退出。** 从真实 CFG 边计算离开的作用域及 owner 集。对可能存在的引用执行受检 release 并清零；未进入声明的分支保留零值。非零 token 操作失败继续 trap，不能静默吞掉非法引用。
3. **挂起和恢复。** 同一作用域内的 await 不退休变量；槽值及其引用交给 continuation，恢复后重新取得调用所需的引用。跨 scope 的正常、失败和取消后继分别执行应有的清理。
4. **异常与 finally。** 根据已经构建的清理 CFG 安排退出，不绕过 finally，不提前释放外层仍可访问的别名。新的 finally 错误按语言错误合同替换原错误。
5. **失效 owner。** Session、World、Object teardown 和退休代码仍由 Runtime 回收，不重新进入 Guest。旧任务与候选任务保持各自的 lane、代次和 VM 所有权。

Guest 使用同一份已验证的派生流分析结果生成替换与退出清理，不再序列化另一份 may/must 集，也不根据变量名推测哪条路径创建过 Task。

## 已实现：退出边数据流基础

[`SemanticAsyncTaskOwnerFlowSolver`](../../Tools/AvidScript.CSharpSemantic/Validation/SemanticAsyncTaskOwnerFlowSolver.cs) 接受由源适配层产生的 block 写入集合、控制流边及边上的退休集合，输出：

- 每段入口和出口可能持有、确定持有的变量集合。
- 每条可达边上确实可能需要释放的变量集合。
- 不让不可达生产者影响活跃路径；循环返回入口也保留最初空状态这条路径。

集合使用有限不动点求解：可能集合取并集，确定集合取交集；传播到后继前移除该边退休的变量。退出一个分支不会影响另一个后继，离开内层 scope 不会移除外层别名。未知变量、非法边、重复事实、缺失入口与超预算 CFG 都拒绝。

现有 [`SemanticAsyncTaskOwnership`](../../Tools/AvidScript.CSharpSemantic/Validation/SemanticAsyncTaskOwnership.cs) 已复用该求解器；旧合同传入空退休集合，继续保留原来的单次声明、初始化和别名校验。求解器只处理数据流；新合同通过下面的源投影和读端验证建立作用域及写入事实。

专项测试包含循环重入、continue/break/异常边、分支条件初始化、外层别名、入口自环、不可达分支和输入拒绝。另外对 256 个分支/循环图枚举所有可达的具体 owner 位集，将其并集、交集及退出集合与求解器比较；这个对照使用有限状态路径枚举，不复制求解器的不动点算法。它验证编译器数据流，不是 .NET/UE 运行结果。

固定 SDK `8.0.416` 下，专项 **34/34**（包含上述 256 图对照）、Semantic 全量 **479/479**、Guest 全量 **589/589** 通过。使用 C7 生命周期验证的两份原源码重新执行正式构建，Semantic、Guest IR、WASM 共 **6/6** 逐字节一致；没有改写旧产物。本切片未改 Runtime，也未重跑 UBT/Automation，不把产物相同称为新的 UE 验收。

工程日志与哈希：`Saved/AvidScriptTaskLocalLifetime/20260926T080725042Z/` 下的 `CSharpSemantic.log`、`CSharpGuest.log`、`parity16.log`、`parity32.log` 和 `parity.json`。专项复现命令，从插件根目录执行：

```powershell
dotnet run --project Tools/AvidScript.CSharpSemantic.Tests/AvidScript.CSharpSemantic.Tests.csproj -c Release -- --task-owner-flow
```

## 已实现：源码与 Semantic 45 读端

[`SemanticAsyncTaskLocalProjector`](../../Tools/AvidScript.CSharpSemantic/Analysis/SemanticAsyncTaskLocalProjector.cs) 从 Roslyn 声明绑定作用域：方法体局部使用已有 activation scope，嵌套块使用对应的 block scope。声明、直接赋值、别名与局部 await 逐一检查；不同静态源码 `async Task<int>` 方法可写入同一局部变量，局部 await 不再记录虚假的固定生产方法。

未初始化声明复用不含 initializer 的 `async_local_declaration`，重赋值保留普通 `assignment`，不会重复声明变量。旧模块保持原有字段与版本。新版本模块中的单次赋值方法也记录完整的 Task owner/scope 列表，不能只删除一个方法的字段绕过校验。

[`SemanticAsyncTaskLocalLifetimeValidator`](../../Tools/AvidScript.CSharpSemantic/Validation/SemanticAsyncTaskLocalLifetimeValidator.cs) 对每段保留有序写入，先验证别名读取，再改变可用集合。它检查符号、生产方法签名、作用域成员、await 结果和每条路径上的确定初始化，并从 CFG 生成退出边及终结段的释放集合。Guest 消费这份派生结果。循环回边不能保留上一轮变量；同一作用域内的 await 不释放变量，外层别名也不随内层块一起退休。

验证覆盖多生产方法重赋值、自赋值、旧别名、未初始化声明后的分支赋值、for/while/do 循环、continue/break/return、异常和取消分派合同，以及遗漏字段、降版、错误作用域、提前读取、隐藏调用等拒绝路径。`Task` 捕获、嵌套赋值表达式、空 Task 和未确定初始化的读取继续拒绝。异常组合仍使用 `ASCS5422` 标识语言错误预览，正式构建必须显式启用相应选项；仅通过 Semantic 验证不代表已执行。

专项命令：

```powershell
dotnet run --project Tools/AvidScript.CSharpSemantic.Tests/AvidScript.CSharpSemantic.Tests.csproj -c Release -- --task-local-lifetime
```

此前源投影批次在固定 SDK `8.0.416` 下完成专项 **50/50**、Semantic 全量 **530/530**、Guest 全量 **593/593**，当时 Guest 尚拒绝新合同。历史日志保存在工程 `Saved/AvidScriptTaskLocalLifetime/20260926T082958561Z-semantic45/` 的 `CSharpSemantic.log`、`CSharpGuest.log`。以下执行批次补齐 Guest 发射与运行证据。

## 已实现：Guest 发射与 IR 25

[`CSharpTaskLocalLifetimes`](../../Tools/AvidScript.CSharpGuest/Lowering/CSharpTaskLocalLifetimes.cs) 负责引用替换和 CFG 退出边清理。每个源方法及其恢复函数都保留 Task owner 槽；入口清零，退出时执行受检 release 后立即清零。恢复函数先取得挂起点传入的引用，再沿实际选择的正常、错误或取消边释放离开作用域的变量，避免丢失恢复前仍存在的 owner。

IR 的 `task_local_lifetimes` 记录函数、owner 槽、release 位置及 scope 退出桥。独立读端 [`GuestTaskLocalLifetimeValidator`](../../Tools/AvidScript.GuestIr/Validation/GuestTaskLocalLifetimeValidator.cs) 检查元数据与实际指令一一对应，release 后必须清零，非零 token 的 Host 拒绝必须 trap。已有异常路由验证器只允许穿过一层已验证的纯 release 桥，不允许跳过任意 CFG 节点。

`exception_model` 区分 `none`、`fault`、`exception`、`cleanup`、`cleanup_only` 和 `cancellation`；模型必须与错误目录、异常路由、直接 await 清理及取消转移合同一致。WASM provenance 增加该字段，Runtime 对 IR 25 使用精确版本及能力检查。普通 Task 不获得语言错误或类型化取消能力，旧 IR 仍保持原格式。

正式构建从实际 Semantic 产物判断版本，包括缓存命中和 prepared 输入。Task 创建、生产者绑定、失败传播及 continuation 引用保留是普通编译器桥，不再错误地依赖语言错误预览开关；仍须精确匹配内部 ID、模块、签名和 dispatch 类型，binding package 不能授权伪造的 Task 导入。

## 执行验证

- Guest 专项 **178/178**：包含 22 份同源 .NET 结果/清理次数对照、WASM 发射、确定性序列化往返及 IR 篡改拒绝。
- Guest 全量 **772/772**、Guest IR **159/159**、WASM backend **356/356**。
- Win64 no-clean UBT 成功；UE Automation **3/3**，Wasmtime JIT / WAMR Interpreter 场景 **134/134**。
- 正式构建 **4/4**：冷构建、两级缓存命中、仅 Semantic 缓存命中、prepared Semantic 输入；每条路径的 Semantic、IR 和 WASM 均与冷构建逐字节一致。普通 Task 导入授权及篡改拒绝 **40/40**，既有类型化取消导入检查 **42/42**。
- 既有正式 Guest 合同检查 **5/5**，覆盖正常构建、错误 Schema、错误版本、未来版本和伪造 provenance。异常/取消预览经修改后的正式入口重新构建，WASM 与此前产物逐字节一致。

22 份源码覆盖立即完成/挂起后完成、不同生产者替换、旧别名、自赋值、分支初始化、for/while/do、continue/break、嵌套作用域、未等待生产者、异常与 finally、受保护的 Timer await。双 VM 场景检查正常完成、开始后立即 teardown、第一批恢复后 teardown，以及显式 Timer 取消；核对结果、finally 次数、Task/等待者、continuation、状态帧和语言错误根的回收。这里的 teardown 直接作用于 Session continuation owner，不能代替实际 Actor/World 销毁、reload/rollback 或共享执行域验证。

.NET 对照使用同一份 C# 业务源码，仅将引擎 NextTick 适配为 .NET 的短延迟。它对照语言结果和 finally 次数，不证明两种调度器具有相同的帧时序。

复现命令（插件根目录）：

```powershell
# 生成源码测试产物，构建 Win64 Editor 并执行双 VM Automation
pwsh -NoProfile -File Build/TestAvidScriptTaskLocalLifetime.ps1

# 正式构建的冷/热缓存、仅 Semantic 缓存、prepared 复用及导入授权检查
# package.json 使用项目已生成的 binding package。
pwsh -NoProfile -File Build/Contracts/TestCSharpTaskLocalLifetimeBuild.ps1 `
  -BindingPackagePath <package.json>
```

UE 执行证据在工程 `Saved/AvidScriptTaskLocalLifetime/20260926T090434236Z/`，包含 `managed.log`、`automation.log`、`results.json` 和 22 组源码生成产物。普通 Task 构建与授权证据在 `Saved/AvidScript/TaskLocalLifetimeBuild/20260926T091814143Z-build/`；异常/取消预览复建在 `Saved/AvidScriptTaskLocalLifetime/20260926T092005993Z-preview/`。既有正式 Guest 合同检查记录在 `Saved/AvidScript/GuestBuildContracts/55052b7a0f5f40d0a993901a7a3ba6b8/`。

## 接续实现与验收

1. 在 Task 替换或循环挂起期间销毁实际 Actor/World，并执行真实 reload/rollback；验证 Task、等待者、帧、取消绑定、异常根与 VM 租约按 owner 回收，退休代码不再恢复。
2. 将新语法接入生成类型的共享执行域，覆盖同域多实例、单实例退出、域隔离及候选失败。
3. 补齐更多真实业务组合、Editor Play 与长期运行证据；这些继续由 P66.C/D 推进。

当前 Task 值限于源码中的 `Task<int>`；任意 `Task<T>`、Task 字段/捕获、嵌套赋值、空 Task await 和 for 初始化器中的 Task 声明仍不在本批支持范围。
