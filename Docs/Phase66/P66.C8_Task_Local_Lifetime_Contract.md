# P66.C8：Task 局部变量的赋值与作用域

状态：Task 重赋值、未初始化声明后的分支赋值、别名和循环局部变量已通过 Semantic 45 / 1.54 → Guest IR 25 / 1.24 生成可运行 WASM。.NET 同源对照、Wasmtime JIT / WAMR Interpreter 执行、实际 Actor/World 销毁及独立 Session 的真实 reload/rollback 已通过。共享生成类型执行域和真实 Play 的组合仍待验证，P66.C 继续实现。

## 要解决的源码

```csharp
Task<int> pending = LoadAsync(1);
Task<int> saved = pending;
pending = LoadAsync(2);
int first = await saved;
int second = await pending;

for (int i = 0; i < 3; ++i)
{
    Task<int> iteration = LoadAsync(i);
    int value = await iteration;
    if (value == 0) continue;
    if (value == 2) break;
}
```

替换 `pending` 只释放该变量原先持有的一份引用，`saved` 仍指向第一个任务。循环每次进入声明都会取得新的任务引用；正常离开、`continue`、`break`、`return`、语言异常和取消都必须释放离开的作用域所持有的引用。生产者任务、await 等待者、源码变量和别名分别拥有自己的引用，不能用“释放变量”代替“取消任务”。

## Semantic 合同

源投影与读端采用 **Semantic 45 / 1.54**。不涉及新形态的模块继续输出既有版本；模块中一旦使用新形态，所有持有 Task 局部变量的方法都必须提供完整的生命周期计划：

- `SemanticAsyncMethod.TaskLocalLifetimes` 记录 Task 局部变量与其词法 scope ID 的关系；scope 的成员 segment 和进出边继续由 Roslyn 控制流构建产生。投影器直接从声明的 Roslyn local 与语法作用域建立关系，不能在 Guest 根据名称或行号猜测。
- 新合同的 `TaskLocalSymbolIds` 总是列出完整 owner 集，不再把“是否存在别名”当成启用条件。未初始化声明需单独表示声明事实，不能伪造一次返回零任务的调用。
- Task 赋值保留普通 `assignment` operation 的目标和值；`TargetSymbolId` 继续表示声明，不能把重赋值伪装成第二次声明。
- 局部 Task await 只要求变量在当前路径已初始化且类型匹配，不再固定到初始生产方法。新合同的 `task_local` 不携带单一 `TaskCallableId`；`task_call` 仍携带实际被调用方法。每个赋值中的调用各自核对静态 async 方法、返回类型和参数，允许不同合法生产方法写入同一个变量。
- 每个局部变量的写入、别名读取、await 读取和 scope 退出都进入验证。可能持有的集合用于清理；所有路径都已初始化的集合才允许读取。赋值需要按 statement 顺序检查，不能只用 block 末尾的事实授权较早的读取。
- 不扩展 CLR、任意 `Task<T>`、Task 字段存储、跨 Session 传递、捕获或空 Task await。未实现形态继续在源码位置拒绝，并保留清晰的诊断。

模型、序列化、Semantic 读端和 CLI 使用同一版本合同。Guest 接受经过验证的 Semantic 45；携带新字段的降版输入、遗漏变量、伪造 scope、隐藏 Task 生产调用和非法读取继续拒绝。可执行合同升级为 IR 25 / 1.24，不新增 Host Task ABI。构建缓存指纹包含编译器源码及产物；普通 Task 源码允许缓存，语言错误预览仍沿用禁用缓存和 prepared 输入的既有约束。

## 引用取得与释放顺序

1. **初始化和替换。** 先求值右侧；别名来源先 retain，再释放目标的旧引用，最后存入新 token。`pending = pending` 与两个变量指向同一 Task 时仍遵循此顺序。右侧调用失败时旧变量保留到相应的清理路径，不能提前清空。
2. **作用域退出。** 从真实 CFG 边计算离开的作用域及 owner 集。对可能存在的引用执行受检 release 并清零；未进入声明的分支保留零值。非零 token 操作失败继续 trap，不能静默吞掉非法引用。
3. **挂起和恢复。** 同一作用域内的 await 不退休变量；槽值及其引用交给 continuation，恢复后重新取得调用所需的引用。跨 scope 的正常、失败和取消后继分别执行应有的清理。
4. **异常与 finally。** 根据已经构建的清理 CFG 安排退出，不绕过 finally，不提前释放外层仍可访问的别名。新的 finally 错误按语言错误合同替换原错误。
5. **失效 owner。** Session、World、Object teardown 和退休代码仍由 Runtime 回收，不重新进入 Guest。旧任务与候选任务保持各自的 lane、代次和 VM 所有权。

Guest 使用同一份已验证的派生流分析结果生成替换与退出清理，不再序列化另一份 may/must 集，也不根据变量名推测哪条路径创建过 Task。

## 已实现：退出边数据流基础

[`SemanticAsyncTaskOwnerFlowSolver`](../../Tools/AvidScript.CSharpSemantic/Validation/SemanticAsyncTaskOwnerFlowSolver.cs) 接受由源适配层产生的 block 写入集合、控制流边及边上的退休集合，输出：

- 每段入口和出口可能持有、确定持有的变量集合。
- 每条可达边上确实可能需要释放的变量集合。
- 不让不可达生产者影响活跃路径；循环返回入口也保留最初空状态这条路径。

集合使用有限不动点求解：可能集合取并集，确定集合取交集；传播到后继前移除该边退休的变量。退出一个分支不会影响另一个后继，离开内层 scope 不会移除外层别名。未知变量、非法边、重复事实、缺失入口与超预算 CFG 都拒绝。

现有 [`SemanticAsyncTaskOwnership`](../../Tools/AvidScript.CSharpSemantic/Validation/SemanticAsyncTaskOwnership.cs) 已复用该求解器；旧合同传入空退休集合，继续保留原来的单次声明、初始化和别名校验。求解器只处理数据流；新合同通过下面的源投影和读端验证建立作用域及写入事实。

专项测试包含循环重入、continue/break/异常边、分支条件初始化、外层别名、入口自环、不可达分支和输入拒绝。另外对 256 个分支/循环图枚举所有可达的具体 owner 位集，将其并集、交集及退出集合与求解器比较；这个对照使用有限状态路径枚举，不复制求解器的不动点算法。它验证编译器数据流，不是 .NET/UE 运行结果。

固定 SDK `8.0.416` 下，专项 **34/34**（包含上述 256 图对照）、Semantic 全量 **479/479**、Guest 全量 **589/589** 通过。使用 C7 生命周期验证的两份原源码重新执行正式构建，Semantic、Guest IR、WASM 共 **6/6** 逐字节一致；没有改写旧产物。本切片未改 Runtime，也未重跑 UBT/Automation，不把产物相同称为新的 UE 验收。

工程日志与哈希：`Saved/AvidScriptTaskLocalLifetime/20260926T080725042Z/` 下的 `CSharpSemantic.log`、`CSharpGuest.log`、`parity16.log`、`parity32.log` 和 `parity.json`。专项复现命令，从插件根目录执行：

```powershell
dotnet run --project Tools/AvidScript.CSharpSemantic.Tests/AvidScript.CSharpSemantic.Tests.csproj -c Release -- --task-owner-flow
```

## 已实现：源码与 Semantic 45 读端

[`SemanticAsyncTaskLocalProjector`](../../Tools/AvidScript.CSharpSemantic/Analysis/SemanticAsyncTaskLocalProjector.cs) 从 Roslyn 声明绑定作用域：方法体局部使用已有 activation scope，嵌套块使用对应的 block scope。声明、直接赋值、别名与局部 await 逐一检查；不同静态源码 `async Task<int>` 方法可写入同一局部变量，局部 await 不再记录虚假的固定生产方法。

未初始化声明复用不含 initializer 的 `async_local_declaration`，重赋值保留普通 `assignment`，不会重复声明变量。旧模块保持原有字段与版本。新版本模块中的单次赋值方法也记录完整的 Task owner/scope 列表，不能只删除一个方法的字段绕过校验。

[`SemanticAsyncTaskLocalLifetimeValidator`](../../Tools/AvidScript.CSharpSemantic/Validation/SemanticAsyncTaskLocalLifetimeValidator.cs) 对每段保留有序写入，先验证别名读取，再改变可用集合。它检查符号、生产方法签名、作用域成员、await 结果和每条路径上的确定初始化，并从 CFG 生成退出边及终结段的释放集合。Guest 消费这份派生结果。循环回边不能保留上一轮变量；同一作用域内的 await 不释放变量，外层别名也不随内层块一起退休。

验证覆盖多生产方法重赋值、自赋值、旧别名、未初始化声明后的分支赋值、for/while/do 循环、continue/break/return、异常和取消分派合同，以及遗漏字段、降版、错误作用域、提前读取、隐藏调用等拒绝路径。`Task` 捕获、嵌套赋值表达式、空 Task 和未确定初始化的读取继续拒绝。异常组合仍使用 `ASCS5422` 标识语言错误预览，正式构建必须显式启用相应选项；仅通过 Semantic 验证不代表已执行。

专项命令：

```powershell
dotnet run --project Tools/AvidScript.CSharpSemantic.Tests/AvidScript.CSharpSemantic.Tests.csproj -c Release -- --task-local-lifetime
```

此前源投影批次在固定 SDK `8.0.416` 下完成专项 **50/50**、Semantic 全量 **530/530**、Guest 全量 **593/593**，当时 Guest 尚拒绝新合同。历史日志保存在工程 `Saved/AvidScriptTaskLocalLifetime/20260926T082958561Z-semantic45/` 的 `CSharpSemantic.log`、`CSharpGuest.log`。以下执行批次补齐 Guest 发射与运行证据。

## 已实现：Guest 发射与 IR 25

[`CSharpTaskLocalLifetimes`](../../Tools/AvidScript.CSharpGuest/Lowering/CSharpTaskLocalLifetimes.cs) 负责引用替换和 CFG 退出边清理。每个源方法及其恢复函数都保留 Task owner 槽；入口清零，退出时执行受检 release 后立即清零。恢复函数先取得挂起点传入的引用，再沿实际选择的正常、错误或取消边释放离开作用域的变量，避免丢失恢复前仍存在的 owner。

IR 的 `task_local_lifetimes` 记录函数、owner 槽、release 位置及 scope 退出桥。独立读端 [`GuestTaskLocalLifetimeValidator`](../../Tools/AvidScript.GuestIr/Validation/GuestTaskLocalLifetimeValidator.cs) 检查元数据与实际指令一一对应，release 后必须清零，非零 token 的 Host 拒绝必须 trap。已有异常路由验证器只允许穿过一层已验证的纯 release 桥，不允许跳过任意 CFG 节点。

`exception_model` 区分 `none`、`fault`、`exception`、`cleanup`、`cleanup_only` 和 `cancellation`；模型必须与错误目录、异常路由、直接 await 清理及取消转移合同一致。WASM provenance 增加该字段，Runtime 对 IR 25 使用精确版本及能力检查。普通 Task 不获得语言错误或类型化取消能力，旧 IR 仍保持原格式。

正式构建从实际 Semantic 产物判断版本，包括缓存命中和 prepared 输入。Task 创建、生产者绑定、失败传播及 continuation 引用保留是普通编译器桥，不再错误地依赖语言错误预览开关；仍须精确匹配内部 ID、模块、签名和 dispatch 类型，binding package 不能授权伪造的 Task 导入。

## 执行验证

- Guest 专项 **178/178**：包含 22 份同源 .NET 结果/清理次数对照、WASM 发射、确定性序列化往返及 IR 篡改拒绝。
- Guest 全量 **772/772**、Guest IR **159/159**、WASM backend **356/356**。
- Win64 no-clean UBT 成功；UE Automation **3/3**，Wasmtime JIT / WAMR Interpreter 场景 **134/134**。
- 正式构建 **4/4**：冷构建、两级缓存命中、仅 Semantic 缓存命中、prepared Semantic 输入；每条路径的 Semantic、IR 和 WASM 均与冷构建逐字节一致。普通 Task 导入授权及篡改拒绝 **40/40**，既有类型化取消导入检查 **42/42**。
- 既有正式 Guest 合同检查 **5/5**，覆盖正常构建、错误 Schema、错误版本、未来版本和伪造 provenance。异常/取消预览经修改后的正式入口重新构建，WASM 与此前产物逐字节一致。

22 份源码覆盖立即完成/挂起后完成、不同生产者替换、旧别名、自赋值、分支初始化、for/while/do、continue/break、嵌套作用域、未等待生产者、异常与 finally、受保护的 Timer await。双 VM 场景检查正常完成、开始后立即 teardown、第一批恢复后 teardown，以及显式 Timer 取消；核对结果、finally 次数、Task/等待者、continuation、状态帧和语言错误根的回收。这里的 teardown 直接作用于 Session continuation owner，不能代替实际 Actor/World 销毁、reload/rollback 或共享执行域验证。

.NET 对照使用同一份 C# 业务源码，仅将引擎 NextTick 适配为 .NET 的短延迟。它对照语言结果和 finally 次数，不证明两种调度器具有相同的帧时序。

复现命令（插件根目录）：

```powershell
# 生成源码测试产物，构建 Win64 Editor 并执行双 VM Automation
pwsh -NoProfile -File Build/TestAvidScriptTaskLocalLifetime.ps1

# 正式构建的冷/热缓存、仅 Semantic 缓存、prepared 复用及导入授权检查
# package.json 使用项目已生成的 binding package。
pwsh -NoProfile -File Build/Contracts/TestCSharpTaskLocalLifetimeBuild.ps1 `
  -BindingPackagePath <package.json>
```

UE 执行证据在工程 `Saved/AvidScriptTaskLocalLifetime/20260926T090434236Z/`，包含 `managed.log`、`automation.log`、`results.json` 和 22 组源码生成产物。普通 Task 构建与授权证据在 `Saved/AvidScript/TaskLocalLifetimeBuild/20260926T091814143Z-build/`；异常/取消预览复建在 `Saved/AvidScriptTaskLocalLifetime/20260926T092005993Z-preview/`。既有正式 Guest 合同检查记录在 `Saved/AvidScript/GuestBuildContracts/55052b7a0f5f40d0a993901a7a3ba6b8/`。

## 已验证：Actor/World 销毁与独立 Session 重载

[`TaskLocalLifetimeLifecycle.cs`](../../Fixtures/Phase66/TaskLocalLifetimeLifecycle.cs) 是 .NET 与正式 WASM 构建共用的源码：先启动第一个子 Task，保存旧别名，再把原变量替换为第二个子 Task；在循环内声明并重赋值 `iteration`，分别等待两个任务，执行 `continue` 和 `break`。取消路径经过子任务重抛、外层 `catch/finally`，随后在仍持有两个 Task 局部变量时再次等待下一帧。

两代源码仅修改计算常量，正常结果分别为 **33**、**65**，取消结果均为 **90**。同源 .NET 共 **4/4** 通过，并检查两个子任务的 finally 次数、外层清理次数、循环次数和清理后的再次恢复。正常方法源码不因 UE 测试调整；[`Guest` 入口](../../Fixtures/Phase66/TaskLocalLifetimeLifecycle.Guest.cs) 负责创建取消源、接入生命周期回调，以及在候选已建立任务之后故意触发 VM trap。

[`TaskLocalLifetimeLifecycle` Automation](../../Source/AvidScriptRuntime/Private/Tests/AvidScriptTaskLifetimeLifecycleTests.cpp) 在两个 VM 中分别覆盖四个位置：普通等待、取消已排队、两个子任务均已取消、外层 catch/finally 已执行但方法再次挂起。阶段推进以源码完成计数为准，遵守 Session 的每 Tick 调度预算，不假定多个回调一次派发完毕。

| 操作 | 双 VM 场景数 | 核对内容 |
| --- | --- | --- |
| `StopAndUnload` | 8 | 幂等停止，队列、Task、状态帧和 VM 租约释放 |
| `World.BeginTearingDown` | 8 | 不再产生可派发回调，旧脚本状态不再变化 |
| `World.DestroyActor` | 8 | Actor 确实被销毁、注册句柄不再解析，旧脚本不再恢复 |
| 正常候选提交 | 8 | 替换为新 VM，新源码返回 65，旧任务及 VM 释放 |
| 已排队取消的候选提交 | 8 | 仅新版取消继续派发，返回 90，finally 各执行一次 |
| 候选建立任务后 trap | 8 | Host effects 回滚，原 VM 身份、状态帧、队列、异常根及 BeginPlay 次数保持；旧源码继续返回 33 或 90 |

Win64 no-clean 构建、Automation **1/1**、上述矩阵 **48/48** 通过。所有场景最终核对 Task 记录、等待者、状态帧、取消绑定、active/prepared continuation 及队列归零；销毁或停止后取消源归零，重载后只保留存活实例的一个取消源，最终停止后也归零。持有两个取消 Task 时执行 GC，异常对象仍由 Task 引用保活；清理后实际收集到零对象、零根。候选与旧版使用不同 WASM，并通过正式 manifest 校验执行字节与读回布局；不通过重启旧代码模拟回滚。

迁移仍只覆盖入口 owner 的三个声明字段（BeginCount、ReloadMode、Result）；辅助类静态状态在候选中从零开始。本组没有扩展状态迁移语义，也不代表生成类型共享执行域、Editor Play 或长期运行已验收。

```powershell
pwsh -NoProfile -File Build/TestAvidScriptTaskLifetimeLifecycle.ps1 `
  -BindingPackagePath <package.json>
```

证据：工程 `Saved/AvidScriptTaskLifetimeLifecycle/20260926T093156600Z/`，包含两代源码、.NET 日志、正式构建产物、`automation.log` 和 `results.json`。共用读取器同时回归既有 Semantic 44 / IR 24 的取消生命周期与重载用例，Automation **2/2**、场景 **60/60** 通过，记录在同目录 `legacy-regression.log`。

## 已验证：共享执行域中的 Task 与取消

[`SharedTaskLifetime.cs`](../../Fixtures/Phase66/SharedTaskLifetime.cs) 从 C# `[UFunction]` 进入实例异步方法，再调用静态 `Task<int>` 方法。每个实例启动两个子任务，保留旧别名、替换变量，在循环内等待，并经过 `catch/finally` 后再次挂起。两个实例共用同一 World 的 VM、堆和静态字段；另一 World 使用独立执行域。

本次修复了没有局部 `try/catch` 的子 Task 无法传播取消的问题。例如：

```csharp
static async Task<int> Child(int value, AvidCancellationToken token)
{
    await AvidContinuations.NextTickAsync().WithCancellation(token);
    return value;
}
```

启用取消预览后，这个等待的取消状态会成为子 Task 的类型化 `TaskCanceledException`，由等待它的外层方法捕获。Guest 使用已有的受检取消来源、错误 owner 和生产者传播合同；不伪造源码中的 `try/catch`，也不放宽 Semantic 或 IR 验证。缺失取消路由、伪造来源 token、遗漏生产者传播记录的 IR 仍被拒绝。Semantic 45 / IR 25 版本及 Host ABI 保持不变。

Win64 no-clean 构建与 [`SharedTaskLifetime` Automation](../../Source/AvidScriptRuntime/Private/Tests/AvidScriptSharedTaskLifetimeTests.cpp) **1/1** 通过。Wasmtime JIT、WAMR Interpreter 共 **30/30** 场景：

| 操作 | 核对内容 |
| --- | --- |
| 正常与取消完成 | 两个实例结果独立；共享静态计数正确；另一 World 独立计数 |
| 单实例结束、UObject GC | 退休实例不再恢复；peer 保留自己的三个 Task 并正常完成 |
| World 开始销毁与 cleanup | 派发门禁拒绝所有待恢复回调并回收任务；实际 `DestroyWorld` 后两个 Session 均卸载，另一 World 继续运行 |
| 包级热重载提交 | 三个实例一起发布，两 World 的旧 VM 释放；重新调用后执行新代码 |
| 准备第一个、第二个实例后失败 | 保留原 VM 身份、实例状态、帧、待派发队列和异常根；旧代码继续完成 |
| 恢复后发生 VM trap | 隔离整个共享域，peer 不能重新进入；另一 World 不受影响 |

`BeginTearingDown` 只设置 World 的销毁状态；测试先调用实际派发门禁 `DrainReady`，确认没有回调可进入 Guest，再调用 `DestroyWorld` 核对 cleanup。它不要求在销毁声明时提前卸载 VM，避免改变 UE 的终结回调顺序。所有场景最终检查 Task、等待者、状态帧、取消绑定和 continuation 归零；取消错误根在 GC 中保活，退休后可被回收。

两代源码只修改计算常量，Guest 专项 **26/26**，其中同源 .NET 对照 **8/8**。除正常/取消返回值外，.NET 对照确认除零失败；UE 用该源码的真实 WASM trap 检查域隔离，未把 VM trap 宣称为可捕获的语言异常。执行证据位于工程 `Saved/AvidScriptSharedTaskLifetime/20260926T095929974Z/`。

当前编译器全量回归 **798/798**，日志为同目录的 `guest-full.log`。原有独立 Session 源码也经当前正式构建入口重新生成两代产物，.NET **4/4**、生命周期/重载 **48/48**、Automation **1/1** 通过，证据位于 `Saved/AvidScriptTaskLifetimeLifecycle/20260926T100212541Z/`。本组未改 Runtime 产品代码；Win64 构建包含新增的共享域测试。

```powershell
pwsh -NoProfile -File Build/TestAvidScriptSharedTaskLifetime.ps1
```

本组使用编译器生成的类型/成员身份与属性路由，连接生产 generated Host、Session 和 dispatcher，但原生反射测试壳预先存在。正式构建后续接入见下一节；新的 UHT 壳编译和 Editor 安装运行不由这组 Runtime 测试代替。

<a id="generated-task-build"></a>

## 已接通：正式生成类型构建

[`BuildCSharpScriptTypes.ps1`](../../Build/BuildCSharpScriptTypes.ps1) 接受 `-AsyncExceptionFlow`、`-DirectAwaitCleanup`、`-AsyncCancellationFlow`，并向 Semantic 与 Runtime 构建两端传递相同选项。异常/取消仍要求 `-LanguageErrors bounded`；未启用、缺失依赖选项、Shipping、headless 和仅生成壳的组合继续拒绝。

类型生成器按原始版本调用现有的异步调用、错误计划、异常/取消和 Task 局部生命周期验证器，再生成反射声明。它不会把 Task 产物降版或删除计划来绕过验证；manifest 保留原始 Semantic 版本与字节哈希。旧同步异常合同保持既有声明视图适配。新读端还检查缺失计划、版本降级、未来版本、错误状态和无关诊断，原有 UHT 签名与方法目录检查保持有效。

修复了异步语言错误预览丢失 UE 方法目录的问题：只有 `ASCS5422` 这一已知预览诊断、且有实际异步错误/异常计划时，源投影继续保留方法目录；存在其他错误时不放行。类型生成器全量 **25/25** 通过，包含普通 Task、异步抛错和共享 Task 取消源码。

正式构建合同 **10/10** 通过。两代 `SharedTaskLifetime.cs` 均生成真实 native shell、Semantic 45 / IR 25、WASM 和运行时包；核对生成文件、源语义、WASM 与包描述符哈希，以及实际传入的三个异步选项。只修改计算常量时，native 结构哈希不变、WASM 改变，分类为 `body_only` 并记录上一包 ID。首版声明清单和包描述符在更新前留档，可独立核对两代身份。拒绝用例不发布包。证据位于工程 `Saved/AvidScript/AsyncGeneratedTypeContracts/20260926T120606548Z/`。

同一编译器实现的 Semantic **530/530**、Guest **798/798**、类型生成器 **25/25** 全量回归通过，日志在 `Saved/AvidScript/AsyncGeneratedTypeContracts/20260926T101447973Z/`。旧同步异常正式生成类型合同 **5/5** 通过，其中两个生成 UFunction 的 WASM 由 Node 实际执行；日志为同目录 `bounded-regression.log`，详细产物位于 `Saved/AvidScript/BoundedGeneratedTypeContracts/9e78fb228b5d4c9ba13e60c45ae42a06/`。

```powershell
pwsh -NoProfile -File Build/BuildCSharpScriptTypes.ps1 `
  -DotNetPath "$env:USERPROFILE/.dotnet/dotnet.exe" `
  -SourcePath Fixtures/Phase66/SharedTaskLifetime.cs `
  -SourceId Fixtures/Phase66/SharedTaskLifetime.cs `
  -ProjectPath Fixtures/Phase66/SharedTaskLifetime.csproj `
  -BindingPackageManifestPath <package.json> `
  -OutputRoot <native-output> -ArtifactRoot <artifact-output> `
  -LanguageErrors bounded -AsyncExceptionFlow -DirectAwaitCleanup -AsyncCancellationFlow

pwsh -NoProfile -File Build/Contracts/TestAsyncGeneratedTypeBuild.ps1 `
  -BindingPackageManifestPath <package.json>
```

这组构建合同只验证原生源码和运行时包。新壳的 UHT/Editor 执行证据见下节；实际 Play 仍未验收。

## 已验证：新 UHT 壳与 World 帧调度

`SharedTaskActor` 已经正式生成、编译并由冷启动 Editor 安装。新增
[`TestGeneratedTaskTypesInEditor.ps1`](../../Build/TestGeneratedTaskTypesInEditor.ps1)
从同一 C# 源码生成两代包，以真实 `UFunction` 的 `ProcessEvent` 和原生方法入口调用脚本，
不再复用已有反射测试壳。

这条路径发现并修复了此前测试未覆盖的问题：没有 `Tick` 覆写的生成 Actor 会一直等待。
修复前，UE World 推进 24 帧后，三个实例仍为初值 `5/10/20`，每个实例残留 4 个等待项。
旧测试手动调用 `Session::TickLive`，因此没有暴露缺失的生产调度入口。

生成类型 Host 现在订阅 `OnWorldPostActorTick`，由所属 World 驱动 Session 的 continuation
和延后回调；不合成用户 `Tick`、不推进第二份时钟。World teardown、Session 活动调用、
调试暂停、应用挂起、包级事务和域故障继续经过原有入口保护。Host shutdown 移除 World 委托。
热路径不创建实例快照，不持有新的 UObject 强引用。

Win64 Wasmtime JIT 的 **12/12** 场景通过：正常/取消两条路径分别验证普通完成、销毁单个 Actor、
销毁 World、准备 1/2 个实例后回滚、全包提交。三个实例分属两个 World；取消值为 `90`，
旧版本正常值为 `11/21/41`，提交后新调用为 `43/53/73`。成功路径确认等待项归零，
脚本 Tick 调用数保持 `0`。测试仅调用正常 `World::Tick`，不手动驱动 Session。

独立源码也暴露了生成模块对五类型示例的编译耦合。示例测试现通过 UBT 参数
`-AvidScriptGeneratedTestSuite=script_defined_types` 或 `shared_task` 显式选择，默认为 `none`。
参数进入 UBT makefile 身份；不能使用只改变环境变量的方式切换已缓存的模块定义。
普通自定义类型构建和反射检查单独验证，旧示例的网络/事件测试入口会启用自己的测试集。

复现命令：

```powershell
pwsh -NoProfile -File Build/TestGeneratedTaskTypesInEditor.ps1 `
  -BindingPackageManifestPath <package.json>
```

运行器备份并核对当前四个生成文件，只由正式构建器安装候选，结束时恢复原文件、刷新 UHT
时间戳并执行 no-clean 原项目构建和示例调用。每次 Automation 核对测试名、数量、结果和退出标记。
证据目录：`Saved/AvidScript/GeneratedTaskEditor/20260926T123258876Z/`；修复前复现记录为
`Saved/AvidScript/GeneratedTaskEditor/20260926T122832402Z/task-lifecycle.log`。
这些是 Editor 命令行自动化证据，不代替真实 Play、Shipping 或自然玩法长跑。

同一 Runtime 修复下，共享 Task 双后端回归 **30/30**、聚焦 Guest **26/26**、同源码 .NET
对照 **8/8** 通过，日志在 `Saved/AvidScriptSharedTaskLifetime/20260926T123806928Z/`。
原五类型包恢复后，生成类型完整回归 **11/11** 通过，覆盖原有异步函数、事件 await/reload、
属性、继承、RepNotify、网络函数、生命周期、Cook 包加载及启动身份检查；日志在上述
`GeneratedTaskEditor` 证据目录的 `canonical-regression/`。这是进程内回归，不是本次新增的真实网络验收。

## 接续实现与验收

1. 继续 [C9 显式抛错与清理抛错组合](P66.C9_Async_Throw_Routing.md)：Semantic 46 已有源码与读端验证，Guest IR / WASM / UE 执行仍待接通。
2. 支持直接向实例字段/属性写入 await 结果，例如 `Value = await ReadAsync()`；当前赋值目标只允许局部变量和当前脚本的静态字段。必须保留左侧表达式在等待前求值、receiver 跨帧存活及恢复后的权限校验，不能靠要求业务代码拆成局部变量规避。
3. 补齐更多真实业务组合、Editor Play 与长期运行证据；这些继续由 P66.C/D 推进。

当前 Task 值限于源码中的 `Task<int>`；任意 `Task<T>`、Task 字段/捕获、嵌套赋值、空 Task await 和 for 初始化器中的 Task 声明仍不在本批支持范围。
