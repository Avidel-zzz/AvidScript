# P66.C6：直接 `await` 的状态与取消合同

状态：合同、无载荷状态保护、Session owner 取消 opt-in、版本化 Host import 与受控 Semantic 43 / Guest IR 23 编译链；受保护的直接 `await` 仍未对正式构建开放，P66.C 未完成。Windows 优先，Android/iOS 验收暂缓。

## 起点

[`DirectAwaitInTry.cs`](../../Fixtures/Phase66/DirectAwaitInTry.cs) 把 `await AvidContinuations.NextTickAsync()` 直接放进 `try/catch/finally`。正式 `-LanguageErrors bounded -AsyncExceptionFlow` 构建返回 `ASCS3002`，退出码 1，且不生成 WASM。当前 `SemanticAsyncControlFlowProjector` 只给 Task await 创建故障与取消后继，`SemanticAsyncExceptionPlanValidator` 也只允许 Task await 位于受保护区域。

原生完成消息已经带 `Completed`、`Failed`、`Cancelled` 状态，`avid_on_continuation_v2` 也接收状态；但编译器生成的无载荷 resume 函数只接收 token，路由器原先丢弃状态。`FAvidScriptSessionContinuations::CancelEntry` 对普通 Timer 取消直接释放回调并终结绑定的生产者 Task，不派发 `Cancelled`。因此只删除 Semantic 的拒绝条件会让脚本在意外失败后继续执行，也无法在有效 Session 的主动取消路径运行 `finally`。

本切片先让编译器生成的无载荷路由只在 `Completed` 时调用 resume；其他状态在进入脚本前 trap，保持 fail-closed。显式声明的无载荷回调沿用原合同。这个保护只收紧现有路由的执行，不改变当前 Semantic/IR 数据格式；它不等于直接 `await` 的取消清理已实现。固定 SDK 8.0.416 的 Guest 自执行回归 **553/553** 通过；正式构建后 Win64 Automation **1/1** 通过，Wasmtime JIT/WAMR Interpreter 正常组合路径 **6/6**，伪造 `Failed` 状态的 Guest 拒绝 **2/2**，结果和清理字段保持未写入。两后端分别报告 `guest_trap`、`trap`，continuation owner teardown 后 continuation 和 Task 归零。日志：`Saved/Logs/AvidScript_CompiledIntegratedLanguageFlow_20260925T201046210Z.log`。

Session owner 现提供独立的 `ScheduleDelayWithCancelResume`，在分配 Timer entry 时记录 opt-in。有效 Session 的主动取消派发一次 `Cancelled`，并在清理回调终结后才取消绑定的生产者 Task；已取消 source 的后绑定也走同一终态。普通 `ScheduleDelay` 不变，teardown 不派发 Guest 清理。Win64 no-clean Editor 构建与 `AvidScript.Runtime.Continuation.CancelResume` Automation **1/1** 通过，核对了状态帧、Task 延迟终结、重复取消和 teardown；日志：`Saved/Logs/AvidScript_CancelResume_20260925T202443152Z.log`。

Host 新增 `avidscript.avid_continuation_delay_cancel_resume_v1`，签名 `(fi)I`；旧 `env.continuation_delay` 保持不变，新 import 没有 `env` 别名。VM catalog、WAMR trampoline、Wasmtime 静态适配、Runtime dispatcher、绑定 schema 和生成的 C# 内部声明均已接线。Win64 no-clean Editor 构建通过；VM import 合同、静态参数转发、Runtime Host 边界、schema、C# 生成器和 manifest 正反例六项聚焦 Automation 各 **1/1** 通过。首次 manifest 测试因 `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.avidscript.json` 未生成而失败；固定 SDK 8.0.416 构建该夹具后重跑通过。日志：`Saved/Logs/AvidScript_CancelResumeImport_20260925T203355630Z.log` 至 `Saved/Logs/AvidScript_CancelResumeImport_20260925T203518138Z.log`，以及 `Saved/Logs/AvidScript_CancelResumeManifest_20260925T203844161Z.log`。固定 SDK Guest 自执行回归 **553/553** 通过；既有 Win64 组合闭环 Automation **1/1**、双后端正常路径 **6/6**、异常状态保护 **2/2** 通过，直接受保护 `await` 仍按 `ASCS3002` 拒绝，日志：`Saved/Logs/AvidScript_CompiledIntegratedLanguageFlow_20260925T204123691Z.log`。尚未让 C# 编译器选择新 import，也未用实际 WASM 模块执行其取消路径。

Semantic 前端新增内部开关控制的 **43/1.52** 预览：`try/finally` 中的直接 `NextTickAsync`/`DelayAsync` 记录正常后继与独立取消后继，故障后继保持 `-1`。读端验证生产者类型、受保护区域、取消终点和清理入口；旧 **42/1.51** 不接受这种路由。正反例、序列化往返及原有 Semantic 自执行回归 **406/406** 通过。正式 CLI 未打开此开关；直接 `await` 仍以 `ASCS3002` 拒绝，等待实际 WASM 的正常/取消双分支验收。

Guest 编译器现可读取内部 Semantic 43 预览并生成 **IR 23/1.22**：受保护 Timer await 调用 `avid_continuation_delay_cancel_resume_v1`，无载荷路由只允许 `Completed`/`Cancelled`，resume 从保存的状态帧分别进入正常后继或取消清理后继。直接取消不借用 Task 语言错误 owner；绑定的生产者 Task 由 Session owner 在清理回调结束后终结。IR 读端验证版本配对、导入签名、调度点、状态分支和取消目标；伪造路线、缺失路线和旧版夹带新字段的负例均被拒绝。固定 SDK 8.0.416 的 Guest 全量回归 **554/554** 通过，正例 IR 可序列化往返并编译成 WASM。尚未对实际 WASM 执行 `Completed`/`Cancelled` 双分支，也未接通正式构建脚本的显式开关。

## 目标合同

1. 受保护的直接 await 使用状态可见的 compiler resume。沿用 `avid_on_continuation_v2` 的状态参数，不改 UE 导出签名。`Completed` 进入正常后继；仍有效的 Session 主动取消时，`Cancelled` 进入独立清理后继，跳过 `catch` 并由内到外执行 `finally`；`Failed` 和非法状态走 Host/VM 故障隔离，不得进入 C# `catch`。
2. 调度需要显式 opt-in 的“取消终态可恢复”合同，保存在 continuation entry 上；旧模块和普通无载荷回调的取消行为不变。opt-in 必须在取消源绑定前原子生效，不能在 `BindCancellationSource` 可能同步取消之后补设标志。Object/World teardown、reload 后的失效 Session 不派发 Guest 清理，只由 owner 回收资源。
3. Semantic **43/1.52** 与 Guest IR **23/1.22** 已建立受控编译链，运行时验收待完成。状态可见 await 记录正常/取消目标及生产者类型；不伪造语言故障目标或源 Task 错误租约。验证器重算受保护区域、取消清理顺序、状态帧及目标可达性，旧版本夹带新路由必须拒绝。Task await 的语言故障路线继续使用现有 Task 租约和 `catch_match`。
4. 生产者 Task 在主动取消后须等 Guest 清理完成，再传播取消并释放最后一个 owner；清理若抛出语言错误，按 C# 规则替换取消终态。任何调度、状态存取、Host import 或 VM trap 失败都不可被脚本 `catch` 捕获。

## 验收

| 场景 | 预期 |
| --- | --- |
| `NextTickAsync` / `DelayAsync` 正常恢复 | 与同源码 .NET 值和 `finally` 次数一致 |
| 有效 Session 主动取消 | 不进入 `catch`；`finally` 一次；生产者 Task 为 `Cancelled` |
| World/Object teardown、成功或失败 reload | 不重新进入失效 Guest；continuation、Task、托管根归零 |
| Host 故障、VM trap、无效状态 | fail-closed，不伪报语言异常，也不执行等待后的用户代码 |
| 伪造 Semantic/IR 路由或旧版产物 | 读端拒绝，不生成可执行 WASM |

同源 .NET 参考、Semantic 正反例、Guest IR 验证与实际 WASM 执行都要覆盖正常和取消分支；Win64 Wasmtime JIT/WAMR Interpreter 分别检查结果、清理次数、错误类别、双重派发与遗留根。当前完成受控 Semantic/IR 编译链，尚缺正式构建入口、`try/catch/finally` 直接 await 及真正的 C# 清理执行验收；[`TestAvidScriptIntegratedLanguageFlow.ps1`](../../Build/TestAvidScriptIntegratedLanguageFlow.ps1) 继续把受保护直接 await 作为负例。
