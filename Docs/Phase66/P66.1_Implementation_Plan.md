# P66 实施计划

## 批次

| 批次 | 责任与写集 | 完成要求 |
| --- | --- | --- |
| P66.A | Semantic 语言契约与词法函数；Semantic/Guest/WASM 测试 | 静态局部函数由源代码完整编译执行；明确捕获/异步/泛型等当前边界；稳定符号、CFG、可达性和诊断 |
| P66.B | Semantic/Guest/IR 的捕获环境与一等委托 | 局部捕获读写、共享环境、lambda、方法组、delegate/event 常用组合；所有权、reload 和 teardown 边界明确 |
| P66.C | 泛型/集合、错误处理与异步组合的语言及运行时合同 | 日常玩法功能不依赖改写成受限样例；类型专门化、传播/清理和挂起语义有执行证据；不暗示全 .NET 支持 |
| P66.D | 集成样例、兼容性、集中审查和 Gate | 从普通源码验证完整语言工作流；记录真实剩余限制，更新后续结构热重载/调试方案 |

## 当前状态

[持久事件状态](P66.B_Persistent_Event_State.md)已从原生订阅根、类型化 Guest ABI 接到生成的 C# 显式订阅接口。按 [UE 事件语言合同](P66.B_Event_Language_Contract.md)，Semantic 30/1.34、Guest IR 14/1.13 现已使选中的 UE 事件可用 C# `+=` / `-=` 执行。Windows 两后端的真实 UE 测试覆盖重复和组合移除、捕获变量、绑定实例、回调内增删、显式订阅共存、源与处理函数各求值一次、无效默认源拒绝、`ref/out`、单播返回及 GC；Session 停止与 World 清理分别验证自动解绑。聚焦 Automation 1/1 和 no-clean Editor 构建通过。跨 World/错误代际的语言源码组合、单播占用冲突、重载候选及真实游戏长时间运行尚未验收，P66.B 仍进行中。下列较早交付条目的限制按各自记录时点解释。

语言事件基础增量（接入源码语法前的记录）：Session 区分显式订阅与每源/事件唯一的语言桥接；`avid_event_language_subscribe_v1`、`avid_event_language_lookup_v1` 接通 Wasmtime/WAMR。Guest IR 14/1.13 增加类型化语言订阅与查找操作，旧版 IR 无权使用；WASM 发射器在查找返回后立即建立帧根。由 Guest IR 生成的 WASM 在两个后端同时建立显式与语言桥接，经过状态查找、强制 GC 和单独取消；当时 Windows 聚焦 Automation 5/5、Guest IR 35/35、WASM 后端 319/319、C# Guest 467/467、正式构建合同 5/5，no-clean Editor 构建通过。此时尚未接入普通 C# `+=` / `-=`；当前能力见上段。

生成接口增量（接入源码语法前的记录）：被选中的事件在 owner 对象代理上生成带稳定 ID 和 ordinal 的强类型 C# `event`，并在生成时拒绝与代理成员冲突的名字；现有 token 订阅接口保留。当时 Editor facade Automation 1/1、生成 facade 的 Guest 回归 7/7 和 no-clean Editor 构建通过；后续已接入 `IEventAssignmentOperation` 与 Guest 组合/移除，当前能力见上段。

[实例异步调用](P66.B_Async_Invocation_Contract.md)已将 Semantic 28/1.32 的接收者、值参数和恢复状态接入双后端执行。支持范围内的普通类、UE 实例、静态辅助方法及绑定委托可启动 `async void`；跨 UE 对象调用的等待归目标 Session 所有，取消、对象 GC、World 清理和重载退休均有实际源码验证。Guest 452/452、Semantic 371/371、正式构建合同 5/5、UE 聚焦 Automation 34/34。下一组推进持久事件订阅；Task/值结果与可组合异步仍由 P66.C 继续，P66.B 不标记完成。

[异步闭包执行](P66.B_Async_Closure_Scopes.md)已把 Semantic 27/1.31 的实际作用域和进入边接入环境分配、退出清理与状态恢复。受支持的静态 async 入口可直接创建共享捕获闭包；循环迭代、局部函数、引用替换、捕获等待结果及恢复前后的取消/teardown 已有双后端运行证据。当前继续实例 async 与持久事件订阅，P66.B 保持进行中。

[持久托管根](P66.B_Persistent_Managed_Roots.md)已从 C# 静态异步入口贯通状态对象、IR 11/1.10、WASM 与双后端执行。实际源码验证局部对象别名、循环图、循环内等待、含引用结构体和同步函数返回的闭包跨 await 保留，并覆盖主动取消与 owner teardown。正式构建入口已同步新版 IR 合同；async 内创建的共享捕获环境由上述新组接通。实例 async、持久订阅、托管暂停帧和对象图热重载仍未完成。

最新方法组增量：[跨对象 UE 方法组绑定](P66.B_Ue_Delegate_Binding.md)在创建委托时选择实际实现，连接同域直接、base、virtual/interface 的同步绑定与调用。委托保持目标弱身份和方法身份，接口/基类视图的相同覆写支持相等与组合移除。下一步转为持久事件/await 根与取消的组合合同；P66.B 仍未完成，下面各交付条目的限制按其记录时点解释。

最新增量：[Host 选择的动态方法派发](P66.B_Host_Selected_Method_Dispatch.md)已连接真实 C# 注册 UE 类型的 virtual/override、显式/隐式接口与 `base` 固定调用，保持共享引用、ref/out 别名和 A→B→A。下列历史小节中的“虚/接口尚未接通”仅适用于其交付时点。当前继续跨 owner 方法组及持久事件/await 根；普通托管类多态、默认接口、封闭泛型接口执行、结构热重载和完整调试不由本增量证明。P66.B 仍未完成。

P66.A 已完成首组编译器实现与托管/WASM 执行验证，详见 [局部函数结果](P66.A_Lexical_Functions.md)。
P66.B 已实现 [直接调用捕获与异步状态保存](P66.B_Direct_Capture_Results.md)、[版本化委托签名合同](P66.B_Delegate_Signature_Contract.md)、[Guest 函数引用和受检间接调用](P66.B_Indirect_Call_Results.md)、[C# 静态方法组委托](P66.B_CSharp_Method_Group_Results.md)及 [同步无捕获 lambda](P66.B_Lambda_Results.md)，均有对应验证。
共享/逃逸捕获已完成 [环境与绑定分析合同](P66.B_Closure_Plan_Results.md)、[环境堆、精确根与循环回收核心](P66.B_Managed_Heap_Results.md)、[WASM Host ABI 与调用根清理](P66.B_Managed_Heap_ABI.md)及 [Guest 受追踪引用与自动根插桩](P66.B_Guest_Managed_References.md)。[C# 闭包执行](P66.B_CSharp_Closure_Execution.md)连接共享 cell 与同步逃逸环境，[C# 内部引用](P66.B_CSharp_Borrowed_References.md)进一步支持捕获值 ref/out、结构体内部写入与委托转发。委托身份、组合/移除、结构体绑定、支持范围内的普通引用对象及 this 捕获已交付；下一步连接 UE receiver、持久回调及跨 await 生命周期，P66.B 不标记完成。
P66.C/D 与 P67-P69 尚未完成。局部能力的通过不能解释为语言完整、框架成熟或竞品领先。

[委托身份与相等](P66.B_Delegate_Identity.md)已连接单目标 `==`/`!=`；[调用列表](P66.B_Delegate_Lists.md)进一步保留顺序、重复项、最后连续匹配移除、返回值及 ref/out 共享位置。[结构体实例委托](P66.B_Bound_Value_Delegates.md)绑定独立受追踪副本。语言 event 与跨 await 生命周期仍未完成。

[普通引用类合同](P66.B_Reference_Class_Contract.md)已补 Semantic 24/1.28 的基类、构造与初始化事实以及安全读取；[普通引用对象执行](P66.B_Reference_Object_Execution.md)进一步连接支持范围内的构造、共享字段与同步实例委托，[this 捕获](P66.B_Receiver_Capture.md)连接同步闭包与原实例保活。UE receiver、持久事件/await 根和完整类语义仍未完成。

[共享内部引用](P66.B_Borrowed_Reference_Contract.md)已完成 IR 6/1.5、WASM 与双后端执行验证，C# 前端已连接同一描述符。Host ABI 保持独立，不将同步借用带入持久存储或 await；完整 ref/in/ref readonly 表达式范围继续由语言合同与执行测试扩展。

P66.B 的 [控制流分配合同](P66.B_Closure_Allocation_Results.md)已明确函数/for/块入口，现由 C# 执行 lowering 使用。P66.C 必须覆盖同步 foreach 的枚举器异常清理 CFG，不能将已有迭代变量作用域分析视为同步 foreach 可运行；既有受控 async 数组迭代另有实现与测试。

执行时保留 P65 全部现存改动和债务。每一独立可审查小节完成集中验证后按精确写集提交；生产门禁与真实开发体验分别记录。

[UE receiver 运行时校验入口](P66.B_Ue_Receiver_Authority.md)已提供双后端 Session/registry/代际/类型/World 校验，并连接 WAMR 补充导入及生成类型预备调用。它是 UE 实例委托的前置合同，编译器执行接入仍需完成，不能视为该语言能力完成。

[方法派发语义合同](P66.B_Method_Dispatch_Contract.md)进一步提供 Semantic 25/1.29 的 virtual/override/sealed、槽身份及调用位置派发事实，区分 base 方法组和虚调用，保留旧版产物兼容。运行时动态路由、UE 实例委托和持久所有权仍待连接。

[当前 owner 的 UE receiver 执行](P66.B_Ue_Receiver_Execution.md)已连接直接实例委托与同步 this 捕获，并通过真实 C# WASM 双后端验证。下一步是跨实例及虚派发路由，再连接事件/await 持久根；不将当前 owner 范围扩大解释为完整 UE 实例调用能力。

[生产返回根授权](P66.B_Returned_Root_Authority.md)已把当前调用帧归属、显式 SetRoot 权限、嵌套 scope 恢复与失败传播接入原生生产实例入口。双后端验证返回后 Host GC 保活、无帧嵌套不能继承或转交祖先根，原生堆 15/15、C# receiver 25/25、GeneratedTypes 17/17 通过。下一步仍是将 C# 方法帧接到生产目标路由，不能将这项根保护当作完整跨对象语言执行。

## 下一组的架构约束

[真实 C# 生产跨对象方法路由](P66.B_CSharp_Production_Method_Routes.md)已接通同域同步直接调用，覆盖真实源码的 A→B→A、完整混合参数、共享对象、ref/out 别名、重载、强制 GC 与失效目标。Guest 392/392、IR 35/35、backend 161/161、原生堆 15/15、GeneratedTypes 18/18 与堆回归 5/5 通过。当前下一步转为 virtual/interface 的实际目标选择、跨 owner 委托和持久事件/await 根；旧条目中的未接通描述保留为对应交付时点，P66.B 仍未完成。

[跨实例方法路由设计](P66.B_Ue_Method_Routing_Design.md)记录 `cb5f95ab` 源码复核：对象 Router 已存在，但生成入口只实现三种参数形状，Session 禁止活动调用重入，Guest 根与内存归属各实例。下一组先统一方法目录、签名与执行域，再连接目标调用和委托；必须保留 ref/out 别名、普通对象身份、同步 A→B→A 与目标状态，不以放宽 owner 校验或标量专用桥接替代完整目标。

结构热重载、跨帧调试和迁移失败的关键假设提前做受控探针，结果进入 P66.D 及 P67/P68 方案冻结；正式交付顺序与 Phase 批次保持不变。当前未宣称这些探针通过。

[UE 实例方法目录](P66.B_Ue_Method_Catalog.md)已落实上述首组的 Semantic 26/1.30 签名与路由元数据，包含私有方法、覆写槽、显式/隐式接口、默认接口和封闭泛型接口身份；冷路径解析及 Guest/生成器输入拒绝已有测试。它尚未形成 Guest IR/Host ABI 的跨实例可执行合同，不能把方法目录完成记为整个方法路由组完成。下一步继续明确目标执行域、共享引用与同步重入，并以双实例 A→B→A 探针验证；P66.B 保持进行中。

[VM 重入探针与预算修复](P66.B_Vm_Reentrant_Execution.md)已验证共享 VM 的内存/global 可见性、资源 scope 退出、内层 trap 和延迟卸载；WAMR 嵌套入口现在消耗外层预算。双后端 2/2 与 WAMR 回归 6/6 通过。继续验证执行域/实例拆分和真实托管引用；尚未开放 Runtime 跨实例入口。

[生成实例调用链](P66.B_Generated_Invocation_Chain.md)进一步接入 Router 的注册/代码代次与父调用身份、深度/入口预算及失败传播，并通过真实 Session 重载与故障诊断回归。C# receiver 10/10、GeneratedTypes 8/8 通过。下一组仍需连接共享 VM/堆/静态状态、实例上下文与完整 C# 跨对象调用；不把独立原生链和 VM 探针当成该端到端能力完成。

[Host 重入销毁保护](P66.B_Generated_Host_Lifecycle.md)修复活动调用拒绝 teardown 后仍销毁 Session 的问题，并保护包事务期间的实例所有权；Windows 构建、C# receiver 10/10、GeneratedTypes 8/8 通过。当前拒绝需要调用者空闲时重试，自动延迟清理仍未实现。该生命周期修复不改变 P66.B 的完成要求，共享执行域与持久根继续推进。

[共享 Runtime 上下文入口](P66.B_Runtime_Context_Invocation.md)已在两个后端验证真实堆/内存的 A→B→A、实例 owner/订阅上下文恢复、预算与错误清理；Windows 构建、C# receiver 10/10、GeneratedTypes 9/9 通过。接下来把执行域的 VM/堆/代码所有权与实例资源分离，连接现有 Host/Session 和 C# 方法路由；当前仍不是生成实例生产路径的共享 VM，更不是持久 event/await 完成。

[Runtime 持有生成绑定](P66.B_Runtime_Owned_Generated_Bindings.md)已使 receiver/属性 import 随 Runtime 存活，通过当前实例的弱权限解析目标；真实 C# 在双后端共享同一 VM 的静态状态，并读写不同实例的 UE 属性，原注册清理后 peer 继续执行。补齐 WAMR scalar getter/void setter 与 f64 签名适配；C# receiver 16/16、GeneratedTypes 10/10、WAMR registry 4/4 通过。生产 Host 仍为每实例建立 VM；下一组分离执行域与实例资源，连接 module/instance 生命周期、完整方法路由、持久根及共享 reload 事务。

[实例上下文回调](P66.B_Contextual_Callbacks.md)已统一 continuation/委托的嵌套上下文、状态消费与失败传播，并接入生成 Session 的候选准备、重载和停止。双后端真实 Timer 与 Session 测试通过：C# receiver 16/16、GeneratedTypes 12/12；旧 continuation 10/10、委托订阅 5/5 回归通过。生产 Host 的共享执行域及 C# 跨 await 持久根仍未完成；下一组继续分离 VM/堆/代码与实例资源的生命周期。

[实例执行状态分离](P66.B_Instance_Execution_State.md)已从 Runtime 提取生命周期、Timer 队列/时钟与观察计数，新增受检实例 BeginPlay/Tick/EndPlay 和 retirement；双后端证明同一 VM 中结束 A 后 B 继续，且共享 global 保留。C# receiver 16/16、GeneratedTypes 13/13、生命周期/Session/回调回归 36/36 通过。生产 Host 仍是独立 Session/VM；下一组按该报告的执行域所有权、故障传播和包级 reload 合同接入，不以仅共享指针代替完整生命周期。

[生成 Session 实例状态接入](P66.B_Generated_Session_Execution_State.md)将生产成员、调度/事件、continuation/委托、调试恢复与快照统一到显式状态，保留候选事务、故障隔离和旧 Session 路径。Windows 构建、C# receiver 16/16、GeneratedTypes 14/14 通过。下一组整合 Host 的共享执行域、域级故障传播与包级 reload；本组未改变生产 VM 的独立所有权，P66.B 不标记完成。

[全包准备后发布](P66.B_Package_Prepared_Reload.md)已将包级 reload 改为准备全部候选、统一复核再发布；失败保留原 VM、global 与实例状态，native effects 逆序还原，无法恢复时全包停止。Windows 构建、C# receiver 16/16、GeneratedTypes 15/15、生命周期/回调回归 36/36 通过。下一组继续把 VM/堆/代码所有权提升到包、World、registry 执行域，处理最后 owner 卸载、域级故障传播和共享候选迁移；这不是共享 VM 或完整 C# 跨对象调用的完成，P66.B 仍进行中。

[生产共享执行域](P66.B_Production_Execution_Domain.md)已把上述所有权合同接入生成 Host：同域 VM/堆/静态状态共享，实例退出独立退休，最后 owner 卸载；失败使全域停止，其他 World 保持运行。候选按域创建、显式迁移一次并按包发布。真实 C#/强制 GC、Timer 取消、域故障与重载后新 owner 均有双后端执行证据；Windows 构建、C# receiver 16/16、GeneratedTypes 16/16 通过。下一组继续实现完整方法签名、调用链与 C# A→B→A，再接持久事件/await 根；共享执行域不是方法路由或 P66.B 的完成。

[生产实例上下文入口](P66.B_Production_Method_Entry.md)连接当前实例权限、同域目标索引、prepared code 和同步重入；完整语言方法 ABI 继续以间接参数帧接入，保持对象身份与 borrowed ref/out 别名。本组执行证据见该报告，不将原生调用探针当作完整 C# 方法派发。

[间接参数帧执行基础](P66.B_Indirect_Method_ABI.md)已交付 IR 7/1.6、类型化帧布局、固定签名 WASM 适配器和调用者 root 接管；双后端验证 16 参数、含引用结构返回、递归、共享别名和 GC。IR 35/35、backend 148/148、Guest 362/362、Windows 构建及堆 Automation 5/5 通过。当前调用点来自 Guest IR；下一组连接真实 C# 方法目录、生产目标路由和虚/接口派发，并验证 Host 返回边界的保活及恶意帧拒绝，不把此基础当作完整跨对象能力。

[C# 方法参数帧](P66.B_CSharp_Method_Frames.md)进一步将目录、直接调用和绑定方法组接入类型化适配器，统一隐藏 receiver 表示但保留原类型权限。IR 8/1.7 的 roots-only 模式支持无闭包 ref/out/in，双后端验证无占位分配、19 个显式参数、含引用结构返回、共享别名、递归和 GC。当前仍是同 owner 执行；下一组继续生产目标路由、虚/接口选择和根授权，不能以该结果关闭跨对象调用或 P66.B。
