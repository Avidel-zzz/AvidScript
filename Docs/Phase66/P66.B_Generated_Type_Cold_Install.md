# P66.B Windows 生成类型冷启动安装

## 结果

五类型 `ScriptDefinedTypes.cs` 样例通过正式 `BuildCSharpScriptTypes.ps1 -HeadlessRelease` 生成 Win64 包并安装到插件当前包指针。首轮同结构安装后，样例新增 `LaunchSpeed` UPROPERTY、`GetLaunchSpeed` UFUNCTION，并让三参数 `ConfigureLaunch` 写入新属性；正式生成链报告 `native_rebuild_required`，原生结构哈希从 `499323b4...` 变为 `3b924382...`。no-clean UBT/UHT 构建与独立 Editor 冷启动通过。`AvidScript.GeneratedTypes` Automation 8/8 覆盖新属性的反射、默认值、C# 方法写入与 float 返回，并继续覆盖 Cook 包加载、继承、生命周期、RPC 和 RepNotify。

## 修复

- 生产包加载先读取并校验规范 WASM，再用受检类型注册表准备精确的 receiver、属性、方法帧和动态派发 Host import 授权；加载后复核 WASM 哈希，避免预检和执行使用不同字节。
- 调试映射的 `defined_function_count` 计入 WASM 后端为 framed export 额外生成的函数，保持 Guest IR、调试偏移、构建合同与运行时校验一致。
- UE 将 World 标记为 teardown 后仍会调用 Actor/Component `EndPlay` 与 WorldSubsystem `Deinitialize`。仅这些原生终止回调可在其所属 Session 中完成同步 C# 逻辑；普通入口、异步恢复和失效 receiver 仍受原有 World 栅栏限制。owner 已失效时，Session 清理跳过无法安全执行的通用 `EndPlay` 导出。
- 原生生成壳的 UFUNCTION 入口原来只执行无参、单 float 参数和无参 int32 返回。现在在准备阶段按 UFunction 反射类型编译受限标量调用形状，支持有界的 bool、32/64 位整数及浮点值参数与返回值；热路径复用该形状，签名不匹配或不支持的 ref/out/复合参数使候选拒绝，而不是静默丢掉调用。本轮实际执行覆盖三参数 `float/bool/int32` 与 float 返回；其他类型组合不由本轮测试证明。

## 验证与边界

- C# Guest 自执行回归 `472/472`，正式 Guest 构建合同 `5/5`。
- 前端完整构建集成 `15/15`、预备语义产物 `13/13`、发布合同 `14/14`、语义缓存集成 `8/8`；缓存发布清单现按 Semantic 30/1.34 记录。
- UE 5.8 源码版 `AvidTPSTemplateEditor Win64 Development` no-clean 构建通过。
- 改变原生结构后的独立冷启动 Editor Automation：`AvidScript.GeneratedTypes` `8/8`、`AvidScript.Runtime.GeneratedTypes` `20/20`、`AvidScript.Editor.GeneratedTypes.BlueprintSubclass` `1/1`；进程退出为零，测试队列正常结束。此前同结构的 receiver 权限负例 `1/1` 与 World teardown `1/1` 已通过；结构变更后的 receiver 权限负例再次 `1/1`。
- 上述均为 Windows 自动化证据；PIE 操作、网络客户端/服务器、长时间游戏运行、Android/iOS 与事件跨 `await` 的新生成壳组合尚未由本组覆盖。P66.B 尚未完成。

## 后续增量：生成类型异步 UFUNCTION

五类型样例增加 `[UFunction] async void SetLaunchSpeedNextTick(float)`。正式生成链产生原生函数壳、Guest IR 与预编译 WASM；Win64 no-clean UBT/UHT 通过。Editor 自动化在两个自动生成的 `Projectile` 实例上从原生函数进入 C#，确认两者在下一帧前各有一个 continuation、参数和 `this` 在恢复后仍正确；销毁其中一个 Actor 后，它不再写入属性，另一个实例仍可恢复。同包包含 Actor 与 Subsystem：Runtime 在没有使用 facade packed Self 的生成实例上按类型注册表校验实际 UClass，避免把绑定包的 Actor Self 要求误加到 Subsystem；真实使用 packed Self 的调用仍按原类型要求校验。

验证：`AvidScript.GeneratedTypes` **9/9**、`AvidScript.Runtime.GeneratedTypes` **20/20**、`AvidScript.Architecture.Session.TypedOwnerValidation` **1/1**，三次独立 Editor 命令均完成队列并以零退出。当前组合只证明生成类的异步函数入口和 owner 取消；生成类自身声明 UE 事件、事件回调跨 `await`、实际 PIE 与长时间游戏运行仍未完成。
