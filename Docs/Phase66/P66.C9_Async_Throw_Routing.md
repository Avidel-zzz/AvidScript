# P66.C9：异步取消中的显式抛错

状态：Semantic、Guest IR、WASM 和正式脚本构建入口已实现；Win64 Wasmtime / WAMR 的同源对照与 Session teardown 测试通过。正式生成 Actor 的销毁、World 退出、reload/rollback 组合已通过 Win64 Editor 自动化 108/108。此项承接 [C8](P66.C8_Task_Local_Lifetime_Contract.md)，P66.C/D 尚未完成。

## 源码与语义

```csharp
try
{
    try { await WaitAsync(); }
    catch (OperationCanceledException) { throw new ArgumentException(); }
    finally { CleanupCount++; }
}
catch (ArgumentException) { return 90; }
```

显式 `throw` 必须进入所属区域的异常出口：try 进入本层有序 catch；catch 经过本层 finally 后进入外层；finally 抛错直接进入外层，不能重入自身或同级 catch。新异常替换正在传播或已捕获的旧异常，旧 owner 释放一次。正常返回、取消、重新抛出及清理抛错均保持 C# 的清理顺序。

## 合同与写集

- Semantic 46 / 1.55 新增 `raise_exception` 控制转移。它携带既有受检异常构造表达式与异常后继，仍由 `ErrorPlan.Throws` 记录类型、构造器和源码位置。旧 `throw` 保持终结 Task 的原语义，旧版本拒绝新转移。
- Roslyn 结构化 CFG 产生区域成员和后继；读端从区域/父作用域独立核对路由。新的异常边进入状态存活、Task 作用域释放和异常 owner 分析。异常替换将 owner 状态变为 Fault，不把旧 Cancellation 带入新 handler。
- Guest IR 26 / 1.25 记录异常创建/替换到分派出口的路由，并由独立读端验证。复用现有 Task 异常对象 ABI；不新增裸指针或绕过 catalog 的接口。
- Guest lowering 在新异常建立成功后替换当前异常 owner，再沿受检边执行 finally/catch。Host 失败继续 trap，不能进入语言 catch。
- 源码仍使用现有预览开关与 bounded language-error 模式。当前支持的零参数框架异常构造器保持原范围。catch/finally 内 await、catch 变量/filter、handler 内嵌套 try 是后续工作，不随本项自动放开。

实现归属依次为 `Tools/AvidScript.CSharpSemantic`、`Tools/AvidScript.CSharpGuest`、`Tools/AvidScript.GuestIr` 与其测试；正式构建入口和 Runtime 只增加新版本识别及执行验收。类型系统、同步异常及旧取消合同不另建旁路。

## 验证要求

1. Semantic：try/catch/finally 抛错，嵌套 try，返回值被 finally 抛错替换，Task 别名与循环退出；拒绝跳过清理、同级 handler 重入、伪造目标、缺失 throw site、降版和版本不匹配。
2. Guest IR：新路由的建立、替换与释放；篡改 owner、类型/源码 token、后继、release 或状态清零必须被拒绝。旧版本保留其原有检查。
3. 同一业务源码在 .NET 与 Win64 Wasmtime / WAMR 中对照结果、异常类型和 finally 次序；分别覆盖立即失败、挂起后失败、显式取消及重复等待。
4. 确认 Task、continuation、状态帧和语言错误根归零，保留 Actor/World teardown、reload/rollback 的生命周期边界。

README 只声明已有执行证据覆盖的范围；Session 测试不替代 Actor/World 自动销毁和生成类型验收。

## 已实现：Semantic 46 的异常后继

`raise_exception` 已接入结构化 CFG、throw site 目录、读端区域校验、局部值存活分析、Task 退出边和异常 owner 数据流。每个复制的 finally 抛错点保留自己的 segment 和源位置；读端同时核对区域成员与源 span，防止删除区域归属后绕过本层清理。

新版本模块的全部 Task 局部变量记录词法生命周期。未使用新抛错形态的源码保留原版本；旧版本不能通过夹带新转移或改变目标获得新的异常语义。源码预览开关、构造器范围和未支持语法的拒绝保持原边界。

固定 SDK 8.0.416：专项 **81/81**、Semantic 全量 **611/611** 通过。专项覆盖 try、catch、finally、新异常覆盖取消、嵌套 finally、等待前/后抛错、返回被清理抛错覆盖、Task 别名与循环，以及目标/元数据/版本篡改。复现：

```powershell
dotnet run --project Tools/AvidScript.CSharpSemantic.Tests/AvidScript.CSharpSemantic.Tests.csproj -c Release -- --async-throw-routing
```

以上为源码合同检查，证据目录：`Saved/AvidScriptAsyncThrowRouting/20260926-source/`。WASM 与运行时证据如下。

## 已实现：Guest IR 26 与 Win64 执行

每个 `raise_exception` 转移携带异常类型和源码 token。WASM 先创建新的 faulted Task 并持有异常对象，确认成功后释放旧异常 owner、清零旧类型，再发布新 owner 并进入目标分支。分配或建立异常失败会 trap；失败时创建的 Task 也会释放。IR 校验分别检查新对象、catalog、旧 owner 释放、清零、发布和后继。Task 作用域退出桥仍由生命周期校验器独立验证。

两个新组合修复了读端与 lowering 的旧假设：

- 一个方法可以先执行 `try/catch`，再执行未被该处理器保护的 `await`。后一次等待失败应终结返回的 Task，不能跳回已经离开的 catch。IR 26 对这种终结分支检查 source/producer，仍拒绝遗漏受保护等待的路由。
- `finally` 无条件抛错时，先前求值的返回值不会被读取。只有新合同中不存在正常 return 后继的情况才允许保留这个写入；普通孤立的返回槽仍被拒绝。

27 组业务源码在 .NET 执行，并编译为同源 WASM。覆盖 try/catch/finally 抛错、异常替换、rethrow、无条件 finally 覆盖返回、跨 Task 传播、重复等待故障 Task、Task 别名、循环作用域，以及子 Task/直接等待取消后改抛新异常。清理顺序用十进制 Trace 记录，例如 `1234` 表示四段清理按顺序各执行一次。

| 检查 | 结果 |
| --- | --- |
| Guest 抛错专项、同源 .NET、IR 篡改与确定性序列化 | 235/235 |
| CSharpGuest 全量 | 1033/1033 |
| GuestIr / WasmBackend 全量 | 159/159、356/356 |
| Win64 no-clean Editor 构建 | 通过 |
| UE Automation：编译产物执行、catalog 拒绝、Host import 版本检查 | 3/3 |
| 27 组源码 × 2 后端 × 3 生命周期模式 | 162/162 |
| 正式构建入口：立即完成、挂起完成、关闭预览时拒绝发布 | 3/3 |

三种生命周期模式为正常完成、首次挂起后 Session teardown、恢复一次后 Session teardown。测试检查结果与 Trace，并在 teardown 后持续排空队列，确认不会恢复已退休的 continuation。Task、waiter、continuation、状态帧、语言错误根及 GC 后对象计数均归零。Wasmtime 使用 JIT，WAMR 使用解释器；本轮未验收 AOT、Shipping 或移动端。

```powershell
# 生成同源对照用例、构建 Editor，并执行双后端测试
pwsh -NoProfile -File Build/TestAvidScriptAsyncThrowRouting.ps1

# 验证正式构建入口；传入已生成的 binding package
pwsh -NoProfile -File Build/Contracts/TestCSharpAsyncThrowRoutingBuild.ps1 `
  -BindingPackagePath <path-to-package.json>
```

源码构建仍需 `-LanguageErrors bounded -AsyncExceptionFlow -DirectAwaitCleanup -AsyncCancellationFlow`。没有新抛错形态的源码继续使用已有版本；Runtime 只为精确的 IR 26/1.25 与 cancellation profile 开放对应错误能力，拒绝版本不匹配、缺失 catalog 或降级 profile。

首轮证据目录：`Saved/AvidScriptAsyncThrowRouting/20260926-guest/`（托管回归与原生构建）、`20260926T132346982Z/`（双后端、WASM 哈希）、`20260926T132644179Z-build/`（正式构建）。生成 UE 类型的后续验证见下文；catch/finally 内 await 等语言缺口仍按 P66.C 计划推进。

## 生成 UE 类型的生命周期与重载

[GeneratedAsyncThrow.cs](../../Fixtures/Phase66/GeneratedAsyncThrow.cs) 通过正式构建入口生成 `AsyncThrowActor`、UHT 反射信息和 WASM 包。用例保留 Task 别名，并覆盖正常完成、子 Task 取消后改抛 `ArgumentException`、finally 抛错覆盖正常结果或原异常、预先取消后直接 await，以及等待后显式抛错。返回值同时编码业务结果和清理顺序，两代源码仅相差方法体中的 `CodeOffset`。

测试通过原生函数和 `ProcessEvent` 调用三个真实 Actor：两个共享同一个 World 的执行域，另一个处于第二个 World。恢复只由 `World::Tick` 驱动，没有手动调用 Session Tick。矩阵为 **6 种源码路径 × 6 种生命周期操作 × 3 个挂起点 = 108 项**：

- 操作：正常完成、销毁一个 Actor、销毁其 World、准备一个/两个候选后回滚、成功发布两 World 的三个实例。
- 挂起点：首次等待、恢复一次且取消异常仍被 Task 持有、清理完成但属性结果尚未写回。
- 检查：结果和清理顺序、退休实例不再写回、同域其他实例继续运行、回滚保留原 VM/实例状态/Task/等待队列/异常根、成功重载执行新版结果。正常完成后检查 Task、waiter、continuation、状态帧与异常根归零，并执行 GC 确认异常对象释放。

这组测试暴露了一个注册顺序缺陷：绑定已经取消的 token 会先把 continuation 排入就绪队列，编译器随后才保存状态并移交 Task 局部引用。`RetainTaskForContinuation` 原来拒绝所有 ready 项，导致合法源码在 Host import 处终止。修复使其与 `StoreState`、`BindTaskProducer` 保持一致：尚未 dispatch 的 ready 项可继续注册；代际、Session/激活代次、已保存状态、消费状态、引用上限和 producer 冲突检查仍然保留。底层测试同时覆盖预先取消和普通完成、别名释放、dispatch 后拒绝注册，以及 Wasmtime/WAMR 的实际保留导入。

| 检查 | 结果 |
| --- | --- |
| 两代源码的同源 .NET / 编译器专项 | 48/48 |
| CSharpGuest 全量 | 1081/1081 |
| 正式生成包：Semantic 46/1.55 → IR 26/1.25、产物哈希、body-only 分类 | 通过 |
| 无测试专属类型依赖的默认构建与冷启动反射检查 | 通过 |
| Task 所有权、Wasmtime / WAMR 的实际 Task 保留导入 | Automation 2/2 |
| 真实 UHT Actor、World Tick、销毁及重载矩阵 | 108/108 |

证据目录：`Saved/AvidScriptAsyncThrowRouting/20260926-generated/`（托管回归）和 `Saved/AvidScript/GeneratedTaskEditor/20260926T135916052Z/`（正式包、原生构建、Automation）。首次实际生成 Actor 测试在 `20260926T134322143Z/task-lifecycle.log` 留下预取消失败记录，修复后的 108 项包含该原始顺序，没有移除失败用例。

```powershell
pwsh -NoProfile -File Build/TestGeneratedTaskTypesInEditor.ps1 `
  -Fixture AsyncThrow -BindingPackageManifestPath <path-to-package.json>
```

测试入口备份四个当前生成文件，检查两代产物哈希与 `body_only` 分类，再安装测试类型；退出时恢复原文件并重新构建、调用原示例。默认不传 `-Fixture` 时仍运行原 SharedTask 用例。该路径使用 Win64 Editor 的 Wasmtime JIT；底层 Task ABI 另测 WAMR Interpreter。真实 Editor Play、Shipping、AOT 与移动端不在本轮验收范围内。
