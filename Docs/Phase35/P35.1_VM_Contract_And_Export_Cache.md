# P35.1 VM 契约与 Export Cache

状态：Done

## RED

先创建 `AvidScriptVM` 模块测试，引用尚不存在的 `AvidScriptVmBackend.h` 与 `AvidScriptVmExportTable.h`。模块编译明确失败于 C1083 缺失 header，证明测试能够检测接口缺口。

## 实现

- 新增 `IAvidScriptVmBackend`，定义 Load、ResolveExport、Call、Unload 和统计契约。
- 新增 `FAvidScriptVmExportHandle`，使用 slot + generation，Reload/Unload 后旧 handle 失效。
- 新增固定容量 `FAvidScriptVmCallFrame`，避免热路径动态参数容器。
- 新增 `EAvidScriptHostBindingId`、`FAvidScriptHostCall`、`FAvidScriptHostCallResult` 和 `IAvidScriptHostDispatcher`。
- 新增 `FAvidScriptVmExportTable`；同名 export 只调用一次 resolver，Reset 推进 generation。
- Runtime 建立对 VM 的显式依赖，但本小组尚未切换旧 WAMR 调用。
- 架构脚本增加 VM public boundary 与五模块描述检查。

## 验证

- `AvidScriptVM` 模块构建成功。
- `AvidScriptRuntime` 与 `AvidScriptEditor` 消费者增量构建成功。
- 不带 Clean 的 Editor Target 元数据已包含 `AvidScriptVM`。
- `AvidScript.Architecture.VM`：2/2 通过。
- `Build/CheckAvidScriptArchitecture.ps1` 通过。

自动化日志：`C:\tmp\AvidScript_P35_VM_Contract_GREEN.log`

## 下一步

P35.2 在 VM Private 中实现真实 WAMR backend、资源释放顺序和无 import smoke；P35.3 再迁移 native wrapper。
