# P35.5 Phase 35 收尾

状态：Done

## 完成内容

- 新建并接入 `AvidScriptVM` 模块。
- WAMR 全局 lease、module、instance、exec env、native symbols、guest memory 与异常读取已私有化。
- 建立 backend、HostCall、dispatcher、opaque export handle 和 generation 契约。
- Runtime 与 WAMR 完全解耦，并缓存五类标准 callback handle。
- 架构检查覆盖五模块描述、VM Public gameplay 隔离和 Runtime WAMR 禁入。

## 验证结果

- VM 架构与 backend 测试：4/4 成功。
- 完整 `Automation RunTests AvidScript`：122/122 成功。
- Performance：2/2 成功。
- 架构检查脚本成功。

日志：

- `C:\tmp\AvidScript_P35_VM_HostDispatcher_GREEN.log`
- `C:\tmp\AvidScript_P35_All_Final.log`
- `C:\tmp\AvidScript_P35_Performance_CachedHandles.log`

性能样本：

| 指标 | Phase 35 结果 |
| --- | ---: |
| registry resolve average | 0.000184 ms |
| binding get average | 0.000286 ms |
| binding set average | 0.000475 ms |
| runtime load average | 0.0020 ms |
| runtime begin average | 0.0013 ms |
| runtime tick average | 0.0007 ms |
| runtime tick p95 | 0.0008 ms |
| runtime unload average | 0.0065 ms |

Runtime Tick 比 Phase 34 微基准高约 0.0003 ms，绝对值仍低，但比例超过原 5% 目标。Phase 37 将继续消除 call frame 复制、成功路径 FString reset 和 backend 虚调用成本，不把该差异标记为已解决。

## 下一步

Phase 36 引入唯一 `FAvidScriptRuntimeSession` owner，收敛 Runtime、Component 与 Reload 的重复状态，并把 scheduler/event router 从 façade 中拆出。
