# P34.0 Framework Architecture 范围

状态：Done

Phase 34 停止向单体 `AvidScriptRuntime` 继续追加 gameplay 功能，先建立 `AvidScriptCore` 与 `AvidScriptBindings` 模块边界、显式生命周期状态机、对象反向索引和架构约束测试。

目标依赖方向为：

`Core <- VM`、`Core <- Bindings`、`Core/VM/Bindings <- Runtime <- Editor`

本阶段迁移 Core 与 Bindings；Phase 35 抽取 WAMR VM backend，Phase 36 统一 Runtime Session/Reload/Scheduler，Phase 37 完成高频路径性能优化，typed overlap/input/hit 延后到 Phase 38 在新 Event Router 上实现。
