# P34.1 架构 RED 测试

状态：Done

## 范围

- 用测试先定义生命周期合法转换、非法转换、Fault 与 Reset。
- 定义 UObject 重复注册、Release、slot 复用和 generation 前进契约。
- 用静态脚本定义 Core、Bindings 与 Runtime 的模块边界。

## RED 证据

- Core 测试首次构建因缺少 `AvidScriptLifecycleState.h` 失败。
- Runtime lifecycle integration 测试首次构建因缺少 `GetLifecycleState()` 失败。
- Bindings 边界测试首次构建因新模块缺少迁移后的 Binding 类型失败。

这些失败均发生在对应实现加入前，证明测试能够检测目标缺口。

## 结果

实现后生命周期测试 3/3、Runtime 状态集成 1/1、Bindings 边界 1/1 通过。

日志：

- `C:\tmp\AvidScript_P34_Core_Lifecycle_GREEN.log`
- `C:\tmp\AvidScript_P34_Runtime_Lifecycle_GREEN.log`
- `C:\tmp\AvidScript_P34_Bindings_Boundary_GREEN2.log`
