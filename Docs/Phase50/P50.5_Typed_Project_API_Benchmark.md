# Phase 50 Typed Object Benchmark

## 目的

本基准为 Phase 50 的 typed object / checked cast 建立项目内性能采样面，固定比较三条同一 fixture、同一 warmup/sample 规则下的路径：

1. Native `UObject::IsA(UActorComponent::StaticClass())`；
2. `FAvidScriptBindingPackage::Dispatch` 的 `avidscript.avid_object_type_is_a` ordinal capability；
3. 已加载 WAMR runtime 中、由 `avid_on_tick` 触发一次真实 checked cast import 的路径。

另行保留现有 typed binding `GetActorLocation` 指标，供集中 Gate 与 Phase 49 同机基线比较。该指标不混入上述三条 checked-cast 的计时结果。

自动化入口：

```text
AvidScript.Performance.TypedObjectBenchmarkSmoke
```

## 测量设计

默认配置与 Phase 49 一致：warmup `3` 轮、sample `20` 轮、每个 sample `1000` 次操作；统计使用 nearest-rank P50/P95。每轮按正反交替顺序执行 Native、binding、既有 typed binding 与 WAMR，降低固定执行先后导致的 cache 或调度偏差。

fixture 是同一 benchmark world 中的 `USceneComponent`。runner 开始时先重置 benchmark sink；每个 warmup/sample round 在所有计时开始前只生成一次 opaque seed，并在该轮内保持不可变。Native 与 Binding 使用完全相同的 `round seed + OperationTag` 纯计算目标序列，分别在语义等价且都应匹配的 `UActorComponent` / `USceneComponent` 以及对应 ordinal `1` / `2` 之间切换。两条路径独立累计 selector checksum，并显式要求 checksum 相等且非零。

三条 checked-cast 路径统一使用逐 operation 计时，每次都恰好读取一次开始时钟和一次结束时钟。target selector、`OperationTag`、opaque seed、参数准备发生在开始时钟之前；计时窗口内分别只保留核心 `UObject::IsA`、package `Dispatch` 或 `Runtime.Tick`；结束时钟之后才累计 elapsed、selector/result checksum、调用不可内联 volatile consumer，并执行返回值、host crossing 与 exported-memory observation 验证。这样防优化 consumer 和验证成本不会污染核心操作，三条路径也承担相同的时钟读取粒度。

Native、Binding 与 WAMR 的成功结果都会送入不可内联、写入 volatile sink 的 consumer，并累计到最终公开 checksum，避免优化构建消除结果消费。三条 checked-cast 路径的成功 checksum 必须一致。WAMR guest 固定使用 `UActorComponent` ordinal；它与 Native/Binding selector 集合中的祖先匹配目标语义等价，且固定预期结果为 `1`。

descriptor v6 在计时前完成解析，WAMR module 也在计时前完成 load/instantiate 和 `BeginPlay`。因此计时区不含 world 创建、对象注册、descriptor JSON 构建、class load、module load、host 侧 exported-memory 验证或数组首次扩容。

WAMR 模块的 `avid_on_tick(float)` 固定调用一次：

```text
avidscript.avid_object_type_is_a(slot, generation, actor_component_ordinal)
```

guest 将 checked-cast 返回值写到 exported memory 的 `match` 字段。host 在每次 warmup 与 timed tick 后读取并要求 `match == 1`；有效 mismatch 返回的 `0`、错误 ordinal 导致的失败或错误 handle 都不能通过 benchmark。

同一 tick 还执行一次 typed upcast artifact：派生 handle 的两个 `i32` local 被复制到基类 handle local，随后写出 slot、generation 与二者 XOR checksum。host 验证三项，证明 upcast workload 实际执行。runtime counter 每次 tick 的唯一增长必须来自 checked-cast import；upcast 的 host-import 数由真实 counter delta 减去 checked-cast operation 数得到，不使用默认常量。

## 结构预算

| 项目 | 预算 | 采样/断言方式 |
| --- | ---: | --- |
| warm typed dispatch class load | 0 | package instrumentation delta |
| warm typed dispatch reflected-name lookup | 0 | package instrumentation delta |
| typed upcast host import | 0 | exported handle/checksum + runtime counter delta |
| checked cast host crossing | 1 / WAMR operation | warmup 结束与 timed 结束的真实 `HostImportCallCount` delta |
| existing typed binding regression | <= 5% | 仅对同机、同 harness、已记录的 Phase 49 median 计算 |

package load 的 class-load / reflected-name lookup 会单独记录，不能与 warm loop 混淆。当前 schema sentinel 的 reflected function lookup 发生于 load，属于预热成本，不进入 checked cast dispatch 的预算。

公开的 `WasmCheckedCastHostCrossingCount` 只覆盖 timed samples：runner 在 warmup 完成后记录 counter 起点，在全部 timed samples 完成后记录终点，并要求真实差值严格等于 timed WAMR operation count。warmup 本身仍逐次验证 `match == 1`、upcast checksum 与单次 crossing delta。

## 本次实测

2026-07-24 在源码版 UE5.8、Win64 Development Editor、NullRHI 下完成一次独立采样。Automation
`AvidScript.Performance.TypedObjectBenchmarkSmoke` 通过，原始日志为
`C:\tmp\AvidScript_P50_TypedBenchmark.log`。本节记录的是当前机器的项目内数据，不替代最终冻结候选的 Gate 重跑。

| 字段 | 当前值 |
| --- | --- |
| 机器 / CPU | Intel Core Ultra 7 265K |
| OS / 电源策略 | Windows 11 专业版 10.0.26200 / 平衡 |
| UE / 配置 | 源码版 UE5.8 / Win64 Development Editor |
| Rendering | NullRHI |
| warmup / samples / iterations | 3 / 20 / 1000 |
| Native `IsA` median / p95 | 0.000016 / 0.000018 ms |
| Binding ordinal median / p95 | 0.000038 / 0.000039 ms |
| WAMR checked cast median / p95 | 0.000738 / 0.000860 ms |
| Native / Binding / WAMR observable checksum | 三者均为 `15455151067357255872` |
| Native / Binding selector checksum | 二者均为 `14893614634143436057`，非零 |
| typed upcast operations / host imports | 20,000 / 0 |
| timed WAMR crossing delta | 20,000，严格等于 20,000 次 operation |
| warm class load / reflected-name lookup | 0 / 0 |
| existing typed binding median | 0.000049 ms |
| Phase 49 comparable median / regression | 待建立同机同 harness 基线，不能伪造 |

Phase 49 已记录对象生命周期与既有 typed binding 的历史项目数据，但没有可直接当作本 harness median 的冻结样本。因而此 benchmark 明确返回 `pending_same_machine_phase49_baseline`，不会将历史 Avg、异机日志或不同 workload 换算成 5% 回退结论。

## Packed Owner 收益

Phase 50 将 `UE.Self` 从分别读取 slot/generation 的两次 Host crossing 压缩为一次
`avid_owner_get_handle` packed handle crossing。真实 PlayablePickup C# -> WASM -> Runtime 用例显示：

| 事件 | Phase 50 前断言 | Phase 50 实测 | 减少 |
| --- | ---: | ---: | ---: |
| 单次 Tick | 6 crossings | 4 crossings | 2 |
| 成功 BeginOverlap | 8 crossings | 6 crossings | 2 |

减少量来自每个事件中两次 `UE.Self` 访问各节省一次 crossing；反射 UFUNCTION、timer 与 tag 查询次数没有被隐藏或移出计数。

## 解释边界

- 这是 PC Editor/NullRHI 的项目内微基准，不代表 Shipping、Cook、Android 或 iOS；
- WAMR crossing 路径验证真实 checked cast import，不用于推导完整 C# authoring、build 或首次 module load 延迟；
- 对象类型 ordinal 与 cached `UClass` 是当前 descriptor 的 immutable plan，不测量 reflection discovery；
- 未在同机、同项目、同 harness 下测试 Puerts、UnLua 或 AngelScript，因此本报告不宣称 AvidScript 在外部框架间领先或更快；
- Phase 50 集中 Gate 需运行本自动化、保留原始日志与机器信息，再决定是否满足 Phase 49 existing typed binding 的 `<= 5%` 预算。
