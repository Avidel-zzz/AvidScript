# P29.1 Actor Scale RED 测试

状态: Done

RED 阶段先要求 Get/SetActorScale3D binding、runtime handlers、C# sample、adapter v8、scale imports 和连续 Tick 行为。C++ 构建因目标 API 缺失报 C2039；旧 adapter 转入 fallback 并因 AActor scale methods 缺失返回 `direct_abi_unsupported`。

