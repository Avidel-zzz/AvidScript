# P29.0 Actor Scale 与 FTransform Snapshot 范围

状态: In Progress

本阶段补齐 handle-backed Actor scale read/write，并建立只读 `FTransform` snapshot C# 投影。scale 进入 adapter lifecycle v8；`FTransform` local、原子 SetActorTransform、SceneComponent 和反射生成不进入本阶段。
