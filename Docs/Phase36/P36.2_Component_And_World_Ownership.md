# P36.2 Component 与 WorldSubsystem Ownership 收敛

状态：Done
实现提交：`1136e3e`

## 实现

- `UAvidScriptComponent` owner 从 direct Runtime 改为 `TUniquePtr<FAvidScriptRuntimeSession>`。
- 删除 `bPlayActive`，BeginPlay、Tick、Event、EndPlay 全部通过 Session。
- Component stats 只读取调用结果与 Session snapshot，不再决定生命周期。
- `UAvidScriptWorldSubsystem` 同样迁移到 Session，并删除 `bWorldPlayActive`。
- architecture script 固化边界：两个 UE owner 头文件出现 direct Runtime owner 或兼容 active bool 时立即失败。

## 验证

- Runtime 与 Editor 模块增量构建成功，未执行 Clean。
- `AvidScript.Component`、Session、Reload：12/12 成功。
- 加入 WorldSubsystem 后 owner 聚焦回归：13/13 成功。
- C# manifest 的 BeginPlay、Tick、Timer、Event、EndPlay 闭环保持通过。
- architecture script 通过。

日志：

- `C:\tmp\AvidScript_P36_Component_Session_GREEN.log`
- `C:\tmp\AvidScript_P36_Owners_GREEN.log`

## 错误记录

首次 WorldSubsystem 编译时，两个匿名命名空间 helper 同名，在 UE unity build 中发生 C2084 重定义。已改为文件语义唯一名 `CopyWorldSessionLoadResult`，并将预防规则写入 `AGENTS.md`。

## 下一步

P36.3 把 Scheduler 与 EventRouter 作为 Session 私有策略拆分，公共 Session API 保持稳定。
