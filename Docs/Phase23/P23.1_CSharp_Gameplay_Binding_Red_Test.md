# P23.1 C# Gameplay Binding RED Test

状态: Done
日期: 2026-07-06

## 范围

P23.1 先写失败测试, 描述 Phase23 要新增的 gameplay binding 行为, 不写生产实现。

## 新增测试意图

- `AvidScript.Binding.ActorBinding.AddLocationOffsetSmoke`: 期望 C++ binding 可把 Actor 从初始位置移动到 `Initial + Offset`。
- `AvidScript.Runtime.WasmActor.DirectHandlerSmoke`: 期望 WASM runtime direct handler 暴露 `HandleActorAddLocationOffsetImport(...)`, 并能通过 host context 移动 Actor。
- `AvidScript.Runtime.WasmActor.MissingContextSmoke`: 期望缺少 host context 时 add offset import fail closed。
- `AvidScript.Guest.CSharp.SampleShapeSmoke`: 期望默认 C# 样例包含 `Actor.AddLocationOffset` facade 与 `actor_add_location_offset` import。
- `AvidScript.Guest.CSharp.SourceAdapterArtifactLifecycleSmoke`: 期望 source adapter 产物 manifest 声明 `env.actor_add_location_offset` required import。

## RED 验证

命令:

```powershell
& "C:\UnrealEngine\Engine\Build\BatchFiles\Build.bat" AvidTPSTemplateEditor Win64 Development "-Project=<ProjectRoot>\AvidTPSTemplate.uproject" -WaitMutex -NoHotReloadFromIDE -Module=AvidScriptRuntime
```

结果: 构建失败, 且失败原因集中在新增 API 尚未实现:

```text
error C2039: "AddActorLocationOffset": 不是 "FAvidScriptActorBinding" 的成员
error C2039: "HandleActorAddLocationOffsetImport": 不是 "FAvidScriptWasmRuntimeInstance" 的成员
Result: Failed (OtherCompilationError)
```

## 结论

RED 阶段有效。下一步进入 P23.2, 实现 `Actor.AddLocationOffset` 的 C++ binding、WASM host import、C# source adapter 和默认样例。