# P16.1 组件绑定 RED 测试

## RED 用例

新增测试文件：

- `Source/AvidScriptEditor/Private/Tests/AvidScriptEditorComponentBindingServiceTests.cpp`

新增自动化：

- `AvidScript.Editor.ComponentBinding.ApplyManifestToActorSmoke`
- `AvidScript.Editor.ComponentBinding.ApplyCSharpReportToSelectedActorSmoke`

测试意图：

- 从 `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.avidscript.json` 直接绑定到 Actor。
- 从 `Saved/AvidScriptCSharpGuest/ActorLifecycle/actor_lifecycle.csharp.report.json` 解析 manifest，再绑定到当前选中 Actor。
- 如果 Actor 没有 `UAvidScriptComponent`，服务应自动创建组件并写入 manifest 路径。

## RED 命令

```powershell
& "C:\UnrealEngine\Engine\Build\BatchFiles\Build.bat" AvidTPSTemplateEditor Win64 Development "-Project=C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\AvidTPSTemplate.uproject" -WaitMutex -NoHotReloadFromIDE -Module=AvidScriptEditor
```

## RED 结果

首次模块构建因为 UE source-list 缓存报告 `Target is up to date`，按 `AGENTS.md` 既有规范检查 unity 文件，确认新测试尚未进入模块。随后重新运行模块级构建，UBT 报告：

```text
Invalidating makefile for AvidTPSTemplateEditor (source file added)
fatal error C1083: 无法打开包括文件: “AvidScriptEditorComponentBindingService.h”: No such file or directory
```

该失败符合预期：测试已经描述了新服务 API，但生产代码尚未存在。

## 关联实现提交

- `8a15a1a` - `P16 bind CSharp reports to script component`
