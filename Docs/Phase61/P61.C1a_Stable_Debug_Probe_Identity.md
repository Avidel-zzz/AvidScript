# P61.C1a 稳定调试 Probe 身份

## 本节完成

- Debug Map v2 的非 hidden sequence point 现在携带 16 位小写十六进制 `probe_id`。
- Probe 身份由模块身份、Guest 执行函数身份和语义操作身份确定性派生；普通函数身份包含 C# 方法
  符号，async resume 身份额外区分恢复入口；同一输入重复构建保持字节一致。
- 投影阶段对整个模块执行碰撞检测；发现重复身份时以 `ASDEBUG1005` 失败关闭。
- 生命周期构建入口校验 probe 格式、hidden 点空值规则和跨函数唯一性。

## 当前边界

本节只完成断点位置的稳定协议身份，还没有宣称断点暂停可用。后续 P61.C1b 将按该身份插入
后端无关的 `avid_debug_probe` 调用并冻结 Debug/Release 插桩策略；Session pause continuation、
单步和变量快照分别在后续小节实现。

## 验证

- `AvidScript.CSharpGuest.Tests`：`119/119` 通过。
- 确定性测试同时覆盖 Debug Map 与 probe ID 的字节一致性。
