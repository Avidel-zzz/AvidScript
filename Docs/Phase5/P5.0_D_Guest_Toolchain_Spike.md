# P5.0 D Guest Toolchain Spike

日期: 2026-07-04
状态: In Progress

## 1. Scope

本小组验证 D-first guest language 路线是否能进入 AvidScript MVP 主线。验证路径:

```text
D source -> .wasm -> FAvidScriptWasmModuleLoader -> FAvidScriptWasmRuntimeInstance -> actor_set_location -> UE Actor
```

## 2. In Scope

- 本机 D/WASM 工具链探测。
- 最小 D guest ABI sample。
- 可诊断 build script。
- PC Editor automation 加载外部 D artifact。
- 记录 D 路线继续推进或切换 C# fallback 的依据。

## 3. Out Of Scope

- 完整 D runtime / Phobos / GC 集成。
- C# guest 工具链实现。
- Android/iOS device smoke。
- Packaging/cook/stage/pak。
- Editor UI、watcher、热重载界面。
- UE API 面扩展。

## 4. Host ABI

继续沿用 Phase 4 ABI:

```text
module: avidscript
import: actor_set_location(slot: i32, generation: i32, x: f32, y: f32, z: f32) -> i32
```

guest exports:

```text
avid_on_begin_play() -> void
avid_on_tick(delta_seconds: f32) -> void
```

## 5. Toolchain Probe

已运行:

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\Plugins\AvidScript\Build\ProbeDGuestToolchain.ps1"
```

结果:

```text
[AvidScript][DGuest][Probe] start
[AvidScript][DGuest][Tool] ldc2=MISSING
[AvidScript][DGuest][Tool] dub=MISSING
[AvidScript][DGuest][Tool] wasm-ld=MISSING
[AvidScript][DGuest][Tool] clang=MISSING
[AvidScript][DGuest][Probe] result=complete
```

结论: 当前本机 PATH 上没有 D/WASM guest 编译工具链。P5.0 后续仍会创建 sample 与 build script, 但真实 D -> WASM artifact 编译会被标记为 blocked-on-toolchain, 不伪造成功产物。

## 6. Build Artifact

D guest sample 已写入:

```text
Plugins/AvidScript/Samples/D/ActorSetLocation/actor_set_location_guest.d
Plugins/AvidScript/Samples/D/ActorSetLocation/README.md
```

build script 已写入:

```text
Plugins/AvidScript/Build/BuildDGuestActorSetLocation.ps1
```

已运行:

```powershell
powershell -ExecutionPolicy Bypass -File "C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\Plugins\AvidScript\Build\BuildDGuestActorSetLocation.ps1" -Slot 1 -Generation 1 -X 123 -Y 456 -Z 789
```

结果:

```text
[AvidScript][DGuest][Build] generated_source=C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\Saved\AvidScriptDGuest\Generated\actor_set_location_guest.generated.d
[AvidScript][DGuest][Build] result=missing_toolchain missing=ldc2,wasm-ld output=C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\Saved\AvidScriptDGuest\actor_set_location_guest.wasm
```

artifact 状态:

```text
C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\Saved\AvidScriptDGuest\actor_set_location_guest.wasm = missing as expected
```

结论: P5.0c 的 fail-closed 路径已验证。当前无法真实生成 D WASM artifact, 因为本机缺少 `ldc2` 和 `wasm-ld`。

注意: sample 当前只表达 D 源码层 ABI。D 编译器最终是否能把 WASM import module 写成 AvidScript Host 期望的 `avidscript`, 需要安装工具链后继续验证。

## 7. Runtime Automation

新增测试:

```text
Plugins/AvidScript/Source/AvidScriptRuntime/Private/Tests/AvidScriptDGuestToolchainTests.cpp
AvidScript.Guest.D.ExternalActorSetLocationSmoke
```

模块级 build:

```powershell
& "C:\UnrealEngine\Engine\Build\BatchFiles\Build.bat" AvidTPSTemplateEditor Win64 Development "-Project=C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\AvidTPSTemplate.uproject" -WaitMutex -NoHotReloadFromIDE -NoUBTMakefiles -Module=AvidScriptRuntime
```

结果:

```text
Invalidating makefile for AvidTPSTemplateEditor (source file added)
[1/3] Compile [x64] Module.AvidScriptRuntime.cpp
[2/3] Link [x64] UnrealEditor-AvidScriptRuntime.lib
[3/3] Link [x64] UnrealEditor-AvidScriptRuntime.dll
Result: Succeeded
```

自动化命令:

```powershell
& "C:\UnrealEngine\Engine\Binaries\Win64\UnrealEditor-Cmd.exe" "C:\Users\user0\Documents\Unreal Projects\AvidTPSTemplate\AvidTPSTemplate.uproject" -Unattended -NullRHI -NoSplash -NoSound -NoP4 -NoLiveCoding -DDC-ForceMemoryCache -stdout -FullStdOutLogOutput -FORCELOGFLUSH -CrashForUAT "-ExecCmds=Automation RunTests AvidScript.Guest.D" "-TestExit=Automation Test Queue Empty" "-abslog=C:\tmp\AvidScript_P5_0_DGuest_Automation.log"
```

结果:

```text
Found 1 automation tests based on 'AvidScript.Guest.D'
AvidScript.Guest.D.ExternalActorSetLocationSmoke
Test Completed. Result={Success}
Warning: D guest WASM artifact is missing because local D/WASM toolchain is documented as missing.
Automation Test Queue Empty 1 tests performed.
```

结论: P5.0d 的 automation gate 已可用。当前没有真实 D artifact, 因此测试走 documented missing-toolchain warning path。安装 `ldc2` + `wasm-ld` 并生成 artifact 后, 同一测试会切换到真实 WASM 加载和 Actor location 验证路径。

## 8. Decision

P5.0 结论: Done, blocked-on-local-toolchain for real D artifact.

当前证据:

```text
Toolchain probe: ldc2/dub/wasm-ld/clang missing from PATH
Build script: result=missing_toolchain missing=ldc2,wasm-ld
D guest artifact: not generated
AvidScript.Guest.D automation: 1 test Success, warning path because artifact is absent and blocker is documented
Full AvidScript regression: 30 tests performed, Success
```

D 路线判断:

- 不切 C# fallback。
- D-first 方向保留, 但还没有完成真实 D -> WASM artifact 证据。
- 下一步需要安装或暴露 `ldc2` 与 `wasm-ld` 到 PATH, 然后重跑 `BuildDGuestActorSetLocation.ps1` 和 `AvidScript.Guest.D`。
- 如果生成 artifact 后出现 WASM import module mismatch, 优先解决 D/LDC 的 import module 映射, 再考虑 Host alias 或 C# fallback。

C# fallback 状态:

- 未触发。
- 触发条件仍是: D 无法稳定生成 freestanding WASM、产物依赖过重、或移动端/工作流风险明显高于收益。
