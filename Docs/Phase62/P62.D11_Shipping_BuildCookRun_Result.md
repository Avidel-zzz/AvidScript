# P62.D11 Win64 Shipping BuildCookRun 结果

## 结论

UE5.8 源码版 Win64 Shipping 的 C# Release、Game Build、Cook、Stage、Archive、
package receipt 与打包程序内游戏逻辑 Oracle 已形成真实闭环并通过。

## 发布与回执

- Generated Type runtime module：`avidscript_generated`，package `116ecb30a340d059fbafa51a418b838f02132f0d27afd60dc90700b5be3203ed`。
- 游戏逻辑 module：`avidscript_phase62_gameplay`，package `4d5c323402baf3bf9844d8ee4018767dc9bdbae7ba8b5c8fcde01178cd190cdc`。
- Shipping 使用 `require_precompiled`，实际加载 Wasmtime `.cwasm`，不依赖运行时 JIT 编译。
- Game receipt 经 UAT 输出身份确认后复用，记录 2 个模块、18 项依赖：16 项 UFS 与 2 项 NonUFS。
- `wasmtime.dll` 与许可证作为 NonUFS 依赖进入回执，DLL 哈希通过验证。

## 打包程序 Oracle

- 逻辑运行时间约 `300 ms`，健康 Session 完成 553 次 Tick。
- `BeginPlay`、`EndPlay`、事件、Timer 与 continuation 均被观察到。
- 生成式 Reflection API 修改 Actor scale，最终约为 `(2.30, 65, 54)`。
- 故障 Session 在事件 `62004` 中触发真实 `integer divide by zero` WASM trap。
- 故障 Session 被隔离后，健康 Session 继续运行；诊断类别为 `guest_trap`。

Shipping 的 `ProjectSavedDir` 位于用户数据目录，机器可读报告位于：

`%LOCALAPPDATA%/AvidTPSTemplate/Saved/AvidScript/PackagedOracle/Shipping-3432-23f161cabd014453a2aed095f675ef94.json`

归档输出位于项目目录：

`Saved/AvidScript/P62D/Shipping/ArchiveRetry3`

## 修复的发布边界

- Oracle 直接启动配置对应的真实 Game binary，并补齐 UE bootstrap 的项目标识。
- Shipping 报告写入真实 `ProjectSavedDir`，继续执行运行时路径边界校验。
- C++ package identity 与发布器统一：可选 `debug_map` 缺失时不再向身份预像追加空字段。

下一步进入 Phase 62 集中 Gate；移动端 AOT 属于 Phase 63。
