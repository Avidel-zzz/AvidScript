# P62.D7 打包故障隔离探针

## 问题

旧探针使用负事件 ID。宿主边界会按公共事件契约拒绝负数，因此调用虽然返回失败，
却没有进入 WASM，也没有验证 VM trap 后的 Session 隔离。

## 实现

- 探针改用未占用的正事件 ID `62004`，满足宿主事件参数契约。
- C# 脚本收到该事件后执行可确定的整数除零，产生真实 WASM trap。
- oracle 要求故障 Session 卸载并记录错误，同时健康 Session 的 Tick 继续增长。

该探针区分“输入被宿主拒绝”和“脚本执行故障被隔离”，确保打包验收覆盖后者。
